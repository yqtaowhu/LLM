# 大模型解码策略（LLM Decoding Strategies）

## 目录
- [1.概述](#1概述)
- [2.核心原理](#2核心原理)
- [3.重要性质](#3重要性质)
- [4.与其他概念的关系](#4与其他概念的关系)
- [5.具体例子](#5具体例子)
- [6.常见面试问题](#6常见面试问题)
- [参考资料](#参考资料)

## 1.概述

**一句话定义**：解码策略是大模型自回归生成文本时，**从每一步的概率分布中挑选下一个 token 的规则**——同样一个训练好的模型，换个解码策略，输出质量、多样性、速度可能天差地别。

**为什么重要**：
- 模型每步输出的不是"答案"，而是一个覆盖整个词表的概率分布 $p(x_t \mid x_{<t})$。**怎么从这个分布里选 token，本身就是模型能力之外的一个独立设计维度**；
- 同一个模型：任务型场景（翻译、代码）用确定性强的方法（greedy/beam），开放型场景（写作、闲聊）用随机采样（top-p/top-k），选错了要么输出千篇一律、要么胡言乱语；
- 解码策略是**零成本可调的**：不用改模型、不用重新训练，只改推理时的采样规则就能显著改变输出行为——工程上性价比最高的"调参点"。

一个容易忽略的事实：**训练好的大模型本身是"条件概率机器"，而用户看到的回答是"概率分布 + 解码策略"共同决定的产物**。GPT-4 的高质量回答，一半功劳在模型，另一半在采样参数怎么配。

## 2.核心原理

### 2.1 问题的形式化：自回归生成

大模型生成文本是逐 token 进行的。给定已生成的序列 $x_{<t}$，模型输出词表上每个 token 的 logits $z_i$，经 softmax 得到条件概率分布：

$$p(x_t = i \mid x_{<t}) = \frac{\exp(z_i)}{\sum_j \exp(z_j)}$$

解码策略回答的问题是：**有了 $p$，下一个 token 选谁？**

$$\boxed{\text{确定性解码（选一个固定答案）} \quad \text{vs} \quad \text{随机采样（按概率抽签）}}$$

这两大类构成了所有解码方法的设计光谱。确定性方法追求"整体最优"，随机方法追求"保真 + 多样"。

### 2.2 Greedy Search（贪心解码）

每一步都选概率最大的 token：

$$x_t = \arg\max_i \; p(x_t = i \mid x_{<t})$$

- **优点**：实现最简单（一行 `argmax`）、输出完全确定（同一输入永远同一输出）、无采样开销、可复现（利于调试和评测）；
- **缺点**：只看眼前一步，是**局部最优**，不保证全局最优——第 1 步选了概率最高的词，可能导致整句后面只能接出糟糕的续写；开放式生成下容易陷入**重复循环**（"很好很好很好……"）。

### 2.3 Beam Search（束搜索）：确定性的"多路下注"

Greedy 每步只保留 1 条路径，Beam Search 保留 $k$ 条（beam width）：

**步骤**：
1. 每一步从当前 $k$ 条候选序列出发，各自扩展出词表上所有可能的下一个 token；
2. 计算每条扩展序列的**累计对数概率** $\sum_t \log p(x_t \mid x_{<t})$；
3. 保留总分最高的 $k$ 条，其余全部剪枝；
4. 重复直到全部候选遇到终止符（EOS），最终返回累计概率最高的那条。

与 greedy 的关系：**beam width $k=1$ 时 beam search 退化为 greedy**。

核心区别在于"悔棋"能力：greedy 像走一条不回头的路，第 1 步选错，后面再好也没用；Beam Search 像派出 $k$ 支探险队同时探路——第 1 步没被选中的方向，只要后续表现出色随时可以反超，因为排序依据是**整条序列的累计概率**，而不是当前一步的概率。看一个能手算的例子：

**手算例子（beam width $k=2$）**：模型续写空白开头，简化词表只有 4 个词：`你、我、是、谁`。

第 1 步，模型给出开头概率：$p(\text{你}) = 0.5$，$p(\text{我}) = 0.4$。Greedy 只留"你"（局部第一）；Beam($k{=}2$) 留两条：`你`(0.5)、`我`(0.4)。

第 2 步，两条路各自扩展：

| 候选序列 | 累计概率 |
|---|---|
| `你` + `是` | $0.5 \times 0.5 = 0.25$ |
| `你` + `谁` | $0.5 \times 0.2 = 0.10$ |
| `我` + `是` | $0.4 \times 0.9 = 0.36$ |
| `我` + `谁` | $0.4 \times 0.05 = 0.02$ |

保留总分前 2：`我是`(0.36)、`你是`(0.25)。注意：**`我是` 反超成了第一名**——"我"在第 1 步是局部第二（$0.4 < 0.5$），但接上"是"后概率高达 0.9，总分反而最高。Greedy 已经把"我"这条路扔了，永远追不回来。

第 3 步，继续扩展：`我是`+`谁` $= 0.36 \times 0.8 = 0.288$，`你是`+`谁` $= 0.25 \times 0.6 = 0.15$。

**结果解读**：

- Greedy 输出 `你→是→谁`，总概率 $0.5 \times 0.5 \times 0.6 = 0.15$；
- Beam($k{=}2$) 输出 `我→是→谁`，总概率 $0.36 \times 0.8 = 0.288$，几乎翻倍。

Beam 找到了明显更好的整句，只因它在第 1 步"多下了一注"——这就是"确定性多路下注"的含义：输出仍然完全确定（没有随机性），但用 $k$ 倍的内存和计算换来**全局更优**的近似解。$k$ 越大越接近真正的全局最优（枚举所有路径），开销也线性增长。

**长度偏置问题**：直接比较累计 log 概率会**天然偏爱短句**（序列越长，累乘的概率只会越小，log 越负）。翻译任务中这个问题很致命——模型会输出截断的半句话。所以实际都加**长度惩罚（length penalty）**：

$$\text{score}(y) = \frac{1}{\mathrm{len}(y)^{\alpha}} \sum_t \log p(y_t \mid y_{<t}), \qquad \alpha \in [0, 1]$$

$\alpha = 0$ 不做惩罚（偏爱短句），$\alpha = 1$ 按长度平均（GNMT 的做法），中间值做温和补偿。

**为什么机器翻译爱用 Beam Search**：翻译有相对唯一的标准答案，概率最高的序列往往就是最好的译文；且整句可以从左到右稳定推进，没有"越写越偏"的风险。

**为什么开放式生成不爱用**：详见 3.3 节——高概率 ≠ 高质量。

### 2.4 Temperature（温度）：控制分布的"锐度"

在 softmax 之前，把 logits 除以温度 $T > 0$：

$$p_T(x_t = i) = \frac{\exp(z_i / T)}{\sum_j \exp(z_j / T)}$$

- $T \to 0^+$：分布趋向 one-hot，等价于 greedy；
- $T = 1$：原始分布，不改变模型输出；
- $T > 1$：分布被"压平"，小概率 token 获得更多出场机会，输出更多样、更"有创意"，但胡说八道（幻觉）的概率也上升。

直觉：**温度是给模型"自信度"调的旋钮**——低温让模型只说最有把握的话（刻板），高温让模型敢于冒险（发散）。名字来自物理中的玻尔兹曼分布，$T$ 越高系统越"混乱"。

### 2.5 Top-k：固定候选数量截断

只保留概率最大的 $k$ 个 token，把它们重新归一化后在里面采样：

$$S_k = \{i : p_i \text{ 排进前 } k\}, \qquad p_i' = \frac{p_i \cdot \mathbb{1}[i \in S_k]}{\sum_{j \in S_k} p_j}$$

- 解决了"长尾采样抽到离谱 token"的问题（原始分布中概率 $10^{-5}$ 量级的 token 被直接排除）；
- **固有缺陷**：$k$ 是固定的，与分布的实际形态无关。当模型很自信（前 1 个 token 占 95%）时，$k=50$ 会强行保留一堆没意义的候选；当模型很犹豫（前 100 个 token 才占 80%）时，$k=50$ 又截得太狠。

### 2.6 Top-p（Nucleus Sampling，核采样）：按累计概率截断

保留**累计概率恰好达到 $p$（如 0.9）的最小 token 集合**，按概率降序累加，一旦总和 $\ge p$ 就停止，然后归一化采样：

$$S_p = \arg\min_{S} \; \mathrm{size}(S) \quad \text{s.t.} \quad \sum_{i \in S} p_i \ge p$$

- **动态候选数**：模型自信时（分布尖锐）可能只留 3~5 个 token；模型犹豫时（分布平坦）可能留几百个——**候选集大小自适应分布形态**，这是它相对 top-k 的核心优势；
- OpenAI API 里的 `top_p` 参数就是它。实际系统中 top-p 与 temperature 经常组合使用：先温度重塑分布，再 top-p 截断。

### 2.7 其他常见策略（了解即可）

- **Min-p**：保留 $p_i \ge p \cdot p_{\max}$ 的 token（$p_{\max}$ 是当前最大概率）。相当于一个"随最大概率缩放"的动态阈值——模型自信时截得狠，犹豫时放得开，比 top-p 更自适应，近年社区（llama.cpp、vLLM）流行；
- **Typical Sampling**：只保留信息量接近模型平均信息量（熵）的 token，动机是模拟人类语言"不选太意外的、也不选完全可预测的"；
- **重复惩罚**：
  - repetition penalty：对已出现过的 token 的 logits 除以惩罚系数 $\gamma$（如 1.1），压低其概率；
  - frequency penalty / presence penalty（OpenAI 系）：分别按"出现次数"和"是否出现过"线性扣减 logits；
  - no-repeat-ngram：禁止重复出现过的 n-gram，简单粗暴；
- **对比搜索（Contrastive Search）**：同时要求候选与已生成内容"高模型概率 + 低相似度"（退化惩罚），兼顾质量与多样性。

## 3.重要性质

### 3.1 解码策略不改变模型分布，只改变"抽取方式"

温度是唯一真正**修改**概率分布的参数（重塑 softmax）；top-k、top-p、min-p 只是**截断 + 归一化**，作用在截断后的子分布上。这个区分在面试里很加分：

- 采样前分布：$p$（或温度重塑后的 $p_T$）；
- 截断：从 $\mathrm{supp}(p)$ 中选子集 $S$；
- 最终分布：$p'(i) = p(i) / \sum_{j \in S} p_j, \; i \in S$。

### 3.2 各策略的确定性谱系

$$\text{greedy} \;\leftrightarrow\; T \to 0 \;\leftrightarrow\; \text{beam}(k{=}1) \quad \text{都是确定性的极限}$$

关键联系：**低温采样在 $T \to 0$ 时收敛到 greedy**；top-p 在 $p \to 0$ 时（只留最大概率 token）也收敛到 greedy。所以 greedy 可以看作所有采样策略的低温/零截断特例。

### 3.3 高概率 ≠ 高质量：Beam Search 的开放生成陷阱

这是解码策略里最经典的反直觉结论。研究表明（Holtzman et al., 2020, *The Curious Case of Neural Text Degeneration*）：最大化整体概率的文本往往是**重复、空洞、模板化**的（"I don't know I don't know I don't know..."），因为自然语言中"人类会写的句子"是高概率但**非最高概率**的；最高概率的序列往往落在模型的"复读机吸引子"里。

原因直觉：语言的真实分布是**重尾**的——我们刻意追求"最可能的下一个词"，会让句子的信息熵逐句下降，最终坍缩成重复。这也是为什么：
- 任务型生成（输入 → 唯一答案）→ 用 beam/greedy；
- 开放式生成（一个 prompt → 无数合理答案）→ 用 top-p/温度采样。

### 3.4 各方法横向对比

| 策略 | 确定性 | 候选集 | 自适应分布 | 典型场景 |
|---|---|---|---|---|
| Greedy | 完全确定 | 1 个 | 否 | 分类评测、代码补全、复现实验 |
| Beam Search | 完全确定 | $k$ 条整体序列 | 否 | 翻译、摘要、结构化生成 |
| Temperature | 随机 | 全词表 | 否 | 多样性全局旋钮，常与其他组合 |
| Top-k | 随机 | 固定 $k$ 个 | 否 | 简单防长尾，需调 $k$ |
| Top-p | 随机 | 动态（累计概率定） | 是 | 开放式对话、写作的事实标准 |
| Min-p | 随机 | 动态（随 $p_{\max}$ 定） | 更自适应 | 高温度下防胡说的现代方案 |

### 3.5 组合使用是工业界常态

实际推理框架（vLLM、HuggingFace generate、TGI）的解码是**多参数协同的流水线**，典型顺序：

1. logits → 加重复惩罚 / frequency penalty；
2. 除以 temperature 重塑分布；
3. top-k 截断（可选）；
4. top-p / min-p 截断（可选）；
5. 归一化、采样、拼接进上下文。

所以面试题中"temperature 和 top-p 能同时用吗"——不仅能，而且是标配：**temperature 负责"多冒险"，top-p 负责"别太离谱"**，二者分工互补。

## 4.与其他概念的关系

- **与训练的关系**：训练用 teacher forcing，模型只学 $p(x_t \mid x_{<t})$；解码策略完全在推理期，不涉及任何参数更新。同一段训练代码可以配完全不同的解码行为；
- **与温度的联系（softmax 统一视角）**：分类模型推理时的 softmax 温度、蒸馏中的温度（Hinton KD）、解码温度是同一数学操作——logits 除以 $T$ 再 softmax——但用途不同（校准 / 软标签 / 采样多样性）；
- **与 RLHF/对齐的关系**：RLHF 改变的是模型本身输出的分布（让好回答概率更高），解码策略是在给定分布上做选择——二者是"改变概率"与"利用概率"的关系。注意：RLHF 后模型输出的分布与 base model 已经不同，同一组采样参数在 SFT 前后表现可能完全不同；
- **与推理加速的区别**：Speculative Decoding（投机解码）、KV Cache、量化等是**加速手段**，其中投机解码用小模型起草 + 大模型验证，数学上保证最终输出分布与目标模型采样完全一致——即"加速但不改变分布"；而解码策略是**改变输出分布本身**。这两类正交，常被面试官拿来对比；
- **与 Beam Search 在 NMT 传统中的地位**：2017 年前的 seq2seq 时代 beam search 几乎是解码的代名词，LLM 时代因开放式生成成为主流，beam search 退守到翻译、结构化输出等窄场景——这是理解"策略要匹配任务"的最佳历史注脚。

## 5.具体例子

### 例 1：Temperature 如何重塑分布

**问题**：模型在某一步对 4 个候选 token 的 logits 为 $\mathbf{z} = [4.0,\ 2.0,\ 1.0,\ 0.5]$，分析不同温度下采样行为的变化。

**步骤**：分别取 $T = 0.5,\ 1,\ 2$，计算 $p_i = \frac{\exp(z_i/T)}{\sum_j \exp(z_j/T)}$。

**计算**：
- $T = 0.5$：logits 变为 $[8.0, 4.0, 2.0, 1.0]$，$\exp$ 后 $[2981, 54.6, 7.39, 2.72]$，总和约 $3045.7$，分布约为 $[0.978,\ 0.018,\ 0.0024,\ 0.0009]$；
- $T = 1$：$\exp$ 后 $[54.6, 7.39, 2.72, 1.65]$，总和约 $66.4$，分布约为 $[0.823,\ 0.111,\ 0.041,\ 0.025]$；
- $T = 2$：logits 变为 $[2.0, 1.0, 0.5, 0.25]$，$\exp$ 后 $[7.39, 2.72, 1.65, 1.28]$，总和约 $13.0$，分布约为 $[0.568,\ 0.209,\ 0.127,\ 0.098]$。

**结果解读**：低温下第 1 个 token 几乎垄断（97.8%），采样近似 greedy；标准温度下它仍有 82.3% 但其他 token 有出头机会；高温下头部被稀释到 56.8%，尾部 token 从千分之几抬到约 10%——**温度不改变排名，只改变"贫富差距"**。

### 例 2：Top-k 与 Top-p 在同一分布上的截断差异

**问题**：接例 1 的 $T=1$ 分布 $p \approx [0.823,\ 0.111,\ 0.041,\ 0.025]$（假设词表只有 4 个词，实际 LLM 中长尾更长），分别用 $k=2$ 和 $p=0.9$ 截断，结果有何不同？

**步骤**：Top-k 按排名取前 $k$ 个；Top-p 按概率降序累加，到累计值 $\ge p$ 停止。

**计算**：
- Top-2：保留前两个 token，候选集 $\{x_1, x_2\}$，归一化后为 $[0.823/0.934,\ 0.111/0.934] \approx [0.881,\ 0.119]$；
- Top-0.9：降序累加：$x_1 \to 0.823$，加 $x_2 \to 0.934 \ge 0.9$ 停止。候选集同为 $\{x_1, x_2\}$，结果与 Top-2 相同。

**结果解读**：分布尖锐时 $k=2$ 和 $p=0.9$ 恰好等价。但换一个平坦分布 $p = [0.3, 0.28, 0.22, 0.2]$（模型很犹豫）：Top-2 强行扔掉后两个各占 20% 的合理候选，而 Top-0.9 会保留全部 4 个（$0.3+0.28+0.22 = 0.8 < 0.9$，需要第 4 个才到 1.0）——**这正是 top-p 自适应的优势所在**。

### 例 3：Beam Search 的长度偏置

**问题**：候选译文 A（8 个词，累计 log 概率 $-9.2$）与译文 B（5 个词，累计 log 概率 $-6.8$），直接比总分选谁？加长度惩罚（$\alpha = 1$）呢？

**步骤与计算**：
- 直接比较：$-6.8 > -9.2$，选短句 B；
- 长度归一化后：$\mathrm{score}_A = -9.2 / 8 = -1.15$，$\mathrm{score}_B = -6.8 / 5 = -1.36$，$-1.15 > -1.36$，选长句 A。

**结果解读**：不加惩罚 beam search 系统性偏爱短输出（甚至输出残缺译文）；加 $\alpha=1$ 后按"平均每词质量"比较，结论反转。这也解释了为什么 HuggingFace `generate` 默认带 `length_penalty` 参数。

## 6.常见面试问题

**Q1：为什么训练时用 argmax/teacher forcing 就够，推理时却需要各种解码策略？**

答：训练时模型学的是"给定前文，真实下一个 token 是什么"的条件分布，损失只需要分布本身（交叉熵），不涉及"怎么选"；推理时要真正**生成**序列，每一步的选择会被后续步骤继承——选错一个 token 会把整段生成带偏（exposure bias 的推理端体现）。而且不同任务对"确定性 vs 多样性"的需求不同，必须靠解码策略显式控制采样行为，这是模型权重无法直接表达的。

**Q2：Greedy、Beam Search、采样的本质区别是什么？**

答：Greedy 是逐步局部最优（每步取 argmax）；Beam Search 是在"整体序列概率"意义下做近似全局最优（保留 $k$ 条最优路径，$k=1$ 时退化为 greedy）；采样是按（截断/重塑后的）分布随机抽取，牺牲确定性换取多样性和对分布真实形态的忠实。前两者输出确定，后者输出随机；前两者追求高概率序列，后者承认"最高概率 ≠ 最好文本"。

**Q3：写出 Temperature 的公式，分析 $T \to 0$ 和 $T \to \infty$ 两个极端。**

答：$p_T(i) = \frac{\exp(z_i/T)}{\sum_j \exp(z_j/T)}$。$T \to 0^+$ 时，$z_i/T$ 之间的差被无限放大，最大 logit 的项指数级主导，分布趋于 one-hot，等价于 greedy decoding；$T \to \infty$ 时，$z_i/T \to 0$，所有项趋于 $\exp(0)=1$，分布趋于均匀分布，采样完全随机。关键直觉：**温度改变 logits 的相对差距，但不改变排序**——softmax 对 logits 加常数不变，但除以 $T$ 会缩放差距。

**Q4：Top-k 和 Top-p 的区别？各自的优缺点？**

答：Top-k 保留**固定数量**的头部候选；Top-p 保留**累计概率达到阈值**的最小候选集。Top-k 的优点是实现简单、计算量可预知（显存/延迟稳定），缺点是 $k$ 与分布形态脱节——分布尖锐时留了太多垃圾候选，平坦时又截掉合理选项；Top-p 候选数自适应分布（自信时留几个、犹豫时留几十上百个），是开放式生成的默认选择，缺点是截断集合大小随每步波动，且当分布长尾极重时（每个 token 都不到阈值）理论上会保留过多候选。现代实践中常用 top-k 兜底 + top-p 精调的组合。

**Q5：为什么 Beam Search 在机器翻译里是标配，在开放式对话生成里反而效果差？**

答：翻译是"输入约束下的单解任务"，概率最高的序列大概率就是正确译文，beam search 恰好在"找高概率序列"这件事上做到了近似最优；且翻译句子短、结构规整，模型概率与人工评价高度相关。开放式生成中，"最高概率的文本"往往重复、空洞、模板化（神经文本退化现象）——因为语言真实分布重尾，逐字选 argmax 会让信息熵坍缩；人类写的文本是"高概率但非最高概率"的。加上 beam search 输出多样性差（k 条候选高度相似）、计算开销大，所以对话/写作场景用 top-p + temperature 采样。

**Q6：Beam Search 为什么需要 length penalty？不加会发生什么？**

答：序列联合概率是逐步条件概率的连乘，越长越小于 1，累计 log 概率随长度单调下降——直接比较总分会让 beam search 系统性偏爱短序列，输出被截断的残句。Length penalty 把总分做长度归一化：$\mathrm{score} = \frac{1}{\mathrm{len}^{\alpha}} \sum_t \log p(y_t \mid y_{<t})$，$\alpha \in [0,1]$ 控制惩罚强度。数值上：$-9.2/8 = -1.15$ 可能反超 $-6.8/5 = -1.36$（见例 3），结论因此反转。

**Q7：Temperature、repetition penalty、frequency penalty、presence penalty 分别解决什么问题？**

答：Temperature 解决**整体分布锐度**问题（多样性与质量的权衡）；repetition penalty 对已出现 token 的 logits 做乘性/除性惩罚（如 $\gamma = 1.1$ 时 $z' = z / \gamma$，或对负 logit 反向处理），缓解复读循环；frequency penalty 按 token 出现**次数**线性扣减 logits（$z' = z - c \cdot \mathrm{count}$），抑制高频重复词；presence penalty 只看**是否出现过**（出现一次即扣固定值），鼓励聊新话题。三者粒度递进：温度是全局的、repetition penalty 是二值的强化版、frequency 是计数的、presence 是布尔的。

**Q8：同一个 LLM，温度调高后回答质量下降、胡编乱造变多，从数学和业务角度解释原因，并给出工程上的缓解方案。**

答：数学上，$T > 1$ 压平分布，长尾低质量 token（现实中错误实体、虚构事实）的概率被等比抬升，采样到它们的几率上升——幻觉本质上很多是"采样到了模型的弱记忆区"。业务上，问答、代码等任务需要事实性，随机性有害。缓解方案：降低温度或改用更自适应的截断（top-p 收紧到 0.8~0.9、min-p）；引入 RAG 让模型分布本身偏向正确证据；对事实性强的输出可降低温度、对创意文案再调高——**按任务分桶配置采样参数**是工业界标准做法。

**Q9：Speculative Decoding 和解码策略是什么关系？它能和 temperature 采样共存吗？**

答：二者正交。Speculative decoding 是**推理加速**手段：小模型（draft）快速起草 $n$ 个 token，大模型（target）并行验证，拒绝-接受采样（accept/reject）保证最终输出分布**数学上严格等于**目标模型直接采样的分布——即"加速但不改变分布"；解码策略是**改变分布本身**（温度重塑、截断）。它们可以共存：只要验证阶段使用同样的带温度/截断的目标分布，就能在保持该解码行为的同时加速。这是"改变输出"（策略）与"更快输出"（加速）的经典对比题。

**Q10（进阶）：为什么 top-p 在分布极平坦时会保留大量候选？Min-p 如何解决？对比搜索的核心思想是什么？**

答：Top-p 的停止条件是累计概率 $\ge p$，若模型对下一步极度不确定（每个 token 概率都只有 0.01 量级），需要上百个 token 才能凑够 0.9，截断形同虚设，长尾噪声全部进入候选。Min-p 改用**相对阈值**：保留 $p_i \ge p \cdot p_{\max}$ 的 token——阈值随当前最大概率自动缩放，自信时（$p_{\max}$ 大）截得紧，犹豫时（$p_{\max}$ 小）截得也相对合理，尤其适合与高温度组合。对比搜索（Contrastive Search）则另辟蹊径：从候选中选"模型似然高 + 与已生成上下文表示相似度低"的 token（退化惩罚项），从表示层面抑制重复，兼顾确定性的质量与采样的多样性。

## 参考资料

- Holtzman et al., *The Curious Case of Neural Text Degeneration*, ICLR 2020（Top-p / Nucleus Sampling 原始论文）
- Fan et al., *Hierarchical Neural Story Generation*, 2018（Top-k 采样、讲述 beam search 退化现象）
- Vaswani et al., *Attention Is All You Need*, 2017（Beam Search 在 NMT 中的应用与长度惩罚）
- Su et al., *A Contrastive Framework for Neural Text Generation*, NeurIPS 2022（Contrastive Search）
- Nguyen, *Min P Sampling: Balancing Creativity and Coherence at High Temperature*, 2024（Min-p）
- HuggingFace Transformers 文档：*Generation strategies*（各参数的工程实现）
