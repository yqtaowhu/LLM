# 提示词工程（Prompt Engineering）

## 目录
- [1.概述](#1概述)
- [2.核心原理](#2核心原理)
- [3.重要性质](#3重要性质)
- [4.与其他概念的关系](#4与其他概念的关系)
- [5.具体例子](#5具体例子)
- [6.常见面试问题](#6常见面试问题)
- [参考资料](#参考资料)

## 1.概述

**一句话定义**：提示词工程是通过精心设计输入文本（提示词/Prompt）来引导大语言模型产出期望行为的实用技术——不改动任何模型权重，仅靠"把话说对"就能大幅改变输出质量。

**为什么重要**：
- 门槛最低、见效最快的模型能力解锁方式：一个更好的 prompt 可能等价于几次完整微调的效果；
- 在 In-Context Learning（上下文学习）范式中，模型的能力大部分在预训练阶段就已"潜伏"，prompt 只是把它们"唤醒"出来，因此写 prompt 的方式直接决定能唤醒多少；
- 工业落地中，prompt 是介于"训练"与"产品"之间的接口层：可版本管理、可 A/B 测试、可热更新，比微调灵活得多。

## 2.核心原理

### 2.1 Prompt 的基本解剖结构

一个完整的提示词通常由以下部分叠加而成：

```
[System / Instruction]   你是一个电商客服助手，只回答售后相关问题。
[Context / Knowledge]    <检索到的商品政策、用户历史对话等>
[Few-shot Examples]      输入: ... → 输出: ...（示范）
[Task Input]             用户问题：我的订单三天了还没发货怎么办？
[Output Format Spec]     请以 JSON 输出，字段: intent, reply, escalate
```

各部分的作用与优先级经验：
- **指令（Instruction）**：告诉模型"做什么"，越具体越好（含角色、约束、输出格式）；
- **上下文（Context）**：提供模型完成任务所需的外部信息，是 RAG 的载体；
- **示例（Few-shot）**：用输入→输出对示范目标行为，效果常强于纯文字描述；
- **格式约束**：明确要求输出格式（JSON / Markdown 表格 / 固定字段），可显著提升下游解析成功率。

### 2.2 In-Context Learning（ICL）：为什么会"照猫画虎"

ICL 的本质：prompt 中的示例构成了一个隐式的条件分布 $p(y \mid \text{demonstrations}, x)$。模型在预训练中见过海量"输入→输出"模式的连续文本，few-shot 示例相当于把模型"带入"了一个特定的数据分布，它自然按该分布续写。

数学上看，训练时模型学到的其实是：

$$p_\theta(y \mid x, c)$$

其中 $c$ 是上下文（示例、指令的拼接）。ICL 没有梯度更新，但 $c$ 的变化会改变整个条件分布——**这等价于一次极其廉价的"软微调"**。

### 2.3 思维链（Chain-of-Thought, CoT）：给模型"草稿纸"

直接要求输出答案时，模型需要在**一次前向的输出分布**里同时完成推理和作答，中间步骤被压缩，错误率高。CoT 的思路：让模型先输出中间推理步骤，每一步生成都会成为后续步骤的条件，等价于把一次复杂计算拆成多次自回归调用：

$$p(\text{answer} \mid x) = \sum_{\text{thought}_1, \dots, \text{thought}_k} \prod_{i=1}^{k} p(\text{thought}_i \mid x, \text{thought}_{<i}) \cdot p(\text{answer} \mid x, \text{thought}_{1..k})$$

效果依据（Wei et al., 2022）：GSM8K 数学题上，PaLM 540B 标准提示仅 17.9%，加入 8-shot CoT 提升到 56.9%。

**变体**：
- **Zero-shot CoT**：仅加一句"Let's think step by step"（Kojima et al., 2022）；
- **Self-Consistency**：同一 prompt 采样多条推理路径，对最终答案做多数投票；
- **ReAct**：思考（Thought）与行动（Action，如调用搜索/工具）交替进行。

### 2.4 主流提示模式速览

| 模式 | 核心做法 | 适用场景 |
|---|---|---|
| Zero-shot | 只有指令 | 模型能力足够强的简单任务 |
| Few-shot | 附带 3~10 个示例 | 格式敏感、领域专用任务 |
| CoT / Zero-shot CoT | 引导逐步推理 | 数学、逻辑、多步决策 |
| Self-Consistency | 多路径采样 + 投票 | 答案可判定的推理题 |
| ReAct / Tool-use | 思考与工具调用交替 | 需要外部知识或实时信息 |
| 结构化输出 | 强制 JSON/Schema 输出 | 工程集成、Agent 编排 |
| 角色设定（Role prompting） | 系统提示中设定身份与约束 | 对话产品、垂直场景 |

### 2.5 写好 Prompt 的工程原则

1. **具体优于模糊**：把"写得好一点"换成"面向初中生、不超过 200 字、分 3 点"；
2. **正面指令**：说"只输出 JSON"，而不是"不要输出多余文字"——否定式约束易被忽略；
3. **把复杂任务拆成多轮**：一条 prompt 塞不下所有约束时，用流水线（先抽取、再总结、再校验）；
4. **让模型自己"思考再回答"**：允许先输出推理过程，再给最终答案（或用两个字段分隔）；
5. **提供失败样例与边界规则**：示例中包含易混淆的反例，减少系统性误判；
6. **迭代与评测**：prompt 改动必须挂在一个固定评测集上对比通过率，避免"感觉变好了"。

## 3.重要性质

**性质 1：示例比描述更有效，顺序和数量都有影响**
直觉：few-shot 示例同时定义了任务的"语义"和"输出分布"。研究表明示例顺序可导致准确率波动超过 10 个百分点；示例应与测试样本在标签分布、难度上尽量一致。

**性质 2：Prompt 对 token 位置敏感（lost in the middle）**
直觉：长上下文中，模型对开头和结尾的信息利用最好，中间部分容易被忽略。关键指令应放在开头或结尾，长文档检索场景需做位置重排。

**性质 3：提示词是"可传递的软件接口"**
直觉：prompt 具有版本、组合、复用等软件属性——这是它区别于传统参数调优的本质特征。许多框架（如 DSPy）已把 prompt 视为可被自动优化的程序参数。

**性质 4：温度等解码参数与 prompt 是互补关系**
直觉：$T$ 控制输出的随机性，prompt 控制输出分布的中心。创意写作用 $T > 0.8$ + 宽松指令；结构化抽取用 $T \approx 0$ + 严格格式约束，两者需协同调参。

**性质 5：CoT 对大模型有效、对小模型可能有害**
直觉：CoT 是一种涌现能力——模型推理能力不足时，"一本正经地写中间步骤"反而会放大错误，产生更流畅但更离谱的错误链条。经验上 10B 以下模型 CoT 收益不稳定。

## 4.与其他概念的关系

| 维度 | 提示词工程 | SFT 微调 | RAG | RLHF |
|---|---|---|---|---|
| 是否改权重 | 否 | 是 | 否（改上下文） | 是 |
| 成本 | 极低（文本） | 中高（数据标注 + 训练） | 中（检索系统建设） | 高 |
| 迭代速度 | 分钟级 | 天级 | 小时级 | 周级 |
| 注入新知识 | 弱（靠上下文塞） | 强（内化） | 强（实时检索） | 弱 |
| 适用信号量 | 有少量示例即可 | 数千~百万条 | 知识频繁更新 | 有偏好数据 |

**协作关系（而非替代）**：
- **Prompt + RAG**：prompt 提供任务格式与约束，RAG 提供知识，两者都在推理期生效；
- **Prompt 之后才微调**：先用 prompt 验证任务可行性（POC），当示例超预算、格式极难约束或需压缩上下文长度时，再升级为 SFT——此时 SFT 数据可直接用 prompt 跑出的高质量输出蒸馏；
- **Prompt + RLHF**：RLHF 改变模型"默认性格"，好的系统提示在此基础上进一步约束具体任务行为。

## 5.具体例子

### 例子 1：Few-shot 情感分类——从"随意描述"到"结构化提示"

**问题**：判断电商评价的情感（正面/负面），要求输出严格为 `正面` 或 `负面` 单词，供下游程序直接读取。

**步骤**：
1. V0 版 prompt：`"判断这条评论的情感倾向：{评论}"`；
2. V1 版：加指令 `"只输出'正面'或'负面'"`；
3. V2 版：再加 4 个 few-shot 示例，其中包含一个带反讽的反例。

**计算（模拟评测集 100 条的结果对比）**：

| 版本 | 输出格式合规数 | 分类准确数 |
|---|---|---|
| V0（裸指令） | 62 / 100 | 71 / 100 |
| V1（+格式约束） | 98 / 100 | 80 / 100 |
| V2（+含反例的 few-shot） | 99 / 100 | 88 / 100 |

**结果解读**：从 V0 到 V2，准确率提升 $88 - 71 = 17$ 个百分点，格式合规率从 62% 提升到 99%。提升最大的是格式合规（+36 个百分点）——说明**工程落地的第一瓶颈往往不是模型能力，而是输出能不能被程序稳定解析**。V2 中那条反讽反例贡献了大部分分类准确率提升，因为反例帮模型区分了"字面情感"和"真实情感"。

### 例子 2：CoT + Self-Consistency 解一道逻辑题

**问题**：一个笼子里有鸡和兔共 35 个头、94 只脚，问各多少只？

**步骤**：
1. Zero-shot 直接问，模型一步心算容易出错；
2. 加入 `"请一步步推理"`（Zero-shot CoT）；
3. 温度 $T = 0.7$ 下采样 5 条推理路径，对最终答案做多数投票（Self-Consistency，$k=5$）。

**计算（5 条路径的答案）**：

- 路径 1：设鸡 $x$ 只，则 $2x + 4(35-x) = 94 \Rightarrow 140 - 2x = 94 \Rightarrow x = 23$，兔 = 12 ✓
- 路径 2：假设全是鸡，$35 \times 2 = 70$ 只脚，多出 $94 - 70 = 24$ 只脚，每只兔多 2 脚，兔 $= 24 / 2 = 12$，鸡 23 ✓
- 路径 3：计算出错，得到鸡 21、兔 14 ✗
- 路径 4：同路径 2 方法，鸡 23、兔 12 ✓
- 路径 5：同路径 2 方法，鸡 23、兔 12 ✓

投票：`鸡 23、兔 12` 出现 4 次，`鸡 21、兔 14` 出现 1 次 → **多数答案：鸡 23 只，兔 12 只**。

**结果解读**：验证 $23 + 12 = 35$ 个头，$23 \times 2 + 12 \times 4 = 46 + 48 = 94$ 只脚，正确。5 条路径中即使 1 条出错，多数投票也能纠错——这就是 Self-Consistency 的价值：用**计算换准确率**，代价是推理成本变为 $k = 5$ 倍。

### 例子 3：结构化输出 + ReAct 打造客服工单分类 Agent

**问题**：客服系统需自动分类工单（退款/物流/咨询）并判断是否需要人工介入，信息可能需要查订单系统。

**步骤**：
1. 系统提示设定角色与可用工具：`可用工具: query_order(用户ID)`；
2. 要求按 ReAct 格式交替输出 `Thought` / `Action` / `Observation`；
3. 要求最终输出 JSON：`{"category": "...", "escalate": true/false}`；
4. 对 `escalate = true` 的样本路由给人工。

**计算（离线评测 200 条工单）**：
- 纯文本输出（人工复制粘贴）：分类耗时平均 45 秒/条；
- ReAct + JSON 结构化输出：自动处理 156/200 条（78%），平均耗时 4 秒/条；剩余 44 条正确流转人工。

**结果解读**：78% 的工单被自动化，单条处理时间从 45 秒降到 4 秒，整体人工工作量降为原来的 $44 / 200 = 22\%$。注意设计上**宁可误报 `escalate=true` 也不要漏报**——把"模糊样本交给人工"作为默认安全网，是生产环境 prompt 设计的重要纪律。

## 6.常见面试问题

**Q1：什么是提示词工程？它和传统机器学习调参有何本质区别？**

答：提示词工程是通过设计输入文本来控制大模型行为的技术，不改任何权重。区别在于：传统调参作用于模型参数空间，需要训练数据与训练流程；prompt 作用于**条件分布的上下文**，利用的是预训练模型的 ICL 能力，迭代成本为分钟级。本质区别是——前者改模型，后者改输入。

**Q2：解释 In-Context Learning 为什么不需要梯度更新也能"学习"？**

答：ICL 中示例 $c$ 与输入 $x$ 一起构成条件 $p_\theta(y \mid x, c)$。模型在预训练中见惯了"上下文提供模式、后续内容按模式续写"的数据，few-shot 示例相当于把激活的分布"搬运"到目标任务附近。参数没动，但**条件分布被上下文改写了**，行为上等价于隐式学习。有研究认为 ICL 在前向过程中实现了某种隐式的梯度下降（Implicit ICL 观点），可作为加分论述。

**Q3：为什么 CoT 能提升推理准确率？它有代价吗？**

答：直接作答要求模型在一次输出中完成全部推理，中间状态被压缩；CoT 让每步推理成为下一步的条件 $p(\text{thought}_i \mid x, \text{thought}_{<i})$，相当于把一次复杂计算分解为多次自回归步骤，每步的搜索空间更小、更可靠。代价：(1) 输出 token 数增加，推理延迟与成本上升；(2) 对小模型可能放大错误；(3) 某些纯知识型任务收益有限。

**Q4：Few-shot 示例的顺序、数量如何影响效果？**

答：顺序敏感：同一组示例不同排列可导致准确率波动 10 个百分点以上（Lu et al., 2022），建议按与测试样本的相似度排序，或用验证集选序。数量上：示例从 0 到 4~8 个收益最陡，之后边际递减并受上下文长度预算约束。标签分布不均衡的示例集会引入输出偏置。

**Q5：Zero-shot CoT 为什么一句"Let's think step by step"就有效？**

答：这句话把模型从"直接续写答案"的模式切换到"续写推理过程"的模式。预训练语料中包含大量逐步解释的文本，该触发词激活了这类分布；同时它把"生成答案"推迟到推理完成之后，利用了 CoT 的分步计算优势。它证明了**prompt 可以在不提供示例的情况下引导行为模式**。

**Q6：生产环境中如何系统化地迭代 prompt，而不是凭感觉改？**

答：把 prompt 当代码管理：(1) 建立固定评测集（覆盖典型 case + 边界 case + 历史 bad case）；(2) 定义可计算指标（格式合规率、准确率、人工审核通过率）；(3) 每次修改只动一个变量并跑评测对比；(4) 用回归测试防止"修好一处坏了另一处"；(5) 对关键 prompt 做版本管理与灰度上线。核心思想：**prompt 工程必须工程化，评测先行**。

**Q7：长 prompt 中为什么会出现"lost in the middle"？如何缓解？**

答：Liu et al. (2023) 发现模型对上下文开头和结尾的信息利用最好，中间位置检索与利用能力显著下降，呈 U 形曲线。缓解：(1) 关键指令放系统提示开头、格式要求放结尾；(2) 多文档 RAG 场景按相关性重排，最重要的放两端；(3) 压缩无关上下文；(4) 任务允许时改用多次短调用替代一次长调用。

**Q8：什么情况下应该从提示词工程升级到 SFT 微调？**

答：判断信号：(1) few-shot 需要的示例太多，上下文装不下或成本过高；(2) 输出格式极难约束，结构化输出频繁失败；(3) 需要稳定的领域风格或内化领域知识，而非每次都靠上下文注入；(4) 请求量大，长 prompt 的推理成本超过了微调的摊销成本。实操路径：先用 prompt 打通 POC，再用 prompt 跑出的高质量数据做蒸馏式 SFT，兼顾成本与效果。

**Q9：prompt 工程与 RAG 如何配合？各自负责什么？**

答：RAG 负责"知识正确"：检索与用户问题相关的文档塞入上下文，解决知识过时与幻觉。Prompt 负责"行为正确"：告诉模型如何引用检索结果（如"仅依据给定材料回答，材料中没有就说不知道"）、如何处理冲突信息、输出什么格式。常见失败是两者互相甩锅——检索质量差时再好的 prompt 也没用，所以要分层归因：先评测检索命中率，再评测生成忠实度。

**Q10：进阶：自动提示优化（如 APE、DSPy）的思路是什么？**

答：把 prompt 从"人工写的字符串"变成"可优化的变量"。APE 用 LLM 自己生成候选指令再按评测分数筛选（类似进化搜索）；DSPy 把任务声明式地定义为程序（签名 + 模块），框架自动编译出最优的 prompt 与 few-shot 组合。共同思想：**以评测指标为损失函数，把提示词搜索自动化**。这预示着提示词工程正在从"手艺"走向"自动化编译"，工程师的角色从写 prompt 转向定义任务与度量。

## 参考资料

- Wei et al., *Chain-of-Thought Prompting Elicits Reasoning in Large Language Models*, 2022
- Kojima et al., *Large Language Models are Zero-Shot Reasoners*, 2022
- Wang et al., *Self-Consistency Improves Chain of Thought Reasoning*, 2022
- Yao et al., *ReAct: Synergizing Reasoning and Acting in Language Models*, 2022
- Liu et al., *Lost in the Middle: How Language Models Use Long Contexts*, 2023
- Zhou et al., *Large Language Models are Human-Level Prompt Engineers*（APE）, 2022
- Khattab et al., *DSPy: Compiling Declarative Language Model Calls*, 2023
- OpenAI / Anthropic Prompt Engineering Guides（官方最佳实践文档）
