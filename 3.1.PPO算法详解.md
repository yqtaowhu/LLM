# PPO：Proximal Policy Optimization 近端策略优化

## 目录
- [1.概述](#1概述)
- [2.核心原理](#2核心原理)
- [3.重要性质](#3重要性质)
- [4.与其他概念的关系](#4与其他概念的关系)
- [5.具体例子](#5具体例子)
- [6.常见面试问题](#6常见面试问题)
- [参考资料](#参考资料)

## 1.概述

**一句话定义**：PPO（Proximal Policy Optimization，OpenAI 2017）是一种 **on-policy 策略梯度**算法，它通过把新旧策略的概率比裁剪在一个小区间内（如 $[0.8, 1.2]$），用最简单的一阶优化实现了「每次只小幅更新策略」这一信任域思想，兼顾稳定性和效率。

**为什么重要**：
- 训练稳定又好调：TRPO 要解带二阶信息的约束优化，PPO 只用 Adam/SGD 加一个 clip 操作，却拿到相近的性能；
- 工业事实标准：RLHF（InstructGPT/ChatGPT）、机器人控制、游戏 AI 中最广泛使用的强化学习算法，是 DeepSeek-R1 出现前大模型对齐的主流选择。

## 2.核心原理

### 2.1 出发点：策略梯度与其高方差问题

#### 先补齐 5 个前置概念（看不懂公式多半是缺这里）

在监督学习里，模型直接告诉你「正确答案是啥」，loss 一算就能更新。但强化学习的场景是：**没人告诉你每一步该做什么，只知道最后好不好**。几个术语用大白话说就是：

| 符号 | 名字 | 大白话 |
|------|------|--------|
| $s_t$ | 状态 State | 玩到第 t 步时「眼前看到的局面」 |
| $a_t$ | 动作 Action | 在这个局面下「走的一步棋」 |
| $\pi_\theta(a\|s)$ | 策略 Policy | 「局面 → 走棋概率」的映射表，$\theta$ 是它的参数（神经网络权重）|
| $r_t$ | 奖励 Reward | 这一步之后的即时打分（可能为 0，也可能负） |
| $\tau = (s_0,a_0,s_1,a_1,\dots)$ | 轨迹 Trajectory | 完整玩一局的所有局面+动作序列，也就是经验里说的 rollout/一条样本 |

以语言模型生成为例：已经生成的文本是 $s_t$（如 `"用户问：xxx；模型已回答：今天天气"`），下一个 token 是 $a_t$（如 `"很"`），整句生成完就是一条轨迹 $\tau$。

#### 核心思想：把「分数高的轨迹」推得更可能出现

我们要优化的目标 $J(\theta)$ 是「期望回报」（一局的平均得分）。训练方式是**梯度上升**：随机猜一局 → 拿到真实得分 → 把这局里的每个决策都往「更可能出现」的方向拉一点。这就是 **REINFORCE 算法**（策略梯度最原始的形式）：

$$\nabla_\theta J(\theta) = \mathbb{E}_{\tau\sim\pi_\theta}\left[\sum_t \nabla_\theta \log \pi_\theta(a_t\|s_t)\, R_t\right]$$

逐符号拆开读：

- $\mathbb{E}_{\tau\sim\pi_\theta}[\cdot]$：对所有可能的对局求平均——实际操作就是采很多局、平均它们的梯度
- $\sum_t$：把这一局里每个时间步的贡献加起来
- $\nabla_\theta \log \pi_\theta(a_t\|s_t)$：**方向项**——「怎么调参数才能让动作 $a_t$ 在这个局面下概率变大」。为什么带 log？因为对连乘的概率取 log 就变成连加，且这个形式的梯度恰好是纯的「调高概率」方向
- $R_t$：**强度旋钮**——从时刻 $t$ 起一直到局终的累计奖励（后面每一步奖励加总）。$R_t>0$ 就拉高这些动作的概率，$R_t<0$（后续受罚）就压低它们

合起来一句话：**一局游戏打得整个后半段都不错，里面每一步都加分；后半段整体很差，则每一步都减分**。

一个具体小例子：猜拳机器人只会出石头/剪刀（各 50%），对手笨得总输给石头。打完 100 局后发现包含「出石头」的局赢多，于是 $R_t>0$ 的局把石头概率从 0.5 推到 0.55……如此迭代下去石头概率逐渐逼近 100%。 $\nabla_\theta \log \pi_\theta(a_t\|s_t)$ 决定往哪边推（剪刀概率会相应被挤低），$R_t$ 决定推多重。

#### 问题：这样练方差太大

按「整局好坏」给所有步同乘一个大系数，问题是太粗暴了：

- 同一个 prompt 下两次生成，一次 RM 打 5 分一次打 −2 分，梯度方向完全不同、噪声巨大；
- 如果所有局面平均分本来就是正的（比如 SFT 初始模型写得都不错），那么**烂回答也会被顺势拉高概率**，因为它的系数是正数只是小一些；
- 需要大量轨迹平均才能抵消这种噪声 → 训练慢且不稳定。

改进是引入 critic $V_\phi(s)$ 学状态价值，用**优势函数**代替回报：

$$A_t = Q(s_t,a_t) - V(s_t) \approx R_t - V_\phi(s_t)$$

直觉：不是问「这条轨迹好不好」，而是问「这一步比在这个局面下的平均水平好多少」，均值已经被 baseline 减掉，梯度更干净。上面的烂回答问题也解决了：平均分哪怕全是正的，「比平均还差」的动作得到的是**负优势**，会被压低而非被拉高。

### 2.2 核心：重要性采样与代理目标

更新后的新策略 $\pi_\theta$ 与采集数据的旧策略 $\pi_{\text{old}}$ 不同。为了能用同一批数据多更新几步，用重要性采样改写期望：

$$J(\theta) = \mathbb{E}_{a\sim\pi_{\text{old}}}\left[\frac{\pi_\theta(a|s)}{\pi_{\text{old}}(a|s)} A(s,a)\right] = \mathbb{E}_{a\sim\pi_{\text{old}}}\big[r_t(\theta)\,A_t\big]$$

其中概率比为：

$$r_t(\theta) = \frac{\pi_\theta(a_t|s_t)}{\pi_{\text{old}}(a_t|s_t)}$$

$r_t(\theta)=1$ 表示两个策略相同；比值偏离 1 越远说明策略挪动越大。朴素地最大化这个目标的问题是：当 $r_t(\theta)$ 变得很大或很小时，重要性采样失效（下文性质 4），优化会崩。PPO 的做法是给这个比值加一个「安全轨道」。

### 2.3 PPO-Clip 目标（主流版本）

$$L^{\text{CLIP}}(\theta) = \mathbb{E}_t\Big[\min\big(r_t(\theta)A_t,\ \text{clip}(r_t(\theta), 1-\epsilon, 1+\epsilon)A_t\big)\Big]$$

逐符号解读：
- $\epsilon$：裁剪幅度，典型取 $0.2$，即允许每轮更新的概率比在 $[0.8,\ 1.2]$ 内变化；
- $\text{clip}(\cdot)$：把比值硬性限制在区间内，超出部分不再提供梯度；
- $\min$：取两项中较小的——即取对策略提升**更保守**的那个估计。

直觉拆开看两种情形：
- **$A_t > 0$（动作好）**：想让 $r_t(\theta)$ 变大以提升该动作概率。一旦 $r_t(\theta) > 1+\epsilon$，clip 项封顶在 $(1+\epsilon)A_t$，梯度消失 → 不鼓励一步提升超过 20%；
- **$A_t < 0$（动作差）**：想让 $r_t(\theta)$ 变小。当 $r_t(\theta) < 1-\epsilon$ 后同理封底 → 不鼓励一次砍掉超过 20% 的概率。

关键结论：

$$\boxed{\text{min} + \text{clip} = \text{只有远离 } 1 \text{ 的过度更新会被抑制，正常方向的梯度保留}}$$

### 2.4 PPO-Penalty 版本（KL 惩罚）

另一种实现是把约束做成软惩罚：

$$L^{\text{KPEN}}(\theta) = \mathbb{E}_t\Big[r_t(\theta)A_t - \beta\,\mathrm{KL}\big(\pi_{\text{old}}(\cdot|s_t)\,\|\,\pi_\theta(\cdot|s_t)\big)\Big]$$

$\beta$ 自适应调整：若一个 batch 的平均 KL 超过目标值 $d_{\text{targ}}$ 的若干倍则增大 $\beta$，反之减小。实践中 clip 版更常用（实现简单、无需调 $d_{\text{targ}}$），但在 RLHF 场景里 KL 惩罚以另一种形式回归（见 2.6）。

### 2.5 GAE：优势估计怎么做

PPO 通常配合 GAE（Generalized Advantage Estimation）。定义 TD 残差：

$$\delta_t = r_t + \gamma V_\phi(s_{t+1}) - V_\phi(s_t)$$

GAE 把多个 TD 残差做指数加权求和：

$$A^{\text{GAE}}_t = \sum_{l=0}^{\infty} (\gamma\lambda)^l\,\delta_{t+l} = \delta_t + \gamma\lambda\,\delta_{t+1} + (\gamma\lambda)^2\,\delta_{t+2} + \cdots$$

- $\gamma$：折扣因子（未来奖励的重要程度）；
- $\lambda$：偏差-方差旋钮。$\lambda=0$ 只有 $\delta_t$（纯 TD，低方差高偏差）；$\lambda=1$ 退化为蒙特卡洛（$R_t - V(s_t)$，无偏但高方差）。实践常取 $\lambda\approx 0.95$。

### 2.6 完整训练循环（以及在 RLHF 中的形态）

标准流程每个迭代：

```
1. 用 π_old 采样一批轨迹（rollout）
2. 用 critic 计算 value，得 δ_t 和 GAE 优势 A_t（batch 内做归一化）
3. 对这批数据做 K 个 epoch 的小批量梯度上升，最大化 L^CLIP - c1·value_loss + c2·entropy_bonus
4. 若 KL(π_old ‖ π_new) 超过阈值（如 0.015）提前停止本批 epoch
5. 更新 π_old ← π_new，进入下一轮
```

在 RLHF（如 InstructGPT）中的对应关系：policy = 语言模型本体（actor），$\pi_{\text{ref}}$ = 冻结的 SFT 模型；同时最多有 4 个大模型副本驻留显存，下面逐个拆解。

### 2.7 RLHF-PPO 的四个模型

| 模型 | 是否训练 | 初始化来源 | 输出 | 一句话职责 |
|------|---------|-----------|------|-----------|
| Actor（Policy） | ✅ 训练 | SFT 模型 | 每个 token 的分布 $\pi_\theta(a_t\|s_t)$ | 被对齐的目标：生成回答 |
| Critic（Value Model） | ✅ 训练 | RM 或 SFT（另接标量头） | 每个状态的价值 $V(s_t)$ | 学「当前回复有多好」的期望，给优势做 baseline |
| Reward Model（RM） | ❄️ 冻结 | Stage 2 训练好的偏好模型 | 整句回答的一个标量分数 $r_{\text{RM}}(x,y)$ | 提供终极奖励信号 |
| Reference（$\pi_{\text{ref}}$） | ❄️ 冻结 | SFT 模型副本 | 每个 token 的概率 $\pi_{\text{ref}}(a_t\|s_t)$ | KL 锚点：防止 policy 漂离自然语言流形 |

**① Actor（策略网络）**

就是被训练的语言模型本身。rollout 时输入 prompt 逐 token 自回归生成；训练时拿优势信号更新它的参数。它既是「环境中的行为者」也是「被优化的对象」——这正是 LLM-RL 与传统 RL 的关键差异：动作空间是几万维词表、每步都是模型的输出层采样。

**② Critic（价值网络）**

结构与 actor 几乎相同（同架构 Transformer 底座），只是把输出层从词表头换成**标量回归头**，估计状态价值 $V(s_t)$。作用是把整句奖励拆到每个 token 上提供 baseline：没有它，优势估计退化为高方差的蒙特卡洛回报（baseline 为零）。注意两个工程细节：(1) InstructGPT 用 RM 来初始化 value head——因为两者都需要理解「什么是好回答」，起点更准；(2) 显存紧张时可让 critic 与 actor **共享底座权重**只各自挂一个 head（TRL/DeepSpeed-Chat 均支持该选项）。

**③ Reward Model（奖励模型）**

Stage 2 用 Bradley-Terry 配对损失训好后就冻结不动（若继续更新会与 policy 形成 moving target，训练崩坏）。推理时读完整个回答输出一个标量分数，**只在句末一次性加入奖励**。它是唯一的「环境裁判」，所以它的质量直接决定对齐上限；也正因它不可微调，才有后面 Q8 的 KL 保护、定期刷新等治理手段。

**④ Reference（参考模型）**

SFT 模型的一份冻结拷贝，唯一工作是逐 token 计算 $\pi_{\text{ref}}(a_t\|s_t)$，拼进每步奖励：$\tilde{r}_t = r_{\text{RM}} - \beta\log\frac{\pi_\theta(a_t\|s_t)}{\pi_{\text{ref}}(a_t\|s_t)}$。注意区分两个很容易混淆的比值：这是 **policy vs ref 的 KL 锚点**（RLHF 特有的安全绳），而 PPO 算法内部的 $r_t(\theta)=$ policy vs $\pi_{\text{old}}$ 是重要性采样比值（每个算法都有）。前者跨整个训练过程不变锚点，后者只在当前 batch 内有效。

一次完整迭代的信号流动：

```
Actor 生成 y → RM 给整句打分 r_RM ─┐
               └→ Reference 算逐 token KL → 拼 token 级奖励 r̃_t
Critic 估 V(s_t) → GAE 得优势 A_t → 更新 Actor 和 Critic
```

这四个模型（其中 actor/critic 可训练、ref/RM 冻结）同时驻留显存，是 PPO 工程成本高的根源，也是 GRPO「去 critic 化」省显存的出发点。

## 3.重要性质

**性质 1：clip 是非对称的保护**
$A_t>0$ 时把上顶封住，$A_t<0$ 时把下底托住；但注意负优势情形中 `min` 选中未被 clip 的一项，梯度仍然指向把概率压回去的方向——所以即便不做 clip，PPO 也天然存在把ratio 拉回区间的力量。这种设计保证「变好不超过 $\epsilon$」严格成立。

**性质 2：r_t(θ) 只在被采样的动作上有意义**
$\pi_{\text{old}}(a|s)$ 大的动作其比值分母也大，两个分布差异过大时个别 token 的比值可能达数百倍——这正是为什么策略和数据不能离得太远：重要性采样只在 $\pi_\theta \approx \pi_{\text{old}}$ 的局部邻域内近似成立。PPO 用 clip + 少量 epoch（通常 3~4 个）让这个假设近似成立。

**性质 3：少 epoch 数据复用的 on-policy 算法**
PPO 严格说是「near on-policy」：每个 rollout 批次内重复利用 $K$ 次（TRPO 论文实测复用过多轮会崩，PPO 靠 clip 缓解）。相比每条数据只用一次的 vanilla PG，样本效率提高数倍；相比真正的 off-policy 方法（Q-learning/SAC 用 replay buffer），差距在于没有 replay、没有 target network，也更稳定。

**性质 4：naive 重要性采样的失效模式**
不带 clip 地优化 $r_t(\theta)A_t$：当某个低概率动作偶然获得巨大正优势时，优化器会把它推成天文数字的概率（因为 $(r_t A_t)$ 无上限），一次就把分布打崩。clip 的本质就是给这个病态项设定了有效边界。

**性质 5：熵 bonus 抑制过早收敛**
损失中常加入 $c_2\,\mathrm{H}[\pi_\theta(\cdot|s)]$（如 $c_2=0.01$）鼓励探索。若不这样做，策略早期收敛后熵迅速塌缩到确定性行为，再也探索不到更好的动作——在 RLHF 里表现为回复越来越模板化的「对齐税」。

## 4.与其他概念的关系

- **vs REINFORCE / A2C**：都属 on-policy 策略梯度家族。REINFORCE 无 baseline 方差爆炸；A2C 有 critic 但单步更新数据利用率低；PPO = A2C + 重要性采样比值 + clip + GAE + 多 epoch 复用。
- **vs TRPO**：同一个 surrogate 目标，TRPO 用 KL 散度的二阶泰勒展开 + 共轭梯度施加**硬约束** $\overline{\mathrm{KL}}(\theta_{\text{old}},\theta)\le\delta$；PPO 用 clip 做**软约束**，只需一阶方法。性能相近，工程复杂度天壤之别。
- **vs off-policy 方法（DDPG/SAC/Q-learning）**：off-policy 有 replay buffer、旧数据反复使用，样本效率更高但需处理分布偏移（target network 等），主要适用于连续控制/离散小动作空间。LLM 词表大、序列长、reward 稀疏，on-policy 类方法更容易落地。
- **vs GRPO**：GRPO 可以理解为「PPO 去 critic 化」：删掉 value 网络，用同 prompt 一组回答的组内标准化奖励替代 GAE 优势，其余 clip 更新等机制保留。显存大幅降低，DeepSeekMath/R1 采用。
- **vs REINFORCE++**：面向 RLHF 的简化派——不用 GAE/token-level KL 打分方式改动，仅做 batch 级 baseline 与归一化，形式更接近升级版 REINFORCE，deepseek 系和许多开源框架用作默认选项。
- **vs DPO**：DPO 干脆不走在线 RL 循环，用离线偏好对的闭式损失隐式完成 KL 正则化奖励最大化；PPO 则显式采样、显式打分，探索能力更强但基建昂贵。

## 5.具体例子

### 例子 1：手算 clip 目标——好的动作该不该继续推？

**问题**：某状态下旧策略选中动作 a 的概率为 $\pi_{\text{old}}(a|s)=0.5$，一次更新后 $\pi_\theta(a|s)=0.65$，估计出的优势 $A_t=+1$，$\epsilon=0.2$。此时的 clip 目标值是多少？梯度还会推它涨吗？

**步骤与计算**：
- 概率比 $r_t(\theta) = 0.65 / 0.5 = 1.3$
- 未裁剪项：$r_t(\theta)A_t = 1.3\times 1 = 1.3$
- 裁剪项：$\text{clip}(1.3,\ 0.8,\ 1.2)\times 1 = 1.2$
- 目标：$\min(1.3,\ 1.2) = 1.2$

**结果解读**：由于 min 取到了被封顶的 1.2，这个 token 的目标是常数——**梯度为零**，本次更新不会再放大它的概率，直到重新采样使 $r_t$ 回到 1.2 以内。对照另一种情况：若下一个 batch 里 $r_t=0.9$（更新过头回落了），目标为 $\min(0.9,\ \text{clip}(0.9)\times1)=0.9$，梯度恢复为正，继续拉高。这就是「自动松紧」的安全机制。

再看反例帮助理解 $\min$ 的必要性：如果换成 naive 地把两项相加或取 max，取 max 会选到 1.3，梯度仍流向放大方向且没有任何限制，那一步就会踩空。

### 例子 2：GAE 手算一遍（含 λ=0 / λ=1 两个极端）

**问题**：某回合只有两步：$s_0 \xrightarrow{a_0} s_1 \xrightarrow{a_1}$ 终止。奖励 $r_0=1$，$r_1=3$。critic 给出 $V(s_0)=1.0$，$V(s_1)=2.0$，终止状态价值为 0。取 $\gamma=0.9$，$\lambda=0.5$，求各步优势。

**步骤与计算**：
- TD 残差：
  $$\delta_0 = r_0 + \gamma V(s_1) - V(s_0) = 1 + 0.9\times 2.0 - 1.0 = 1.8$$
  $$\delta_1 = r_1 + \gamma\cdot 0 - V(s_1) = 3 + 0 - 2.0 = 1.0$$
- GAE 优势（只往前看一步即可终止）：
  $$A^{\text{GAE}}_0 = \delta_0 + \gamma\lambda\,\delta_1 = 1.8 + 0.45\times 1.0 = 2.25$$
  $$A^{\text{GAE}}_1 = \delta_1 = 1.0$$

**结果解读**：对比两端——$\lambda=0$ 时 $A_0=\delta_0=1.8$（完全信 critic，一步 bootstrap）；$\lambda=1$ 时 $A_0=R_0-V(s_0)=(1+0.9\times 3)-1.0=2.7$（完全信真实回报）。$\lambda=0.5$ 的 2.25 介于两者之间：既利用了实际奖励，又被 critic 的中间估值平滑了噪声。蒙特卡洛的 2.7 方差大（假如 $r_1$ 换成随机数就有波动），TD(0) 的 1.8 又依赖 $V(s_1)=2.0$ 是否估准——GAE 让你在两者间插值。

### 例子 3：PPO-Penalty 的自适应 KL 系数走一轮

**问题**：采用惩罚版 PPO，目标 KL 设定 $d_{\text{targ}}=0.02$，规则为：本批实测 KL 超过 1.5 倍目标则 $\beta \times 1.5$，低于目标的 $1/1.5$ 则 $\beta \div 1.5$。初始 $\beta=0.1$，四批数据的实测 KL 分别为 0.05、0.04、0.008、0.015，求 $\beta$ 的演化。

**步骤与计算**：
- 第 1 批：KL=0.05 > 0.02×1.5=0.03 → $\beta = 0.1\times1.5 = 0.15$
- 第 2 批：KL=0.04 > 0.03 → $\beta = 0.15\times1.5 = 0.225$
- 第 3 批：KL=0.008 < 0.02/1.5≈0.013 → $\beta = 0.225/1.5 = 0.15$
- 第 4 批：KL=0.015 ∈ (0.013, 0.03) → $\beta$ 保持 0.15

**结果解读**：$\beta$ 像恒温器一样围绕目标震荡收敛，实测 KL 最终贴着 $d_{\text{targ}}$ 波动。这套自适应逻辑同样出现在现代 RLHF 实现（以及 deepspeed-chat、TRL 库）的 early-stop 中：一个 batch 平均 KL 超 0.015~0.02 直接丢弃本批剩余更新。数值上也解释了为什么 RLHF 文献常引用 KL∈[0.01, 0.05] 作为健康区间。

## 6.常见面试问题

### Q1：写出 PPO-Clip 的目标函数，并解释每一部分的作用。
**答**：$L^{\text{CLIP}}(\theta)=\mathbb{E}_t[\min(r_tA_t,\ \text{clip}(r_t,1-\epsilon,1+\epsilon)A_t)]$。$r_t=\pi_\theta/\pi_{\text{old}}$ 是新旧策略概率比，实现 off-policy 式的数据复用；clip 把 $r_t$ 限制在 $[1-\epsilon,1+\epsilon]$（典型 $[0.8,1.2]$）防止更新过大；min 保证取的是更保守的估计。直觉：好的动作最多把概率提 20%，差的动作最多压 20%，超出后梯度消失等待重新采样。

### Q2：为什么要同时有 min 和 clip？只用 clip 行不行？
**答**：不行。若只用裁剪项 $\text{clip}(r_t,1-\epsilon,1+\epsilon)A_t$，会有两个方向被误伤：当 $A_t>0$ 而 $r_t<1-\epsilon$（好动作的概率被压得过低）时，目标值是常数 $(1-\epsilon)A_t$、梯度为零——但此时我们正需要梯度把概率拉回来；同理 $A_t<0$ 且 $r_t>1+\epsilon$ 时的回修梯度也被抹掉。min 的作用是在这些「仍需纠偏」的情形选中未裁剪的 $r_tA_t$，恢复梯度；而只有在真正的过度更新方向上（好动作概率已超上限、坏动作概率已低于下限）才取封顶/封底的常数。两者组合实现了「越界的坏方向没有梯度，仍在纠偏的好方向保留梯度」。

### Q3：$A_t<0$ 时 clip 还能保护住下界吗？clip 有没有已知的失效缺口？
**答**：分情形讨论梯度流向（设 $\epsilon=0.2$，$A_t=-1$）：(1) $r_t=1.3$ 时目标 $=\min(-1.3,\ -1.2)=-1.3$，min 选中**未被 clip 的**一项，梯度照常把 $r_t$ 往回拉；(2) $r_t=0.7$ 时 clip(0.7)=0.8，目标 $=\min(-0.7,\ -0.8)=-0.8$，取到被封底的常数项，梯度为零——动作概率已被压到区间下限之下时不允许继续压低，这是保护而非漏洞。真正的经典缺口在另一侧：当 $A_t>0$ 且 $r_t>1+\epsilon$ 时梯度归零，且**对策略把多余概率质量挪到哪里毫无约束**——策略可能在个别动作上过度集中导致确定性坍缩，需熵 bonus 缓解。面试中能说清「梯度何时消失、何时保留」即是满分答案。

### Q4：PPO 说自己是 on-policy，凭什么还能复用数据跑多个 epoch？
**答**：靠重要性采样修正 + clip 控制偏差半径。数据由 $\pi_{\text{old}}$ 采出，多次小步更新后 $\pi_\theta$ 与 $\pi_{\text{old}}$ 逐渐偏离，重要性比值 $r_t$ 精确补偿了这个偏离的一阶影响；而重要性采样的方差随分布距离指数增长，所以必须用 clip/KL 早停把偏离限制在小邻域内，且 epoch 数量要少（通常 3~4）。这是「近似 on-policy」：与真正 off-policy（replay buffer 跨越很远的历史）有本质区别。

### Q5：比较 TRPO 与 PPO。
**答**：共同起点都是受限策略优化的 surrogate：$\max_\theta \mathbb{E}[r_t(\theta)A_t]\ \text{s.t.}\ \overline{\mathrm{KL}}\le\delta$。TRPO 用 KL 的一阶/二阶近似 + 共轭梯度法 + Fisher 矩阵求解每步带约束的更新，理论完备但实现复杂、难并行、难扩展。PPO 用 clip 把约束「烘焙」进目标函数，纯一阶优化器（Adam）即可，方便分布式大规模训练。实证性能相近，因此工业界几乎只保留 PPO 及其后裔。

### Q6：推导 GAE，并说明 λ 如何权衡偏差与方差。
**答**：定义 TD 残差 $\delta_t=r_t+\gamma V(s_{t+1})-V(s_t)$，它是优势的无偏估计当且仅当 $V$ 正确（$(k+1)$ 步回报减 $V$ 可写成若干残差之和）。$k$ 步优势 $A^{(k)}_t=\sum_{l=0}^{k-1}\gamma^l\delta_{t+l}$；GAE 是所有 $k$ 的指数加权混合 $\sum_k \lambda^{k-1}A^{(k)}_t=(1-\lambda)\sum(\gamma\lambda)^l\delta_{t+l}$，可写成 $\delta_t+\gamma\lambda\delta_{t+1}+\cdots$。$\lambda\to0$：只信 $\delta_t$，方差小、偏差来自 $V$ 的误差；$\lambda\to1$：等价于 Monte Carlo，无偏但方差大。经验取 0.9~0.98。

### Q7：画出 RLHF 中 PPO 的完整架构，涉及几个模型？显存瓶颈在哪？
**答**：四个模型——policy（待训练 LM+actor head）、reference（冻结 SFT，算 per-token KL 惩罚进 reward）、RM（冻结，句末打分）、critic（value head，可与 policy 共享底座也可独立）。瓶颈：(1) 四个 LM 副本的权重显存；(2) rollout 阶段的 KV cache 随序列长度暴涨；(3) critic 若独立则是第二个可训练大模型。缓解手段：policy/critic 权重共享（InstructGPT 做法）、ZeRO 切分、LoRA 化 policy、或直接换 GRPO（省去 critic 与 RM 可选）。

### Q8：reward 里混入 KL 惩罚的目的和公式是什么？β 怎么选？
**答**：$\tilde{r}_t = r_{\text{RM}}\cdot\mathbb{I}[t=T] - \beta\log\frac{\pi_\theta(a_t|s_t)}{\pi_{\text{ref}}(a_t|s_t)}$（KL 逐 token 计入，RM 分只在最后加上）。目的：防 reward hacking、保持流畅性和 SFT 能力。$\beta$ 过大学不到东西、过小训崩，InstructGPT 取 0.001~0.02 量级并配合早停监控；也有实现把 KL 作为 PPO 惩罚项的自适应系数（参考例子 3 的恒温器式调节）。

### Q9：PPO 训练后期策略熵快速下降、输出越来越固定，怎么诊断和缓解？
**答**：诊断：监控 batch 熵曲线与 unique response 占比。原因：优势信号集中在少数模式上，优化不断削尖分布，且 $A_t>0$ 被 clip 后剩下的一直是被反复强化的高概率动作。缓解：(1) 熵正则 $+c_2 H[\pi]$；(2) 减少 epoch 数、加大 KL/clip 强度；(3) 批内做优势白化避免少数异常样本主导；(4) 混入 SFT/无 RL 数据的联合损失；(5) 检查是否优势归一化出错或 batch 过小引入噪声。

### Q10（进阶）：对比 GRPO、REINFORCE++ 对 PPO 的改造，说明了什么趋势？
**答**：GRPO：删除 critic，同 prompt 采 G 个回答用 $(r_i-\bar r)/\text{std}$ 做优势，显存近乎减半，特别适合数学/代码这类结果可控判分的任务；代价是组内样本耦合、样本利用率略降。REINFORCE++：保留 PPO 的比值与 clip 但去掉 GAE/token-level value，用全局 batch baseline，实现更简。趋势：在大模型场景下，「traceable verifiable reward + 强大的初始化（SFT/预训练）」使得复杂 variance reduction（学习一个 critic）性价比下降，算法在做减法——保住「限制单步更新幅度」这一核心安全机制即可。

## 参考资料
- Schulman et al., *Trust Region Policy Optimization*, 2015
- Schulman et al., *Proximal Policy Optimization Algorithms*, 2017 (arXiv:1707.06347)
- Schulman et al., *High-Dimensional Continuous Control Using Generalized Advantage Estimation*, 2016
- Ouyang et al., *Training language models to follow instructions with human feedback* (InstructGPT), 2022
- Shao et al., *DeepSeekMath: Pushing the Limits of Mathematical Reasoning* (GRPO), 2024
- Hu, *REINFORCE++: A Simple and Efficient Approach for Aligning Large Language Models*, 2025
