# 大模型 SFT（监督微调）

## 目录
- [1.概述](#1概述)
- [2.核心原理](#2核心原理)
- [3.重要性质](#3重要性质)
- [4.与其他概念的关系](#4与其他概念的关系)
- [5.具体例子](#5具体例子)
- [6.常见面试问题](#6常见面试问题)
- [参考资料](#参考资料)

## 1.概述

**一句话定义**：SFT（Supervised Fine-Tuning）是在预训练模型基础上，用「指令 → 高质量回答」的成对数据继续做监督学习（本质上还是预测下一个 token），教会模型以对话/指令的方式响应。

**为什么重要**：
- 它是「会续写的模型」到「能用的助手」之间最便宜、最稳定的一步；
- 数据质量 > 数量（LIMA 论文：仅 1000 条精选样本即可获得很强的对齐效果），因此 SFT 的核心工作是造数据、洗数据、定格式。

## 2.核心原理

### 2.1 训练目标与 Loss Mask

SFT 数据一条样本通常形如：

```
<system> 你是一个有用的助手 </system>
<user>   解释什么是过拟合        </user>
<assist> 过拟合是指模型在训练集上……（完整回答） </assist>
```

损失仍是交叉熵语言建模损失，但只在 assistant 回答部分计算：

$$\mathcal{L}_{\text{SFT}} = -\sum_{t \in \text{response}} \log p_\theta(x_t \mid x_{<t})$$

其中 $t\in\text{response}$ 表示只对答案区间内的 token 求和。这就是 **loss mask**：prompt/system 部分 `mask=True` 不计损失。

为什么这样做？我们希望模型学会「生成好的回答」，而不是学着模仿用户的提问。如果 prompt 也算 loss，模型会把大量容量花在学习「用户会怎么说话」上。

### 2.2 Chat Template 与特殊 token

对话必须用模板拼接，常见设计（简化自 ChatML / Llama 格式）：

```text
<|im_start|>system
你是一个有用的助手<|im_end|>
<|im_start|>user
你好<|im_end|>
<|im_start|>assistant
你好！有什么可以帮你？<|im_end|><eos>
```

要点：
- 角色 token、`<eos>` 是**新加入词表的特殊 token**，它们的 embedding 从随机初始化开始学（或手工初始化）；
- 推理时模型生成 `<eos>` 或 `<|im_end|>` 即认为回答结束——忘记在 SFT 中正确处理结束符是「模型停不下来一直复读」的最常见原因之一；
- 多轮对话训练时通常每轮的 response 都计 loss（也有只算最后一轮的做法），且同一样本内多轮共享上下文。

### 2.3 全参数微调 vs 参数高效微调（PEFT）

全参微调更新所有 $\theta$，成本高。PEFT 只训练极少量新增参数。

**LoRA（Low-Rank Adaptation）**：冻结原权重 $W_0\in\mathbb{R}^{d\times k}$，为每个目标层增加低秩旁路：

$$W = W_0 + \Delta W = W_0 + B A,\quad B\in\mathbb{R}^{d\times r},\ A\in\mathbb{R}^{r\times k},\ r \ll \min(d,k)$$

推理时无额外延迟（$\Delta W$ 可合并进 $W_0$）。常配 scaling 系数 $\alpha/r$ 控制增量幅度。典型超参：$r=8\sim 64$，$\alpha=r$ 或 $2r$，目标模块选 Q/K/V/O 及 MLP 层。

**QLoRA**：把基座量化到 NF4，再在其上训 LoRA，使单卡（如 24GB）也能微调 33B+ 模型；反量化精度靠双重量化 + 分页优化器维持。

其他 PEFT 还有 Prefix-tuning、P-Tuning v2（可学习连续 prompt 向量）、Adapter 等，当前实践主流是 LoRA 一族。

### 2.4 SFT 数据从哪里来

1. **人工标注**：贵但质量上限最高（InstructGPT 起步用的人工示范约 13k 条）；
2. **开源数据集**：Alpaca（self-instruct 由 GPT 生成的 52k）、ShareGPT、OpenAssistant、FLAN 系列任务化数据；
3. **自我蒸馏 / 合成**：用强模型生成 (instruction, response)，配合过滤器（打分模型、去重、难度分层）；evol-instruct 让简单问题进化出多样难度变体；
4. **数据配方经验**：覆盖多领域多任务 + 质量/多样性优先 + 长短答案混合；蒸馏与 CoT 数据要混合不同风格防正则塌缩。

### 2.5 训练细节要点

- 学习率远小于预训练（全参常见 $1\times10^{-5}\sim 2\times10^{-5}$，LoRA 可大一个数量级如 $1\times 10^{-4}$~$4\times 10^{-4}$）；
- 一般 1~3 个 epoch 就够，多了容易过拟合成「背模板」、泛化下降；
- packing：把多条短样本拼接到 max_length 提高利用率，注意跨样本 attention mask 要互相隔离；
- 常见坑：`tokenizer` 添加 special token 后忘了 resize embedding；变长序列需要正确的 padding 与 label -100 忽略值。

## 3.重要性质

**性质 1：SFT 改变的是条件分布而非知识容量**
公式视角：SFT 极小化 $-\log p_\theta(\text{answer}|\text{question})$，它是把预训练分布中「像好助手会说的部分」放大。所以单靠 SFT 很难注入全新事实性知识——那属于预训练阶段的工作；SFT 主要改变行为模式（格式、语气、解题步骤习惯）。

**性质 2：灾难性遗忘的温和版本**
小学习率 + 少 epoch 下，模型通用能力基本保留但仍可能下降（尤其代码/数学）。缓解：混入一定比例预训练 replay 数据、LoRA 限制更新子空间、控制 SFT epoch。

**性质 3：低秩假设的经验有效性**
微调产生的权重增量 $\Delta W$ 的内在秩（有效维度）很低，故 $r$ 取 8~64 已能逼近全参效果——这是「微调 ≠ 重学知识」的另一佐证。

**性质 4：Loss mask 对多轮对话的成本影响**
设 prompt 占总长度比例为 $\rho$（实践中常高达 60%~90%），真实学习的 token 只有 $(1-\rho)$ 部分。若不 mask 会把大多数梯度浪费在学提问上——这是很多自研 SFT 效果差的隐性 bug 来源。

## 4.与其他概念的关系

- vs **预训练**：预训练自监督学知识与语言，SFT 监督学行为；预训练数据 TB 级、SFT 万级即可见效。
- vs **RLHF/DPO**：SFT 模仿「标准答案」（点估计），RLHF 优化「偏好序」（更劣不该优于更优）；顺序通常是 SFT → RM/PPO 或 DPO。
- vs **继续预训练（continued pre-training / CPT）**：当目标是灌入新领域知识（医疗、法律语料）用 CPT，学习率和数据形式都贴近预训练；CPT 之后仍需 SFT 才能变成听话的模型。
- 与知识蒸馏的关系：用强模型生成 SFT 数据训练小模型，就是一种白盒/黑盒混合的蒸馏方式。

## 5.具体例子

### 例子 1：手算 LoRA 的参数节省

**问题**：对一个 Llama-7B 风格模型的某个注意力层，$d_{model}=4096$，四个投影矩阵均为 $4096\times 4096$。若对所有四个矩阵加 $r=8$ 的 LoRA，新增参数是多少？占原始多少比例？

**步骤与计算**：
- 原始四矩阵参数：$4\times 4096^2 = 67{,}108{,}864$
- LoRA 新增：每个矩阵贡献 $A$ 和 $B$ 两块，即 $r(d+k)=8\times(4096+4096)=65{,}536$；
  四个矩阵共 $4\times 65{,}536=262{,}144$
- 占比：$\frac{262{,}144}{67{,}108{,}864}\approx 0.39\%$

**结果解读**：只需训练约千分之四的可训参数就能完成对该层的能力调整。推广到整个 7B 模型，LoRA 只训约 20M~40M 参数，一张消费级显卡即可承担梯度与优化器状态（原来全参需要 ~7B×16 bytes ≈ 112GB 仅优化器侧开销）。

### 例子 2：构造一条带 loss mask 的训练样本

**问题**：把「请把这句话翻译成英文：今天天气很好。」/「The weather is nice today.」写成训练时 labels。

**步骤**：
1. 拼接：`<user> 请把这句话翻译成英文：今天天气很好。<eos_for_user_turn><assistant> The weather is nice today.<eos>`
2. 对 prompt 部分（含 role 标记）：label 设为 `-100`（PyTorch CrossEntropyLoss 忽略值）；
3. 对 answer 部分（含结尾 eos）：label = 正常 token id。

逐 token 表示：

```
token ids :  [user][请][把]…[。][turn_end][assistant][The][weather]…[。][eos]
labels    :  [-100][-100]…[-100] [-100]     [-100]   [The][weather]…[。][eos]
```

**结果解读**：反向传播只会更新「如何生成翻译答案」方向的梯度。丢失这一步会导致 loss 表面下降很快（因为背 prompt 容易）但模型实际没学会跟随指令，甚至开始在输出里复述用户的问题。

### 例子 3：数据质量对比实验（直觉数字）

**问题**：同样是 50k 条数据，「1000 条专家手写 + 复制扩充」与「50k 条网络爬取问答」哪个更值得用？

**参考证据**（LIMA 及后续工作的一般结论）：LIMA 用 1,000 条精选样本得到的效果可与数万条普通数据的模型相当；而若混入 5%（约几百条）低质噪声样本，模型输出质量会出现可感知下滑。

**结果解读**：SFT 数据遵循「短板效应」——少量烂数据带来的格式污染、幻觉习惯会被均匀扩散到所有回答上。清洗排序的优先级高于扩量。

## 6.常见面试问题

### Q1：SFT 的损失函数和预训练有何异同？
**答**：形式相同，都是 next-token 交叉熵 $\mathcal{L}=-\sum_t\log p(x_t|x_{<t})$；区别在于求和范围（仅 response 区间）与数据形态（成对指令数据 vs 连续文档）。相同点意味着实现上可直接复用 LM 训练框架。

### Q2：为什么要做 loss mask？不做会怎样？
**答**：让梯度只来自回答部分。不做的话模型浪费容量学习 prompt 分布，会出现复述输入、答非所问、在回答里夹带角色标记等退化行为；同时实际有效数据效率下降数倍。

### Q3：LoRA 的原理是什么？为什么低秩有效？$\alpha$ 起什么作用？
**答**：冻结 $W_0$，旁路训练低秩分解 $BA$。有效因为微调权重增量的内在秩很小（任务信息可压缩到少数方向）。初始化 $A\sim\mathcal N$, $B=0$ 保证起始时 $\Delta W=0$ 不破坏原模型。$\alpha$ 是缩放因子，实际增量为 $\frac{\alpha}{r}BA$：同样 $r$ 变大时用它保持幅度可比，常用 $\alpha=r$ 或 $2r$。

### Q4：QLoRA 在 LoRA 上做了什么改进？
**答**：(1) 4-bit NormalFloat（NF4）量化基座——针对正态分布权重设计的量化格点；(2) 双重量化（对量化常数再做量化）省显存；(3) 分页优化器防止显存峰值 OOM。代价是训练速度慢于 bf16 LoRA，推理前一般还要合并或转回高精度。

### Q5：多轮对话如何组织训练数据？有没有多种做法？
**答**：常见做法一：整个会话拼成一条，每一轮 assistant 回复都算 loss（样本利用率高，=context 中所有轮共同贡献）。做法二：只对最后一轮算 loss（强化学最终回答的行为）。另有把每轮拆成独立样本的简单做法（浪费但干净）。各家选择不一，关键是对齐推理时的使用方式。

### Q6：SFT 出现「复读机」「说个不停」的可能原因？
**答**：典型原因是 `<eos>/<|im_end|>` 未被正确加入词表或未在标签中出现，模型从未学会终止；也可能因 packing 时 attention mask 泄漏导致跨样本干扰、或重复惩罚相关解码参数不合理。先检查数据里的终止符出现频率与 logits 再查配置。

### Q7：什么时候选择全参微调而不是 LoRA？
**答**：当需要大幅改动能力分布（如领域迁移严重、增加新语言）、训练数据量大且算力充足时全参更好；小数据轻对齐 LoRA 足够。经验上 LoRA 与全参差距随数据规模增大而拉开。

### Q8：LIMA「Less is More」说明了什么？有什么适用边界？
**答**：说明对齐主要激发已有能力而非灌输新能力，1000 条高质量样本足以塑造行为。边界：它依赖一个已经很强的底座与严格的样本筛选过程；对垂域知识密集任务或弱底座仍需更大规模数据。

### Q9：SFT 之后紧接着就上线会有什么问题？
**答**：SFT 学的是「有样学样」，缺少偏好层面的取舍：可能冗长谄媚、拒绝过度、无法在「有用」与「安全」冲突处权衡；且暴露分布外的对抗输入表现差。因此生产链路一般是 SFT → RLHF/DPO，再用红队评测兜底。

### Q10（进阶）：什么是 NEFTune？它为什么对小数据 SFT 有效？
**答**：NEFTune 在训练时向 embedding 加入均匀噪声 $\epsilon\sim U(-\alpha/L,\alpha/L)$（$L$ 为序列长度），相当于对嵌入施加扩散式正则，抑制对个别样本表面特征的过拟合；在小规模 SFT 上带来一致提升。考察点是把「数据少→过拟合→噪声正则」这条逻辑链说清楚。

## 参考资料
- Ouyang et al., *Training language models to follow instructions with human feedback* (InstructGPT), 2022
- Hu et al., *LoRA: Low-Rank Adaptation of Large Language Models*, 2021
- Dettmers et al., *QLoRA: Efficient Finetuning of Quantized LLMs*, 2023
- Zhou et al., *LIMA: Less Is More for Alignment*, 2023
- Jain et al., *NEFTune: Noisy Embeddings Improve Instruction Finetuning*, 2023
