# 大模型量化（LLM Quantization）

## 目录
- [1.概述](#1概述)
- [2.核心原理](#2核心原理)
- [3.重要性质](#3重要性质)
- [4.与其他概念的关系](#4与其他概念的关系)
- [5.具体例子](#5具体例子)
- [6.常见面试问题](#6常见面试问题)
- [参考资料](#参考资料)

## 1.概述

**一句话定义**：量化是把模型中高精度数值（FP32/FP16/BF16）用更低位宽（如 INT8、INT4、FP8）来表示和存储的技术——本质是「用更少的比特存同样的信息，误差控制在模型可容忍的范围内」。

**为什么重要**：
- **省显存**：模型权重是显存大头。FP16 的 7B 模型要 14 GB，INT4 只要约 3.5 GB——70B 模型从「8 卡 A100」变成「单卡 48G」就能装下，消费级硬件跑大模型几乎全靠量化；
- **省带宽、提速度**：大模型推理的 Decode 阶段是**访存瓶颈**（每生成一个 token 都要把全部权重从显存读一遍，见[大模型推理](4.大模型推理.md)），权重位宽减半，读取量就减半，速度近似翻倍；
- **省成本**：同样的 GPU 能塞更大的 batch、更多的并发请求，服务成本近似按位宽比例下降。

一句话总结动机：**大模型推理的瓶颈是显存容量和显存带宽，而不是算力——量化恰好同时压缩这两者。**

## 2.核心原理

### 2.1 基本映射：实数 → 整数

量化的核心是建立一套「实数 ↔ 整数」的双向映射。以对称量化为例，把一组权重 $w$ 映射到 $b$ 比特整数：

$$s = \frac{\max|w|}{2^{b-1}-1}, \qquad w_q = \mathrm{clip}\left(\mathrm{round}\left(\frac{w}{s}\right),\ -2^{b-1},\ 2^{b-1}-1\right)$$

$$\hat{w} = s \times w_q \quad (\text{反量化，推理时真正参与计算})$$

- $s$（scale，缩放因子）：整数格点之间的间距，是量化唯一需要存的额外信息；
- $w_q$：量化后的整数（INT4 范围是 $[-8, 7]$，INT8 是 $[-128, 127]$）；
- 误差来源：`round` 的舍入误差，单个元素误差 $|e| \le s/2$——**数值范围越大，scale 越大，误差越大**。这一句话是理解后面所有算法的钥匙。

非对称量化多存一个零点（zero point）$z$，适合分布不关于 0 对称（如 ReLU 后的激活）：

$$w_q = \mathrm{clip}\left(\mathrm{round}\left(\frac{w}{s} + z\right),\ 0,\ 2^b-1\right), \qquad \hat{w} = s \times (w_q - z)$$

直觉：对称量化像一把「以 0 为中心」的尺子；非对称像一把「可以平移」的尺子，挪到数据实际分布的位置上，格点利用率更高。

### 2.2 量化粒度：scale 算多细

上面说了误差正比于数值范围，那么让每组数据**共享一个更小的范围**就能降误差，这就是粒度（granularity）：

| 粒度 | scale 的范围 | 特点 |
|---|---|---|
| per-tensor | 整个权重矩阵一个 | 最省，但 outlier 会拉大全局 scale |
| per-channel | 每个输出通道一个 | 常用于权重 |
| per-group | 每连续 g 个权重一个（g 常取 128） | LLM INT4 量化的事实标准 |

LLM 的权重里存在少量**离群值（outlier）**，若 per-tensor 一个 scale，少数大值会把 scale 撑大，其余 99% 的正常值被迫用粗格点——所以 4-bit 时代基本都退到 per-group 甚至 per-channel。

### 2.3 两条路线：PTQ vs QAT

- **PTQ（Post-Training Quantization，训练后量化）**：拿训练好的模型直接量化，只用少量数据（几百条）做校准。成本极低（小时级、单卡），是 LLM 的主流选择；
- **QAT（Quantization-Aware Training，量化感知训练）**：训练时插入伪量化节点（前向模拟量化、反向直通估计器 STE 传梯度），让模型「提前学会适应粗格点」。精度上限更高，但要重新训练，成本高——一般只在 2-bit 等极端位宽（如 BitNet 1.58-bit）才值得。

选型直觉：**4-bit 以上用 PTQ 就够；4-bit 以下或追求极限压缩时 QAT 开始占优。**

### 2.4 只量化权重，还是连激活一起？—— W4A16 与 W8A8

记号 WxA：W = 权重位宽，A = 激活位宽。

- **Weight-only（如 W4A16）**：权重存成 INT4，计算前反量化回 FP16 再做矩阵乘。为什么 decode 阶段够用？因为 decode 每步只算 1 个 token，瓶颈是「把权重从显存搬到计算单元」的带宽——权重小了搬运就快，反量化的那点计算开销可以忽略。
- **Weight-activation（如 W8A8）**：权重和激活都量化成整数，用整数 Tensor Core 直接做 INT8 矩阵乘。为什么需要？因为 **Prefill（处理长 prompt）和开大 batch 时是计算密集型**，FP16 反量化路径用不满 Tensor Core 算力，必须两端都量化才能吃满硬件吞吐。
- 激活量化的难点：激活的离群值比权重严重得多（某些通道激活值可达数百倍于其他通道），per-tensor 直接量化精度会崩——这正是 SmoothQuant 等算法要解决的问题。

### 2.5 主流算法演进

#### （1）RTN（Round-To-Nearest）：朴素基线
逐元素四舍五入到最近格点。8-bit 时精度尚可，**3/4-bit 下精度崩塌**——因为 per-group 内的 outlier 和舍入误差累积会显著放大。

#### （2）LLM.int8()：离群值分离（2022）
观察：少数特征维度的激活绝对值极大（可达其他维度的 20 倍以上）。方案是把矩阵乘按列拆成两部分——离群列用 FP16 精确计算，其余 99.9% 用 INT8，结果相加。首次让 175B 模型 INT8 推理不掉点，但 FP16 分支破坏了整数 Tensor Core 的连续性，吞吐提升有限。

#### （3）GPTQ：用二阶信息补偿量化误差（ICLR 2023）
思想源自经典剪枝算法 OBS/OBQ：**量化某一列权重产生的误差，可以被其余未量化列的调整抵消**。逐列量化，用 Hessian（$\mathbf{H} = 2\mathbf{X}\mathbf{X}^\top$，$\mathbf{X}$ 为该层校准输入）指导误差分配：

$$\mathbf{E} = \min \|\mathbf{W}\mathbf{X} - \hat{\mathbf{W}}\mathbf{X}\|^2, \qquad \delta \mathbf{W} = -\frac{w_q - w}{H^{-1}_{qq}} \cdot \mathbf{H}^{-1}_{:,q}$$

即：量化第 $q$ 列后，把误差按 Hessian 逆矩阵的比例摊到后面的列上。配合 Cholesky 分解保证数值稳定、分块 + 懒更新降低计算量，把 OBQ 从「只能处理 1 亿参数」扩展到 175B、几小时量化完。GPTQ 是 INT4 时代的精度标杆。

#### （4）AWQ：激活感知，保护「显著权重」（MLSys 2024 最佳论文）
洞察：**只有约 1% 的权重通道是「显著」的**——判据不是权重本身大，而是它乘到的激活值大（大激活 × 权重量化误差 = 大输出误差）。保护方法是 per-channel scaling：给显著通道乘放大系数 $s$，让它们「撑开」占据更多量化格点，降低相对误差；推理时激活除回 $s$，数学上完全等价（$\mathbf{Y} = (\mathbf{X}/s)(s\mathbf{W})$）。相比 GPTQ：不用反传、校准只需几分钟，W4 精度相当或更好，且对硬件更友好（标准 INT4 GEMM kernel），是当前端侧和 vLLM/SGLang 部署的首选之一。

#### （5）SmoothQuant：把激活的难度「搬」给权重（2022）
仍用 $\mathbf{Y} = (\mathbf{X}/s)(s\mathbf{W})$ 这个等价变换，但目的相反：激活难量化（离群大）、权重好量化（分布平缓），那就数学上把激活的离群性按比例迁移到权重上：

$$s_j = \frac{\max|\mathbf{X}_{:j}|^{\alpha}}{\max|\mathbf{W}_{:j}|^{1-\alpha}}$$

$\alpha \in [0,1]$ 控制迁移强度（常取 0.5），迁移后两者都「一样平缓」，从而实现 W8A8 全整数推理，适合高并发服务化。

#### （6）FP8 与微缩放格式：低精度浮点路线
- **FP8**：两种格式——E4M3（4 位指数、3 位尾数，精度高、范围小，用于前向计算）和 E5M2（范围大、精度低，用于梯度）。相比 INT8，浮点格式对离群值天然更鲁棒，H100/H200 的 FP8 Tensor Core 让 TensorRT-LLM/vLLM 的 FP8 推理精度损失 <1%，已是云端服务的主流之一；
- **MXFP4 / 微缩放（microscaling）**：每 32 个元素共享一个小的 FP8 scale，Blackwell GPU 原生支持，4-bit 下兼顾浮点的鲁棒性与整数的密度；
- **2-bit 前沿**：QuIP#（格点量化 + 随机旋转「摊平」离群值）、BitNet（1.58-bit 三值权重，QAT 路线）——极低位宽必须配合旋转/重训练。

#### （7）GGUF（llama.cpp / Ollama）
面向端侧 CPU/Metal 推理的格式，k-quants 系列按层/按张量重要性混合位宽（如 4-bit 为主、关键层 6/8-bit），不依赖高端 GPU，是个人电脑跑大模型的事实标准。

## 3.重要性质

### 性质 1：显存与带宽近似按位宽比例下降
权重显存 $\approx$ 参数量 × 每参数字节数。FP16（2 字节）→ INT4（0.5 字节），显存降到 1/4；decode 阶段每步读权重的时间也近似降到 1/4。但要注意：**模型小了不等于总显存线性小**——KV Cache 不随权重位宽下降（除非单独做 KV 量化），长上下文时它可能反超权重成为大头。

### 性质 2：量化误差与数值范围成正比，outlier 是低比特精度的头号敌人
单个元素量化误差 $|e| \le s/2$，而 $s$ 由组内最大绝对值决定。一个 100 倍的离群值会让同组其余元素的误差放大 100 倍。GPTQ（误差补偿）、AWQ（显著通道缩放）、SmoothQuant（迁移离群性）、QuIP#（旋转打散离群值）本质上都是围绕 outlier 的不同解题思路。

$$\boxed{\text{一切 LLM 量化难题的根源：激活/权重中的少数离群值撑大了量化范围}}$$

### 性质 3：量化的是误差容忍度，不是信息无损压缩
量化在数学上是有损的，但神经网络对权重的微小扰动有很强的鲁棒性——大量实验表明 4-bit 量化（配合好算法）困惑度（PPL）损失通常在 1% 以内，多数下游任务准确率几乎不变。极限位宽（2-bit）下才需要 QAT 或旋转技术兜底。

### 性质 4：Decode 提速来自带宽，Prefill 提速来自算力
- Decode（访存瓶颈）：W4A16 就能显著提速，速度比 ≈ 位宽比 ×（其他开销修正）；
- Prefill / 大 batch（计算瓶颈）：需要 W8A8 / FP8 才能吃满整数或 FP8 Tensor Core 的算力。

所以工业界常见组合：**云端高并发用 FP8/W8A8 服务化，单卡/边缘用 W4A16（AWQ/GPTQ），端侧 CPU 用 GGUF。**

## 4.与其他概念的关系

### 4.1 模型压缩三剑客对比

| 维度 | 量化 | 剪枝 | 蒸馏 |
|---|---|---|---|
| 压缩对象 | 数值表示精度 | 参数数量/结构 | 模型本身大小 |
| 典型收益 | 显存/带宽按位宽降 | 稀疏化，理论可跳过 0 计算 | 参数量成倍减少 |
| 精度损失 | 4-bit 内很小 | 需要精心选择剪枝对象 | 取决于学生模型容量 |
| 硬件依赖 | 低（有低比特 kernel 即可） | 非结构化稀疏硬件支持差 | 无特殊依赖 |
| 代表工作 | GPTQ、AWQ、SmoothQuant | SparseGPT、Wanda | DistilBERT、Minitron |

三者可组合：常见流水线是「剪枝 + 蒸馏得到小模型 → 量化部署」，实践中**量化是性价比最高、工程门槛最低的一步**。

### 4.2 与推理系统的关系
量化不是孤立的：vLLM/SGLang 原生支持 AWQ/GPTQ/FP8 checkpoint；TensorRT-LLM 主打 FP8 + 算子融合。与[大模型推理](4.大模型推理.md)中的 KV Cache 结合，还可以做 KV Cache 的 FP8/INT8 量化（KV 没有永久离群值问题，量化更安全），长上下文场景收益巨大。

### 4.3 与训练的关系
量化最初是推理技术，现在正在向训练渗透：DeepSeek-V3 等已采用 FP8 混合精度训练，把训练显存与通信也降下来。方向是「全链路低精度」：FP8 训练 → FP8/W4 推理 → FP8 KV Cache。

## 5.具体例子

### 例子 1：手算一个对称 INT8 量化（问题 → 步骤 → 计算 → 结果解读）

**问题**：把权重向量 $\mathbf{w} = [-0.9,\ 0.3,\ 1.5,\ -0.2]$ 量化成 8-bit 对称整数，并评估误差。

**步骤**：
1. 求缩放因子 $s$；
2. 逐元素量化取整；
3. 反量化还原，计算误差。

**计算**：
1. $\max|w| = 1.5$，$s = 1.5 / 127 \approx 0.01181$
2. $w_q = \mathrm{round}(w/s)$：
   - $-0.9 / 0.01181 \approx -76.2 \rightarrow -76$
   - $0.3 / 0.01181 \approx 25.4 \rightarrow 25$
   - $1.5 / 0.01181 \approx 127.0 \rightarrow 127$
   - $-0.2 / 0.01181 \approx -16.9 \rightarrow -17$
3. 反量化 $\hat{w} = s \times w_q$：
   - $-76 \times 0.01181 \approx -0.8976$（误差 $0.0024$）
   - $25 \times 0.01181 \approx 0.2953$（误差 $0.0047$）
   - $127 \times 0.01181 \approx 1.5000$（误差 $0$）
   - $-17 \times 0.01181 \approx -0.2008$（误差 $0.0008$）

**结果解读**：最大误差约 $0.0047 \le s/2 \approx 0.0059$，与理论一致。相对原始值，误差在 1% 左右——对单层可忽略，但若每层误差都朝同一方向累积，几十层后就会可观，这正是 GPTQ「跨列补偿误差」要解决的累积问题。

### 例子 2：70B 模型部署的显存账（为什么量化是唯一解）

**问题**：想在单张 48GB 显存的 GPU（如 A6000）上部署 Llama-3-70B，可行吗？

**步骤**：
1. 计算 FP16 部署所需显存；
2. 计算 INT4（W4A16，group size 128）部署所需显存；
3. 对比并估算 KV Cache 占用。

**计算**：
1. FP16：$70 \times 10^9 \times 2\,\text{B} = 140\,\text{GB}$ —— 单卡差近 3 倍，即使 2 卡 48G 也放不下权重加 KV Cache；
2. INT4：权重 $70 \times 10^9 \times 0.5\,\text{B} = 35\,\text{GB}$，加上 scale 等少量元数据（per-group 128 即每 128 个权重多存一个 FP16 scale，开销约 3%）约 **38 GB**，能装进 48G 卡；
3. KV Cache（INT8 量化、GQA 8 个 kv-head、$d_{head}=128$、80 层、序列长 4096、fp16 计）：
   $2 \times 80 \times 8 \times 128 \times 4096 \times 2\,\text{B} \approx 1.3\,\text{GB}$（单条序列），batch=8 时约 10.7 GB——38 + 10.7 ≈ 48.7 GB，**再给 KV 做 FP8 量化即可安全放下**。

**结果解读**：量化让 70B 模型从「至少 2 台 8 卡服务器级别」降到「单卡可跑」，同时 decode 阶段每步读权重从 140 GB 降到约 35 GB，理论 token 生成速度最高可提升约 4 倍（实际受 kernel 与计算开销影响，常见 2~3 倍）。这就是 4-bit 量化成为单卡部署标配的原因。

## 6.常见面试问题

### Q1：什么是模型量化？为什么大模型推理特别需要量化？

**答**：量化是把 FP32/FP16 数值映射到低位宽（INT8/INT4/FP8）表示的有损压缩技术，核心是一个带 scale 的线性映射，反量化后近似还原。大模型推理特别需要，因为瓶颈在显存容量和带宽而非算力：权重显存和每步权重读取量都随位宽近似线性下降，FP16→INT4 显存降到 1/4，decode 速度可提升 2~3 倍；同时 4-bit 下精度损失（PPL）通常在 1% 以内，代价极小。

### Q2：对称量化和非对称量化有什么区别？分别适合什么场景？

**答**：对称量化的映射是 $\hat{w} = s \times w_q$，格点关于 0 对称，只存一个 scale；非对称多存一个零点，$\hat{w} = s \times (w_q - z)$，格点范围可平移。权重分布一般近似关于 0 对称，用对称量化省一个参数；ReLU 后激活全非负、分布偏一侧，用非对称量化格点利用率更高。直觉：数据分布「居中」用对称尺子，「偏侧」用可平移的尺子。

### Q3：直接把权重 INT4 化（RTN）精度为什么会崩？LLM 量化的主要难点是什么？

**答**：主要难点是**离群值（outlier）**。量化误差 $|e| \le s/2$，$s$ 由组内最大绝对值决定——LLM 权重和激活中存在少数绝对值极大的通道（激活尤甚，可达其他通道数十倍），会把 scale 撑大，使其余 99% 的正常值只能用粗格点，误差被放大并逐层累积。RTN 不做任何处理，8-bit 时误差尚可容忍，3/4-bit 下就超过模型容忍度，PPL 崩溃。所以 GPTQ/AWQ/SmoothQuant 本质都是围绕 outlier 的对策。

### Q4：介绍 GPTQ 的核心思想。

**答**：GPTQ 是基于二阶信息的训练后量化。关键观察：量化第 $q$ 列权重产生的输出误差，可以通过微调**其余未量化列**来补偿。它以逐层输出重建误差 $\min\|\mathbf{W}\mathbf{X} - \hat{\mathbf{W}}\mathbf{X}\|^2$ 为目标，用 Hessian $\mathbf{H}=2\mathbf{X}\mathbf{X}^\top$（$\mathbf{X}$ 是校准输入）决定误差的补偿方向和步长，逐列量化、逐列摊误差。工程上用 Cholesky 分解保证 $H^{-1}$ 数值稳定、分块+懒更新降低计算量，把只能处理 1 亿参数的 OBQ 扩展到 175B。优点是 4-bit 精度高；缺点是量化过程较慢（离线小时级）。

### Q5：AWQ 的核心洞察是什么？和 GPTQ 有什么本质区别？

**答**：AWQ 的洞察：只有约 1% 的权重通道是「显著」的，判据是**它乘到的激活值大**（激活大 × 权重量化误差 = 输出误差被放大），而不是权重本身大。保护方法是 per-channel scaling：给显著通道乘一个 $s>1$ 的系数撑开占更多量化格点，激活端除回 $s$，数学上完全等价（$\mathbf{Y}=(\mathbf{X}/s)(s\mathbf{W})$），零成本保护关键通道。与 GPTQ 的区别：AWQ 不做误差补偿、不依赖反传，校准只要几分钟（GPTQ 要小时级）；GPTQ 优化「误差如何在列间摊销」，AWQ 优化「哪些通道值得更高分辨率」——一个减总误差，一个把误差预算花在刀刃上。同位宽下 AWQ 精度相当或更好、对硬件更友好，是当前 W4A16 部署的主流。

### Q6：SmoothQuant 解决什么问题？公式里的 α 起什么作用？

**答**：解决**激活难量化**的问题：激活的离群值远比权重严重，W8A8 全整数推理时激活端 per-tensor 量化精度会崩。SmoothQuant 用等价变换 $\mathbf{Y}=(\mathbf{X}/s)(s\mathbf{W})$，按 $s_j = \max|\mathbf{X}_{:j}|^{\alpha}/\max|\mathbf{W}_{:j}|^{1-\alpha}$ 把激活的离群性「数学上搬」到权重上，使两端分布都平缓。$\alpha$ 是迁移强度：$\alpha=0$ 完全迁给权重，$\alpha=1$ 完全迁给激活，常取 0.5 让两者各承担一半、整体误差最小。它和 AWQ 用同一个等价变换，但哲学相反：AWQ 拉大显著通道的 scale（保护权重），SmoothQuant 压平激活的 scale（迁走难度）。

### Q7：W4A16 在 decode 阶段快，为什么 prefill 和高并发场景还要 W8A8/FP8？

**答**：因为两个阶段的瓶颈不同。Decode 每步只算 1 个 token，是访存密集型：瓶颈是把权重从显存搬到计算单元的带宽，权重 INT4 后搬运量降 4 倍，反量化回 FP16 计算的开销可忽略，所以 W4A16 就有效。Prefill 处理整段 prompt、大 batch 时是计算密集型：瓶颈在矩阵乘本身的算力，FP16 反量化路径无法使用 INT8/FP8 Tensor Core 的高算力，必须权重和激活都量化成整数/FP8 才能吃满硬件吞吐。所以经验法则是：单卡小 batch 用 W4A16（AWQ），云端高并发服务用 W8A8（SmoothQuant）或 FP8。

### Q8：FP8 有哪两种格式？和 INT8 量化相比有什么优势？

**答**：FP8 有 E4M3（4 位指数 3 位尾数：精度高、动态范围约 ±448，用于前向激活与权重）和 E5M2（5 位指数 2 位尾数：范围大、精度低，适合梯度）。相比 INT8：浮点格式在更大动态范围内近似均匀相对误差，对离群值天然鲁棒，不需要复杂的校准和离群值处理；且 H100+ 硬件原生支持 FP8 Tensor Core，精度损失 <1%。代价是相同位宽下 FP8 的「均匀格点在原点附近」分布不如整数密集，极值附近相对误差更大——但实践中 E4M3 的表现已足够好，是云端 FP8 服务化（TensorRT-LLM、vLLM）的主流选择。

### Q9：如何评估一个量化模型的质量？

**答**：三个层次：
1. **困惑度（PPL）**：在 WikiText-2、C4 等数据集上对比量化前后 PPL，4-bit 好算法通常相对损失 <1%；
2. **下游任务**：MMLU、GSM8K 等零样本/少样本准确率——PPL 变化小不代表推理类任务不退化，两者要一起看；
3. **长文本与生成质量**：长上下文场景下注意力异常累积（如重复生成、复读）是量化常见失效模式，需专门的 needle-in-haystack 和长文本生成评测。此外还要区分「量化全过程质量」：校准数据的代表性、per-group 配置、以及是否对 embedding/lm_head 等敏感层保留高精度（llama.cpp 混合位宽的思路）。

### Q10（进阶）：如果要做到 2-bit 甚至 1.58-bit，PTQ 还可行吗？谈谈技术路线。

**答**：2-bit 下 RTN 和常规 PTQ 基本不可行，需要更强的结构性措施，三条路线：
1. **旋转不变量**：注意力对正交变换不变，QuIP# 等方法先对权重和激活做随机 Hadamard 旋转，把离群值「摊匀」到所有维度后再量化，配合格点码本可把 2-bit PPL 损失压到可接受范围；
2. **QAT**：BitNet 用三值权重 {-1, 0, 1}（1.58-bit）从头训练/继续训练，让模型原生适应极端低精度，但训练成本高，适合自研模型厂商；
3. **混合位宽**：只对不敏感层用 2-bit、敏感层保 4/8-bit，工程上更稳妥。
决策依据：有训练资源且追求极致端侧压缩 → QAT；只能拿开源模型 → 旋转 + 格点量化的 PTQ；生产环境稳妥 → 还是 4-bit 起步。

## 参考资料

- Frantar et al. *GPTQ: Accurate Post-Training Quantization for Generative Pre-trained Transformers*. ICLR 2023
- Lin et al. *AWQ: Activation-aware Weight Quantization for LLM Compression and Acceleration*. MLSys 2024 (Best Paper)
- Xiao et al. *SmoothQuant: Accurate and Efficient Post-Training Quantization for Large Language Models*. ICML 2023
- Dettmers et al. *LLM.int8(): 8-bit Matrix Multiplication for Transformers at Scale*. NeurIPS 2022
- Micikevicius et al. *FP8 Formats for Deep Learning*. arXiv 2022
- llama.cpp k-quants 文档：https://github.com/ggerganov/llama.cpp
- 本仓库相关笔记：[4.大模型推理](4.大模型推理.md)（decode 访存瓶颈与 KV Cache）
