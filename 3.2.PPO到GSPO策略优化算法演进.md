# 从 PPO 到 GSPO：LLM 策略优化算法演进与差异对比

## 目录
- [1.概述](#1概述)
- [2.演进全景：每个算法改了什么](#2演进全景每个算法改了什么)
- [3.逐维度差异对比](#3逐维度差异对比)
- [4.重要性质](#4重要性质)
- [5.具体例子](#5具体例子)
- [6.常见面试问题](#6常见面试问题)
- [参考资料](#参考资料)

## 1.概述

**一句话总览**：PPO、GRPO、DAPO、GSPO 等算法本质上是同一条策略梯度流水线——「采样 → 估优势 → 限幅更新」——的不同装配方案，后续算法各自只改动了流水线中的一两个零件：**GRPO 删掉了 critic，RLOO 换掉基线与归一化方式，REINFORCE++ 简化优势估计，DAPO 修补 GRPO 的四个工程缺陷，Dr. GRPO 修正它的两个数学偏差，GSPO 把优化粒度从 token 提升到序列，CISPO/SAPO 则继续改造裁剪机制本身**。

**为什么值得横向梳理**：
- RLHF / RLVR（可验证奖励强化学习）是大模型后训练的核心环节，算法选型直接决定显存成本、稳定性和推理能力涌现；
- 这些算法的差异点非常集中（优势估计、重要性采样粒度、loss 归一化、KL 处理、裁剪方式五个维度），用一张对比表就能说清，是面试高频考点；
- 演进逻辑本身反映了工业实践的判断：**在大模型场景下，复杂的方差缩减技术（学 critic）性价比下降，而「限制单步更新幅度」这一信任域思想始终保留**。

**演化主线速览**：

```
REINFORCE (1992, 纯策略梯度, 高方差)
   │  +critic +GAE +clip
   ▼
PPO (2017) ── 用于 RLHF (InstructGPT 2022): 4 模型、token 级 KL 惩罚
   │
   ├─ ReMax / RLOO (2023): 用 leave-one-out baseline 替代 critic
   ├─ REINFORCE++ (2025): batch 级归一化 + 序列级 IS,极简主义
   │
   └─ GRPO (2024, DeepSeekMath): 组内标准化优势,去 critic
        │  DeepSeek-R1 用其涌现长链推理 → 引爆开源社区
        ├─ DAPO  (2025, 字节): clip-higher / 动态采样 / token级loss / 软长度惩罚
        ├─ Dr.GRPO (2025, Sea AI Lab): 去掉 std 归一化和长度归一化,修正偏差
        ├─ CISPO (2025, MiniMax): 裁剪重要性权重而非更新幅度,不丢 token 梯度
        └─ GSPO (2025, 阿里 Qwen): 序列级重要性比率,稳定 MoE 训练 (Qwen3 采用)
```

## 2.演进全景：每个算法改了什么

### 2.1 共同骨架：所有算法都长一个样

几乎所有这些算法的目标函数都可以写成统一形式——**每条回答的梯度贡献 ≈ 优势 × 重要性比率 × 对数概率**：

$$g_i \propto \hat{A}_i \cdot w_i(\theta) \cdot \nabla_\theta \log \pi_\theta(y_i \mid x)$$

其中各符号含义：
- $x$：prompt；$y_i$：第 $i$ 条回答（rollout），长度记 $|y_i|$；
- $\hat{A}_i$：**优势估计**——「这条回答比平均水平好多少」，是各算法分歧最大的地方；
- $w_i(\theta)$：**重要性比率**——新策略与采样时旧策略 $\pi_{\theta_{\text{old}}}$ 的概率比，允许一批数据复用多个优化 step；
- 限幅机制（clip / 惩罚 / 门控）：防止 $w_i$ 失控把训练打崩，是 TRPO/PPO 留下的核心遗产。

沿这条流水线，每个算法只需要回答四个问题，就得到自己的「配方」：

1. **优势 $\hat{A}_i$ 怎么估？**（学 critic 逐 token 估？还是同 prompt 组内 / 全局 batch 相对比较？）
2. **重要性比率在哪个粒度算？**（token 级、序列级、还是几何平均？）
3. **loss 怎么归一化？**（除以总 token 数？除以每条长度再平均？还是直接求和？）
4. **KL 怎么管、探索怎么保？**（reward 内惩罚、loss 内正则、还是不管 KL 靠裁剪？）

### 2.2 PPO：带 critic 的原点

目标函数（详见 [3.1.PPO算法详解](3.1.PPO算法详解.md)）：

$$L^{\text{CLIP}}(\theta) = \mathbb{E}\Big[\min\big(r_t(\theta)\hat{A}_t,\ \text{clip}(r_t(\theta), 1-\epsilon, 1+\epsilon)\hat{A}_t\big)\Big]$$

- **优势**：critic $V_\phi(s_t)$ + GAE 逐 token 给出 $\hat{A}_t$，方差最低；
- **代价**：要额外训练一个和 policy 同量级的价值网络（显存翻倍），且 $V$ 估不准时优势也跟着错；
- **KL**：RLHF 场景把 $-\beta\log\frac{\pi_\theta}{\pi_{\text{ref}}}$ 逐 token 混进 reward。

### 2.3 GRPO：删掉 critic，组内比

DeepSeekMath（2024）提出。对每个 prompt $x$ 采样 $G$ 条回答，用**组内标准化**替代 critic：

$$\hat{A}_i = \frac{r_i - \mathrm{mean}\{r_1,\dots,r_G\}}{\mathrm{std}\{r_1,\dots,r_G\}}$$

$r_i$ 是整条回答的标量奖励（RM 分或规则判分），组内所有 token 共享同一个 $\hat{A}_i$。目标函数保留 PPO 的 clip 结构（token 级比率 $w_{i,t}=\frac{\pi_\theta(y_{i,t}\mid x,y_{i,<t})}{\pi_{\theta_{\text{old}}}(y_{i,t}\mid x,y_{i,<t})}$），KL 从 reward 挪到 loss 里做独立正则（系数 $\beta$ 典型 0.04）。

- **收益**：显存近乎减半（去掉 critic）；同一 prompt 组内比较天然消除了「题目难易」对奖励尺度的干扰；
- **遗留问题**：std 归一化和长度归一化引入数学偏差（→ Dr. GRPO）、token 级比率高方差（→ GSPO）、裁剪压熵（→ DAPO/CISPO）。

### 2.4 REINFORCE++：极简主义的回摆

Hu et al.（2025）。主张「REINFORCE 配上好的批归一化就够用」：

$$\nabla_\theta J = \frac{1}{G}\sum_{i=1}^{G} \mathrm{trim}\big(w_i(\theta)\big)\,\hat{A}_i\,\frac{1}{|y_i|}\sum_t \nabla_\theta \log \pi_\theta(y_{i,t}\mid\cdot)$$

- 优势 = 整条回报 $R_i$ 减去 **batch 内均值**（不做组内、不除 std），再 whitening；
- 重要性比率是**序列级**的（$w_i$ 整条共享一个值），截断（trim）代替 clip；
- token 级 KL 估计用 k3 无偏估计器，batch 归一化。

效果上与 GRPO 相当或更稳，被多家开源框架（OpenRLHF 等）作为默认选项。它证明了：**critic 和组归一化都不是必需的，真正干活的是 baseline 减均值 + 限幅**。

### 2.5 RLOO：leave-one-out 基线

Ahmadian et al.（2024，Google DeepMind，arXiv:2402.10265）。同样每 prompt 采 $k$ 条，但优势用「留一法」估计——第 $i$ 条的 baseline 是**其余 $k-1$ 条**的平均：

$$\hat{A}_i = r_i - \frac{1}{k-1}\sum_{j\ne i} r_j$$

直觉：自己不能给自己当参照系，去掉自己后基线与本条样本独立，估计**无偏**。相比 GRPO 不做 std 归一化，因此没有「难度偏差」（见 2.7）。常与「重复采样数据增强」搭配：同一 prompt 的多条回答都进 loss。它主张：GRPO 的成功来自去 critic 与减均值基线，而非标准化本身。

### 2.6 DAPO：GRPO 的四个工程补丁

Yu et al.（2025，字节 Seed），面向大规模长链推理 RL 训练，四个改动各自对应 GRPO 的一个可观测故障：

| 改动 | 针对的问题 | 做法 |
|------|-----------|------|
| **Clip-Higher**（解耦上下裁剪 $\varepsilon_{\text{low}},\varepsilon_{\text{high}}$，如 0.2/0.28） | 负优势 token 的比率被压到下限后梯度消失，低概率 token 没机会翻身 → 熵坍缩 | 放宽上界，给低概率 token「涨回来」的空间，维持探索 |
| **动态采样** | 组内全对/全错时 $\hat{A}_i \equiv 0$，白占 batch 却零梯度 | 过采样后过滤掉准确率为 0 或 1 的组，保证 batch 内有效梯度占比（可验证奖励任务） |
| **Token-Level Policy Gradient Loss** | GRPO 先除以回答长度 $\mathrm{len}(y_i)$ 再对样本平均 → 长回答里每个 token 的话语权被稀释 | loss 分母用 batch 总 token 数 $\sum_i \mathrm{len}(y_i)$ 而非 $G \cdot \mathrm{len}(y_i)$，长回答不被降权 |
| **Overlong Reward Shaping** | 超长回答被截断后判错，产生虚假惩罚噪声 | 只对截断进区间 $(L_{\max}-2048, L_{\max}]$ 的回答给线性软化惩罚 |

另外 DAPO 直接去掉 KL 项（可验证奖励场景不怕跑偏），并用 KL 散度监控代替约束。

### 2.7 Dr. GRPO：拆掉两个有偏的归一化项

Liu et al.（2025，Sea AI Lab），「Detecting & Removing the Spurious Bias in GRPO」。指出 GRPO 的两处数学缺陷：

1. **难度偏差（std 归一化）**：$\hat A_i = \frac{r_i-\bar r}{\mathrm{std}}$ 会让「刚好一半对一半错」的中等难度题目主导梯度——二值判分下组内奖励标准差 $\approx \sqrt{p(1-p)}$（$p$ 为准确率），在 $p=0.5$ 处最大、越简单/越难的题被压得越小，除以 std 等于给中等题的梯度乘上最大权重；全对/全错的题则本就无信号。简单题和难题学不到东西，训练信号被人为扭曲；
2. **长度崩溃（response length normalization）**：GRPO 的 loss 除以 $|y_i|$ 导致**短回答的每 token 梯度被放大**，模型有动力「写短话蹭梯度」，出现长度先涨后崩的非单调曲线。

修正极其简单——**去掉分母的 std，loss 不用 $|y_i|$ 归一化**（组内 Advantage 直接用 $r_i-\bar r$，token 系数用常数 $1/L$ 或直接求和）。论文用数学题实验证明：修正后长度稳定，准确率显著更高。

### 2.8 GSPO：把「优化单位」对齐到「奖励单位」

Zheng et al.（2025，阿里 Qwen 团队），Qwen3 系列（Instruct/Coder/Thinking）的训练算法。核心论点：**GRPO 在 token 级用单样本上的概率比作重要性权重，数学上是病态的（ill-posed）**——重要性采样修正的是「分布」，而每个 token 位置只被采样一次，逐 token 的比值起不到分布校正作用，只注入随长度累积的高方差噪声，长序列 + MoE 场景下会直接导致训练崩溃。

奖励是发给整条序列的，所以重要性比率和裁剪也应该在序列级。GSPO 定义**长度归一化的序列级比率**（即逐 token 比值的几何平均）：

$$s_i(\theta) = \left(\frac{\pi_\theta(y_i\mid x)}{\pi_{\theta_{\text{old}}}(y_i\mid x)}\right)^{1/|y_i|} = \exp\left(\frac{1}{|y_i|}\sum_{t=1}^{|y_i|} \log \frac{\pi_\theta(y_{i,t}\mid\cdot)}{\pi_{\theta_{\text{old}}}(y_{i,t}\mid\cdot)}\right)$$

目标函数沿用 PPO 的 clip 形式，只是把 $w_{i,t}$ 换成 $s_i$（组内序列共享一个优势 $\hat A_i$，仍用 GRPO 的组均值基线）。几何平均使不同长度的 $s_i$ 数值范围一致，一个裁剪系数 $\epsilon$（典型 $3\times10^{-4}$ 量级，注意比 token 级的 0.2 小得多）可以通吃所有长度。

**对 MoE 的意义**：训练-推理引擎的数值差异会使专家路由翻转，token 级比率剧烈波动，GRPO 需要 Routing Replay（缓存并重放旧策略的激活路径，费显存费通信）才能稳住；GSPO 只看序列整体似然，对单 token 路由抖动天然不敏感，直接稳定训练 MoE。

**局限**：序列级裁剪更「钝」——一条回答里个别 token 概率暴涨也可能整条不被裁，需配合监控；多轮/工具调用场景论文给出 token-level 变体（比率取被 mask 掉 token 之外的平均）。

### 2.9 CISPO 与 SAPO：继续改造「限幅」本身

- **CISPO**（MiniMax-M1，2025）：批评 PPO 系「裁剪 token 更新幅度」会把被裁 token 的梯度完全丢弃（信息浪费）。改为**裁剪重要性权重本身并 detach**：$w'_{i,t}=\mathrm{clip}(w_{i,t},\,\text{lo},\,\text{hi})$（常数上下界），然后用 $w'_{i,t}\,\nabla_\theta\log\pi_\theta$ 更新——**所有 token 都保留梯度**，只限制其话语权上限。适合「少而关键 token 决定成败」的推理任务。
- **SAPO**（Qwen，2025.11）：GSPO 的软版本——用温度化的 **soft gate** $\big(g_i(\theta)\big)^{1/\tau_i}$ 取代硬裁剪，正负优势用不同温度平滑过渡，梯度不再在边界处突变截断；且冷热启动/不同阶段自适应选择序列级或 token 级粒度。定位是大规模训练的「更平稳版 GSPO」。

## 3.逐维度差异对比

### 3.1 总览表

| 算法 | 优势估计 | IS/裁剪粒度 | loss 归一化 | KL 处理 | 关键增量 |
|------|---------|------------|------------|---------|---------|
| PPO | critic + GAE（逐 token） | token | 逐 token 平均 | reward 内 $-\beta\log\frac{\pi}{\pi_{\text{ref}}}$ | 信任域基线 |
| GRPO | 组内 $(r_i-\bar r)/\mathrm{std}$，序列共享 | token | $\frac{1}{G}\sum_i \frac{1}{\mathrm{len}(y_i)}\sum_t$（先内后平均） | loss 内正则 | 去 critic |
| RLOO | 组内留一 $r_i-\frac{1}{k-1}\sum_{j\ne i}r_j$ | 序列（REINFORCE 式） | 样本平均 | 通常无 | 无偏基线 |
| REINFORCE++ | batch 均值基线 + whitening | 序列级 trim | 序列内 token 平均 | loss 内（k3 估计） | 极简 |
| DAPO | 同 GRPO | token，上下界解耦 | $\frac{1}{\sum_i \mathrm{len}(y_i)}\sum_i\sum_t$（token 级） | 去掉，仅监控 | 四工程补丁 |
| Dr. GRPO | 组内 $r_i-\bar r$（**不除 std**） | token | 常数 $1/L$ 或求和 | loss 内 | 去偏 |
| GSPO | 同 GRPO（组均值基线） | **序列级几何平均** | 序列平均 | 可选正则 | 粒度对齐奖励 |
| CISPO | 组内（GRPO 式） | token 权重裁剪（不丢梯度） | token 级 | 无 | 保信息 |
| SAPO | 同 GRPO | 序列级软门控 | 序列平均 | 无 | 平滑限幅 |

### 3.2 差异的本质：四个正交旋钮

1. **baseline 的信息量排序**：$R_t - V_\phi(s_t)$（逐状态，最细但最贵）＞ 组内相对（同题对比，消题目难度）＞ batch 全局相对（最糙但零成本）。GRPO 系赌的是：**LLM 经 SFT 后单条奖励噪声大，但同 prompt 组内相对比较足够准**，critic 学的「状态价值」在文本生成里本来就难估。
2. **IS 粒度之争（token vs 序列）**：token 级粒度细、能区分「回答里哪步好」，但奖励根本给不出 token 级信号，细粒度是伪精细；序列级与奖励粒度一致、方差低，但一荣俱荣一损俱损。这条线是当前前沿主战场（GSPO/SAPO vs DAPO/CISPO 阵营）。
3. **归一化即偏置**：除以 $\mathrm{std}$ → 难度偏差；除以 $|y_i|$ → 长度偏差。Dr. GRPO 的贡献是提醒大家：**任何归一化都在重新分配梯度话语权，必须论证其合理性**。
4. **限幅方式**：硬 clip（梯度归零）→ 权重裁剪（梯度保留、幅度受限）→ soft gate（连续衰减），逐步用信息换稳定。

## 4.重要性质

**性质 1：算法在「删组件」与「补偏差」之间摆动**
2023-2024 的主题是删：删 critic（GRPO/RLOO/REINFORCE++）、删 RM（RLVR 用规则判分）、删 KL（DAPO）。2025 之后的主题是补：补长度偏差（DAPO token-loss、Dr. GRPO）、补难度偏差（Dr. GRPO）、补熵坍缩（Clip-Higher）、补分布校正粒度（GSPO）。「删」的红利吃完后，大家发现省下的每一步都要用理解力还回去。

**性质 2：所有归一化因子都是隐式课程权重**
GRPO 除 std 后，同等优势差值下，组内奖励越「纠结」（接近对半开）梯度被 $\frac{1}{\mathrm{std}}$ 放大越强 → 等价于自动选择中等难度课程，但放大比例失衡（准确率 $p$ 的题目权重 $\propto \frac{1}{\sqrt{p(1-p)}}$ 级别）。去掉 std 后优势只反映「相对好坏的原始差值」，课程更中性。这说明：**RL 算法的内核不只是优化器，还是数据调度器**。

**性质 3：GRPO 组内共享优势 ⇒ 无法做步级信用分配**
$\hat A_i$ 整条序列同值，意味着「第 37 个 token 写错了」和「第 5 个 token 写对了」获得同等力度的更新；错误会被正确 token 稀释、正确会被错误连坐。PRIME（隐式过程奖励）、TreePO（树状分支归因）等针对此点，但需要过程级标注或树搜索成本。DAPO/GSPO 阵营的回应是：接受粗粒度，把序列级统计做稳更重要。

**性质 4：训练-推理不一致是新战场**
rollout 由推理引擎（vLLM/SGLang）产出、训练由 Megatron/FSDP 完成，两者数值精度差异（MoE 路由翻转、logits 精度）会让「旧策略概率」对不上。token 级比率对此极其敏感（GSPO 论文指出 GRPO 每次更新平均激活约 10% 新专家），这成为序列级方法兴起的直接工程动因之一；配套手段还有 FP32 logits、训练-推理数值对齐等。

**性质 5：算法选择 = 任务奖励形态 × 资源预算**
结果可验证（数学/代码）→ GRPO/DAPO 系（规则判分，免 RM）；偏好驱动（对话/写作）→ 仍需 PPO 或 GRPO+RM；显存极紧 → 无 critic 系 + 单阶段；MoE 大模型 → GSPO/SAPO；追求每 token 信息效率（推理关键步）→ CISPO。没有全能冠军，只有场景匹配。

## 5.具体例子

### 例子 1：手算 GRPO 组内优势——退化组为什么零梯度

**问题**：对某数学题采样 $G=4$ 条回答，奖励为 $r = \{1,\ 0.5,\ -0.5,\ -1\}$；另一批有 $r' = \{1, 1, -1, -1\}$ 和 $r'' = \{1, 1, 1, 1\}$ 两组，各自优势是多少？

**步骤与计算**：
- 第一组：$\bar r = 0$，样本标准差 $\mathrm{std} = \sqrt{(1+0.25+0.25+1)/4} = 0.866$，得 $\hat A = \{1.15,\ 0.58,\ -0.58,\ -1.15\}$；
- $r'$ 组：$\bar r = 0$，std = 1，$\hat A = \{1, 1, -1, -1\}$——注意与第一组相比，即使把 std 换成 Dr. GRPO 的不归一化版本 $\{1,1,-1,-1\}$，**符号结构完全相同**，std 只是整体缩放；
- $r''$ 组：$\bar r = 1$，$r_i - \bar r \equiv 0$ → $\hat A \equiv 0$，**本组梯度为零**（GRPO 里还会除以 std=0，实现上按 0 处理）。

**结果解读**：全对/全错的组是「无效样本」——占着 rollout 算力和 batch 名额却贡献零梯度。数学题 RL 训练后期准确率两极分化，大量组退化，这就是 DAPO 动态采样的动机（见例子 4）。同时第一组 vs $r'$ 组可见：std 归一化真正起作用的差异在**组与组之间**的难度权重，而不是组内排序。

### 例子 2：GRPO vs Dr. GRPO vs DAPO 的三种归一化（长度偏差实算）

**问题**：一组 $G=2$，判分二值：回答 A 长 $|y_A|=1000$ token 且正确（$r=1$），回答 B 短 $|y_B|=100$ 且错误（$r=0$）。用 $\bar r=0.5$、$\mathrm{std}=0.5$，三种 loss 形式下「正确」各拿到多少总梯度、每 token 多重？

**步骤与计算**：组内优势 $\hat A_A = (1-0.5)/0.5 = +1$，$\hat A_B = -1$。

| loss 形式 | 正确答案 A 的总梯度权重 | A 的每 token 权重 |
|---|---|---|
| GRPO：$\frac{1}{G}\sum_i \frac{1}{\mathrm{len}(y_i)}\sum_t$ | $\frac{1}{2}\times 1 = 0.5$ | $0.5/1000 = 5\times10^{-4}$ |
| Dr. GRPO：$\frac{1}{G}\sum_i \sum_t$ | $\frac{1000}{2} = 500$ | $0.5$ |
| DAPO token-level：$\frac{1}{\sum_i \mathrm{len}(y_i)}\sum_i\sum_t$ | $\frac{1000}{1100} \approx 0.909$ | $\approx 9.1\times10^{-4}$ |

**结果解读**：GRPO 把 1000 个 token 的正确推理链压缩成 0.5 的话语权，而 100 个 token 的错误只有 0.5——梯度对「长度」的感知与对错无关，纯粹被归一化扭曲，这就是长度崩溃的机制；Dr. GRPO 矫枉过正地让长回答话语权线性膨胀（有注水激励）；DAPO 的 token 级折中被广泛采用（ verl/TRL 默认支持）。三种写法优势排序完全一致，**差的只是每个 token 的梯度斤两**。

### 例子 3：Clip-Higher——熵坍缩的数值演示

**问题**：$\varepsilon_{\text{low}}=0.2$ 时，某低概率 token（$\pi_{\text{old}}=0.01$）在连续负优势锤打后比率跌到 $w=0.7$（概率 0.007）。GRPO 继续训练时它的梯度如何演化？若想让它可以涨回 0.013，需要什么条件？

**步骤与计算**：
- 当 $w < 1-\varepsilon_{\text{low}} = 0.8$ 且优势仍为负时，$\min$ 取到被封顶的 clip 项 $(1-\varepsilon_{\text{low}})\hat A$（$\hat A<0$ 时为较大者）→ 梯度为零：概率已被压出区间，**系统不再继续压**（这是保护）；
- 但回升是另一回事：此后该 token 只有出现在正优势的组里才有回升梯度，且增速被上限 $1+\varepsilon=1.2$ 限制——训练后期熵已整体坍缩、负优势锤击远多于正优势，**被锤进下限的 token 极难翻身**，策略熵单调下降；
- Clip-Higher：取 $\varepsilon_{\text{high}}=0.28$（DAPO 设定），上限放宽到 $1.28$，被压低的 token 在正优势时获得更多回升空间 → DAPO 实验中训练全程熵保持平稳，AIME 准确率显著抬升。

**结果解读**：上下界解耦承认了一个不对称事实：**「压错」的伤害有界（失去一个模式），「再也抬不起来」的伤害无界（模式永久死亡）**。这是 2025 年后各算法（Clip-Higher、CISPO 不丢梯度、SAPO 软门控）共同的哲学——限幅要限「变坏」，别限「变好」。

### 例子 4：DAPO 动态采样 + token-level loss 的一次迭代

**问题**：某 step batch 大小 512（每 prompt 采 $G=16$，即 32 个 prompt）。统计发现 8 个 prompt 的组全对（128 条）、4 个全错（64 条）。问：有效梯度样本占比多少？动态采样怎么做？

**步骤与计算**：
- 退化组共 $8+4=12$ 个 prompt、192 条回答，优势全为 0 → 有效样本 $512-192=320$ 条，占比 $320/512 = 62.5\%$；
- DAPO 做法：过采样至约 $512/0.625 \approx 820$ 条，过滤后保留恰好 512 条有效回答（组内准确率 $\in(0,1)$），并只保留这些组参与梯度；
- 再叠加 token-level loss：设有效回答平均长度 4000、被过滤的退化组平均 8000，GRPO 式归一化里长而无效的回答仍会通过分母影响其他样本权重，DAPO 式 $\frac{1}{\sum |y_i|}$ 在过滤后分母只剩有效 token，梯度尺度更纯净。

**结果解读**：训练后期准确率高时退化组占比可超 50%，rollout 成本（GRPO 最贵的一环）一半打水漂。动态采样把「算力换方差」做到极致：零优势样本既不进梯度也不进分母，等效 batch 的信息密度翻倍。

### 例子 5：token 级比率 vs GSPO 序列级比率的方差

**问题**：一条 $|y|=2000$ 的回答，训练后各 token 的 log 比率 $\log w_t$ 独立同分布，均值 0、标准差 0.1（典型的小幅更新）。求 GRPO 与 GSPO 各自「该回答拿到的权重」的波动幅度。

**步骤与计算**：
- GRPO：每个 token 用自己的 $w_t$ 参与裁剪与缩放，极端 token 的 $\log w_t$ 可达 $\pm 3\sigma = \pm 0.3$，即单 token 权重在 $[0.74,\ 1.35]$ 摆动，长序列尾部几乎必然存在被裁 token；
- GSPO：序列权重 $s = \exp\big(\frac{1}{2000}\sum_t \log w_t\big)$，指数项的标准差为 $\frac{0.1}{\sqrt{2000}} \approx 0.0022$ → $s$ 的波动约 $\pm 0.7\%$（$\pm 3\sigma$ 内 $[0.993,\ 1.007]$），3σ 之外的病态 token 被几何平均稀释掉；
- 这也解释了 GSPO 论文里 $\varepsilon \approx 3\times10^{-4}$ 量级的裁剪系数为何比 PPO/GRPO 的 0.2 小三个数量级——**比率的数值尺度完全不同，不可直接迁移超参**。

**结果解读**：token 级比率的问题不是「不准」，而是奖励根本不在 token 级——逐 token 比值不提供分布校正，只注入噪声，且序列级的对数比率噪声随 $\sqrt{|y|}$ 累积，长序列 + MoE 下被裁剪机制进一步放大；GSPO 用几何平均把长序列拉回统一尺度，代价是丢失 token 区分度——再次呼应 3.2 节的粒度之争。

## 6.常见面试问题

### Q1：GRPO 相比 PPO 改了什么、代价是什么？
**答**：改动：删 critic，优势从「$R_t - V_\phi(s_t)$ 逐 token」变成「组内标准化 $\frac{r_i-\bar r}{\mathrm{std}}$ 整条共享」；KL 从 reward 惩罚变为 loss 正则。收益：显存近乎减半、少一个难训的价值网络、组内比较自动消除题目难度差异。代价：失去步级 credit assignment（整条回答同罪同赏）、std/长度归一化引入偏差、rollout 数 $G$ 增大（典型 8~16）。

### Q2：GRPO 里除以 std 为什么被 Dr. GRPO 称为「偏置」？
**答**：因为缩放因子 $\frac{1}{\mathrm{std}}$ 依赖组内奖励分布，而奖励分布反映的是**题目难度**：中等难度（对半开）的组获得最大梯度权重，接近全对/全错的题被压制，相当于给模型强加了一个未经论证的课程。期望优势 $\mathbb{E}[\hat A_i]$ 因此不再与「回答真实好坏」线性对应。去掉 std 后优势只由 $r_i - \bar r$ 决定，排序不变、权重中性，实验显示长链推理训练显著更稳。

### Q3：为什么 GSPO 说 GRPO 的 token 级重要性比率是 ill-posed？
**答**：重要性采样比值 $\frac{\pi_\theta(a\mid s)}{\pi_{\text{old}}(a\mid s)}$ 是对**分布差异的校正因子**，其良好性质（无偏性、方差可控）建立在「对 $a$ 的分布充分采样」上；LLM 生成中每个位置动作只被采样一次，逐 token 比值不执行任何分布校正，只提供单点乘性噪声。奖励在序列级、优势在序列级，却用无意义的 token 级权重去缩放梯度，噪声还随序列长度累积（MoE 路由抖动放大此效应）→ 训练崩溃。GSPO 的修正：比率、裁剪、优化三者统一为序列级，且用 $1/\mathrm{len}(y_i)$ 次方（几何平均）稳定数值尺度。

### Q4：DAPO 四个改动分别治什么病？（简述机制）
**答**：Clip-Higher 治熵坍缩（负优势锤死后低概率 token 无法翻身→上下界解耦 0.2/0.28）；动态采样治零优势退化组浪费 batch 与算力（过滤全对/全错组）；Token-Level Loss 治长回答被 $1/\mathrm{len}(y_i)$ 降权的长度偏置（分母改 batch 总 token 数）；Overlong Reward Shaping 治截断误判引入的奖励噪声（超长区间线性软化惩罚）。共同点：都是围绕「GRPO 能跑但跑不稳、跑不快」的工程病灶。

### Q5：PPO / GRPO / REINFORCE++ 三者在「baseline 怎么做」上的谱系？
**答**：PPO 学一个参数化 baseline $V_\phi(s_t)$（逐状态、信息最细、成本一个模型）；GRPO 用组内均值当 baseline（无参数、消难度、需 $G$ 倍采样）；REINFORCE++ 用全局 batch 均值（无参数、零额外采样、但不同 prompt 混在一起噪声大，靠 whitening 补救）。三者共享同一 clip/trim 限幅。可加一句：RLOO 的留一均值在统计上比组均值更「干净」（baseline 与自身样本独立，避免自相关偏差）。

### Q6：RLOO 和 GRPO 的数学关系？为什么一个不除 std、一个除？
**答**：两者都是「同 prompt 多采样 + 组内相对」家族的。留一优势 $\hat A_i = r_i - \frac{1}{k-1}\sum_{j\ne i} r_j$ 与 GRPO 的 $r_i - \bar r$ 只差一个 $O(1/k)$ 项（$\bar r$ 含自身）。关键差异在缩放：GRPO 除组 std 带来难度偏差（见 Q2），RLOO 根本不除，天然无此偏置，但也没做尺度归一——奖励量纲不稳时两者都会受影响。RLOO 论文视角：GRPO 的标准化并非成功要素，去掉 critic 才是。

### Q7：CISPO 与 PPO/GRPO 的裁剪有什么本质区别？什么时候该选它？
**答**：PPO/GRPO 裁剪「更新幅度」（比率进入 min/clip 组合，越界 token 梯度直接为零）；CISPO 裁剪「重要性权重本身」并 stop-gradient：$w'=\mathrm{clip}(w,\cdot,\cdot)$ 后作为常数乘在 $\nabla_\theta\log\pi_\theta$ 上——每个 token 都有梯度，只是被压住上限，可用更紧/更松的常数边界保护关键但稀有的推理 token。适合「少数 token 承载主要信息」的长链推理、以及需要全 token 探索的场景；代价是所有越界 token 仍参与计算（省不了算力），且完全失去「不更新」这一层硬保护，依赖权重上界设置合理。

### Q8：为什么 MoE 模型做 RL 特别不稳？GSPO 是怎么缓解的？
**答**：MoE 的专家路由对数值扰动敏感，训练与推理引擎的 logits 精度差异会让同一 token 前后激活不同专家，token 级概率剧烈抖动 → token 级比率的病态噪声被成倍放大。传统方案 Routing Replay：缓存采样时的路由决策并在训练时强制重放，稳但费显存费通信且限制专家容量利用。GSPO 只看序列整体似然（几何平均对单 token 路由翻转不敏感），无需重放路由即可稳定训练，且天然允许 RL 阶段自由重分配专家。

### Q9：长链推理 RL 训练中出现「熵持续下降、回答越来越模板化、AIME 不再涨」，按算法演进给出的线索怎么排查和处置？
**答**：这是 GRPO 系已知病灶，排查顺序：(1) 看被 clip token 中负优势占比与比率分布——大量比率贴在下界 ⇒ 上界过紧，改 Clip-Higher（$\varepsilon_{\text{high}}$ 0.28）或换 CISPO 保梯度；(2) 看退化组占比 ⇒ 动态采样；(3) 看长度曲线非单调崩 ⇒ 检查是否 GRPO 式除以回答长度的归一化，切 token-level 或 Dr.GRPO 常数；(4) 熵仍救不回 ⇒ 引入探索 bonus 或退到 PPO（critic 优势更细，有时能重新分配信用）。体现「诊断即维度遍历」：优势、粒度、归一化、限幅四个旋钮各查一遍。

### Q10（进阶）：如果让你从零为「MoE 长链数学推理」选 RL 算法，给出方案和理由
**答**：推荐 **GSPO（或 SAPO）+ 可验证奖励 + 动态采样**，不保留 KL 正则（RLVR 场景）、FP32 logits。理由链：MoE ⇒ 规避 GRPO token 级比率（路由抖动），序列级粒度免 Routing Replay；结果可验证 ⇒ 免 RM 免 critic，组内 baseline 足够；数学题 ⇒ 动态采样挤掉零优势组；长链 ⇒ 用 DAPO 式 token-level loss 或 GSPO 序列平均避免长回答被降权。反面论证：纯 PPO 在此场景要训一个会随长度爆炸的 critic 且显存翻倍，GRPO 直跑会崩，DPO 系离线方法无法探索新推理模式——把「场景 → 旋钮」逐项映射即满分答案。

### Q11（延伸）：DPO 这类离线方法与上述在线 RL 算法的根本分野在哪？
**答**：上述算法全都在做「在线采样 + 事后打分 + 策略更新」的闭环，具备探索能力（模型能发现自己没试过的更好推理路径）；DPO 把 RLHF 的目标 $\max \mathbb{E}[r]-\beta\,\mathrm{KL}$ 代入 Bradley-Terry 闭式解，变成在**固定偏好对**上的分类损失——不生成、不打分、无探索，效果上限被数据分布钉死。RLVR 时代 DPO 式方法难以催生「Aha moment」正是缺探索所致；折中路线是 STaR/拒绝采样式「迭代 DPO」：定期用当前模型采样、判分、构造新偏好对再离线训练。

## 参考资料
- Schulman et al., *Proximal Policy Optimization Algorithms*, 2017 (arXiv:1707.06347)
- Shao et al., *DeepSeekMath: Pushing the Limits of Mathematical Reasoning* (GRPO), 2024 (arXiv:2402.03300)
- Ahmadian et al., *Zero Training Examples Needed: Reinforcement Learning for Reasoning in Language Models* (RLOO), 2024 (arXiv:2402.10265)
- Hu et al., *REINFORCE++: A Simple and Efficient Approach for Aligning Large Language Models*, 2025 (arXiv:2501.03269)
- Yu et al., *DAPO: An Open-Source LLM Reinforcement Learning System at Scale*, 2025 (arXiv:2503.14476)
- Liu et al., *Understanding R1-Zero-Like Training: A Critical Perspective* (Dr. GRPO), 2025 (arXiv:2503.20783)
- Zheng et al., *Group Sequence Policy Optimization* (GSPO), 2025 (arXiv:2507.18071)
- MiniMax et al., *MiniMax-M1: Scaling Test-Time Compute Efficiently with Lightning Attention* (CISPO), 2025 (arXiv:2506.13585)
- Qwen Team, *Soft Adaptive Policy Optimization* (SAPO), 2025 (arXiv:2511.20347)
