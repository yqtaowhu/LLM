# RAG：检索增强生成（Retrieval-Augmented Generation）

## 目录
- [1.概述](#1概述)
- [2.核心原理](#2核心原理)
- [3.重要性质](#3重要性质)
- [4.与其他概念的关系](#4与其他概念的关系)
- [5.具体例子](#5具体例子)
- [6.常见面试问题](#6常见面试问题)
- [参考资料](#参考资料)

## 1.概述

**一句话定义**：RAG 是在生成之前先从外部知识库检索相关内容的架构——模型"开卷考试"而不是"闭卷背诵"，用检索结果的准确性对冲参数化知识（训练数据）的过时与幻觉。

**为什么重要**：
- LLM 的知识冻结在训练截止日，且私有数据模型从未见过——RAG 是**不改权重注入新知识**的标准解法；
- 检索到的证据可以直接附在答案后面（引用溯源），这是金融、医疗、法务等场景的硬需求；
- 知识更新只需更新索引，成本是小时级；对比微调的天级迭代与重新训练，RAG 是绝大多数知识密集型应用的第一选择。

## 2.核心原理

### 2.1 RAG 的基本流程：离线索引 + 在线生成

```
离线索引（一次性 / 定时更新）：
  文档加载 → 切块（Chunking）→ Embedding 编码 → 存入向量库
                          （附元数据：来源、标题、时间）

在线服务（每次请求）：
  用户 Query → Query 改写/扩展 → 混合检索（向量 + BM25）
      → 重排（Rerank）→ 取 Top-k 拼进 Prompt → LLM 生成 → 引用溯源
```

两个阶段的责任划分：**检索阶段负责"找对材料"，生成阶段负责"说对话"**。线上效果差时必须先分层归因——是检索没召回，还是召回了但没排到前面，还是模型没用好材料。

### 2.2 Embedding：把语义变成向量

Embedding 模型（通常是基于 Transformer 的双塔结构）把文本映射为稠密向量，使**语义相近的文本在向量空间中靠近**。相似度常用余弦相似度：

$$\text{sim}(q, d) = \frac{q \cdot d}{\|q\| \, \|d\|}$$

其中 $q$、$d$ 分别是查询与文档的向量。向量归一化后，余弦相似度等价于点积，工程上普遍这样简化。

**双塔 vs 交叉编码器**（这是检索与重排分层的根源）：

| 结构 | 做法 | 复杂度 | 质量 |
|---|---|---|---|
| 双塔（Bi-Encoder） | Query 与文档**各自独立**编码，向量相似度匹配 | 文档向量可离线预计算，在线只需算一次 query | 中，两塔互不可见，无法精细交互 |
| 交叉编码器（Cross-Encoder） | Query 与文档**拼接后**一起过 Transformer，输出相关分 | 每对都要前向一次，无法预计算 | 高，token 级交叉注意力 |

结论：**召回用双塔（快、可 ANN 加速），重排用交叉编码器（准、只算几十个候选）**。工程上向量检索用 HNSW/IVFPQ 等 ANN 索引加速（详见 [Search/7.2.HNSW.md](../Search/7.2.HNSW.md) 与 [Search/7.1.IVFPQ.md](../Search/7.1.IVFPQ.md)）。

### 2.3 切块（Chunking）：RAG 的第一道质量闸门

文档切块粒度直接决定检索质量：
- **固定长度滑窗 + 重叠（overlap）**：如 512 token、重叠 64，简单通用；重叠防止答案正好被切在边界上；
- **语义切块**：按句/段 embedding 相似度断点，保持语义完整；
- **父子切块（Parent-Child）**：用小块做检索（匹配更准），命中后返回其父块（上下文更全）；
- **结构感知切块**：Markdown 标题、代码块、表格整体不切断。

经验区间：块长 256~1024 token，具体值必须挂评测集扫参，不存在普适最优值。

### 2.4 混合检索与重排（Rerank）

**为什么需要混合检索**：向量检索擅长语义泛化（"退货"能匹配"退款流程"），但对专有名词、型号、缩写不敏感；BM25 等稀疏检索正好相反。两者互补，融合常用 **RRF（Reciprocal Rank Fusion）**：

$$\text{RRF}(d) = \sum_{i=1}^{R} \frac{1}{k + \text{rank}_i(d)}$$

其中 $R$ 是检索路数，$\text{rank}_i(d)$ 是文档 $d$ 在第 $i$ 路结果中的名次（未出现则不计），$k$ 通常取 60 用于平滑头部名次的差距。RRF 只用名次不用分数，天然规避了两路分数量纲不可比的问题。

**重排的典型管线**：

```
混合召回 Top-50（快，宽召回） → Cross-Encoder 打分 → 取 Top-5 进 Prompt
```

重排器把"宽召回"和"精排"解耦：召回阶段宁可多召回（保住召回率），重排阶段用更贵但更准的模型压缩到 few 个高质量上下文（提升精确率，同时省 token、缓解 lost in the middle）。

### 2.5 Query 侧优化

用户原始 query 常常不适合直接检索，常见手段：
- **Query 改写**：把口语化、有指代的问句补全为独立检索句；
- **Multi-Query**：让 LLM 把一个问题改写成多个不同角度的查询，结果合并去重；
- **HyDE**：先让 LLM 生成一个"假设答案"，用假设答案的向量去检索——因为"答案与答案更相似"，可显著缓解 query 与文档的语义鸿沟；
- **Step-Back Prompting**：先抽象出上层问题（"爱因斯坦何时获诺奖" → "爱因斯坦的生平成就"），用抽象问题扩大召回面。

### 2.6 RAG 的评测：RAGAS 三层指标

| 层 | 指标 | 衡量什么 |
|---|---|---|
| 检索层 | Context Recall | 该召回的证据召回了几成 |
| 检索层 | Context Precision | 排在前面的上下文有多少是真正相关的 |
| 生成层 | Faithfulness（忠实度） | 答案是否忠于检索材料，不编造 |
| 生成层 | Answer Relevance | 答案是否切题 |

这套分层归因思想与提示词工程文档中"先评检索命中率，再评生成忠实度"（见 [LLM/5.提示词工程.md](5.提示词工程.md)）一脉相承。

## 3.重要性质

**性质 1：RAG 的上限由检索决定**
直觉：生成模型再强，正确证据不在上下文里就只能靠参数记忆"猜"。经验上把 Context Recall 从 70% 提到 90%，端到端准确率的提升远大于换一个更大的生成模型。$\boxed{\text{RAG 调优应先调检索，再调生成}}$

**性质 2：检索与生成的语义鸿沟是固有矛盾**
直觉：query 是问句，文档是陈述句/知识点，两者分布不同（"问题与答案不像，答案与答案才像"）。HyDE、query 改写都是在填这条鸿沟。

**性质 3：Top-k 不是越大越好**
直觉：k 增大先提升（召回更全）后下降（噪声淹没证据 + lost in the middle + token 成本上升），存在拐点；配合重排器后，往往能用更小的 k 得到更好效果。

**性质 4：RAG 缓解幻觉而非消除幻觉**
直觉：材料在上下文里不等于模型一定照着说——模型仍可能忽略或曲解材料。需要靠提示约束（"仅依据材料回答，没有就说不知道"）、忠实度评测和引用校验兜底。

**性质 5：索引新鲜度是 RAG 的工程命门**
直觉：知识以"索引更新延迟"为单位过期，因此增量索引、变更检测、失效删除是生产系统的一等公民，而不是"把文档全量重灌一遍"。

## 4.与其他概念的关系

| 维度 | RAG | 微调（SFT） | 长上下文（直接塞全文） | 纯提示词工程 |
|---|---|---|---|---|
| 是否改权重 | 否 | 是 | 否 | 否 |
| 注入新知识 | 强（实时检索） | 强（内化，但更新需重训） | 强（受窗口限制） | 弱（靠人工塞） |
| 知识更新成本 | 小时级（更新索引） | 天级 | 分钟级（换输入） | 分钟级 |
| 上下文成本 | 低（只塞 Top-k） | 无额外上下文 | 高（全文进 prompt） | 视任务 |
| 可溯源 | 有引用 | 无 | 有 | 无 |
| 擅长 | 知识密集型问答 | 稳定的格式/风格/领域行为 | 单篇长文档深度问答 | 任务格式与行为约束 |

**与提示词工程的关系**：RAG 负责给对"知识"，prompt 负责定义"行为"——如何引用、如何处理材料冲突、材料中没有时怎么办、输出什么格式。两者都在推理期生效、都可分钟级迭代，是天然搭档（组合方式详见 [LLM/5.提示词工程.md](5.提示词工程.md) Q9）。

**与微调的关系**：不是二选一。决策信号：知识**频繁变化或需要溯源** → RAG；行为/风格/格式需要**稳定内化** → SFT。生产中常见组合是 "SFT 定行为 + RAG 供知识"：先微调让模型学会"依据材料作答"的通用技能，再由 RAG 在运行时供给具体知识。判断口诀：$\boxed{\text{知识用 RAG，行为用微调}}$

**演进方向**：
- **Naive RAG → Advanced RAG**：加 query 改写、混合检索、重排；
- **Modular / Agentic RAG**：检索成为 Agent 可调用的工具，由模型决定"检索什么、检索几轮、是否足够"，支持多跳推理；
- **GraphRAG**：把知识组织成图（实体 + 关系），擅长"全局性、多实体关联"的问题，弥补向量检索只擅长局部相似匹配的短板。

## 5.具体例子

### 例子 1：Embedding 余弦相似度——手算检索排序

**问题**：知识库中有 3 个文档块，用户问"如何申请退货"，哪个最应该被召回？

**步骤**：
1. Embedding 模型把 query 与各文档编码为归一化向量；
2. 为便于手算，取 2 维示意（真实是 768~3072 维）：query $q = [0.8, 0.6]$；
3. 文档：$d_1$（退货流程）$= [0.6, 0.8]$，$d_2$（退款政策）$= [1.0, 0]$，$d_3$（换货说明）$= [0, 1]$；
4. 逐个计算余弦相似度并排序。

**计算**（向量已归一化，$\|q\| = \|d\| = 1$，点积即余弦）：

$$\text{sim}(q, d_1) = 0.8 \times 0.6 + 0.6 \times 0.8 = 0.48 + 0.48 = 0.96$$
$$\text{sim}(q, d_2) = 0.8 \times 1.0 + 0.6 \times 0 = 0.80$$
$$\text{sim}(q, d_3) = 0.8 \times 0 + 0.6 \times 1.0 = 0.60$$

排序：$d_1 (0.96) > d_2 (0.80) > d_3 (0.60)$。

**结果解读**：退货流程文档最相关，被 Top-1 召回。注意 $d_2$（退款）与 $d_3$（换货）的差距只有 0.2——真实系统中相关候选往往挤在一起，这正是需要**重排**做精细区分的原因。此外，若 query 是"我买的东西不想要了怎么办"，其向量与 $d_1$ 的相似度会下降（口语与文档术语的语义鸿沟），需要 query 改写来补救。

### 例子 2：RRF 融合两路检索结果——手算公式

**问题**：BM25 与向量检索对同一 query 各返回 Top-4，如何融合成统一排序？

**步骤**：
1. 两路结果：BM25 排序为 A, B, C, D；向量排序为 C, A, E, B；
2. 用 RRF，$k = 60$，对每个文档累加 $\frac{1}{60 + \text{rank}}$；
3. 按 RRF 分数重排，取 Top-3。

**计算**：

| 文档 | BM25 名次 | 向量名次 | RRF 分数 |
|---|---|---|---|
| A | 1 | 2 | $\frac{1}{61} + \frac{1}{62} = 0.01639 + 0.01613 = 0.03252$ |
| C | 3 | 1 | $\frac{1}{63} + \frac{1}{61} = 0.01587 + 0.01639 = 0.03227$ |
| B | 2 | 4 | $\frac{1}{62} + \frac{1}{64} = 0.01613 + 0.01563 = 0.03175$ |
| D | 4 | — | $\frac{1}{64} = 0.01563$ |
| E | — | 3 | $\frac{1}{63} = 0.01587$ |

融合排序：**A > C > B**，取 Top-3：A、C、B。

**结果解读**：A 只在两路各排第 1、2 名，虽无单项第一却总分最高——RRF 奖励的是"**两路都认为它相关**"的文档，这正是融合的意义：BM25 看重字面匹配，向量看重语义，双方一致的候选最可信。分数差异很小（0.03252 vs 0.03227，相对差不足 1%），但排序稳定，因为 RRF 只依赖名次，对两路分数的量纲完全不敏感。

### 例子 3：加重排前后的端到端效果对比

**问题**：企业内部知识库问答（100 条测试问题），Naive RAG（向量 Top-5 直进 prompt）的 Context Recall 只有 72%，且答案常忽略材料细节，如何改进？

**步骤**：
1. 保持召回不动，在检索后加 Cross-Encoder 重排：先宽召回 Top-50，重排取 Top-5；
2. prompt 增加忠实度约束："仅依据以下材料回答，材料中没有则明确说不知道，并给出引用编号"；
3. 用 RAGAS 评测改进前后的四项指标，并统计单次请求延迟。

**计算（改进前后对比）**：

| 指标 | Naive RAG | + 重排 + 忠实度约束 |
|---|---|---|
| Context Recall | 72% | 73%（宽召回弥补，仅 +1pp） |
| Context Precision | 54% | 86%（+32pp） |
| Faithfulness | 0.81 | 0.93（+0.12） |
| 端到端答对率 | 63 / 100 | 81 / 100（+18pp） |
| 单次延迟 | 1.9 s | 2.1 s（+0.2 s） |

**结果解读**：Context Recall 几乎没变（72%→73%，宽召回只小幅补回），真正起作用的是**重排把正确证据从"召回但排在后面"提升到 Top-5**，Context Precision 提升 32 个百分点；配合忠实度约束，端到端答对率提升 18 个百分点，代价仅为约 0.2 秒重排延迟。验证了性质 1：瓶颈在"召回的正确证据有没有被排进上下文"，重排是性价比最高的一步。

## 6.常见面试问题

**Q1：什么是 RAG？它解决 LLM 的哪些问题？**

答：RAG = 检索增强生成，生成前先从外部知识库检索相关内容拼进上下文。解决三类问题：(1) 知识过时——模型知识冻结在训练截止日；(2) 私有数据缺失——企业内部文档模型没见过；(3) 幻觉——有了可引用的证据，答案可溯源、可验证。核心价值：不改权重，以索引更新代替重训。

**Q2：RAG、微调、长上下文如何选型？**

答：知识频繁变化、需要溯源引用 → RAG；输出风格/格式/领域行为需稳定内化 → 微调；单篇长文档的深度问答（如一份合同逐条分析）→ 长上下文直接塞。三者可组合：SFT 定行为 + RAG 供知识 + 长上下文处理整篇文档。记忆口诀：**知识用 RAG，行为用微调**。

**Q3：描述一个完整 RAG 系统的离线与在线流程。**

答：离线：文档加载 → 切块（附来源/时间等元数据）→ Embedding 编码 → 入向量库（建 ANN 索引）。在线：query 改写 → 混合检索（向量 + BM25）→ 重排 → Top-k 拼进 prompt → LLM 生成 → 引用溯源。排障时按"检索召回 → 检索排序 → 生成忠实度"三层归因。

**Q4：双塔模型和交叉编码器的区别？为什么 RAG 两者都用？**

答：双塔把 query 与文档独立编码，相似度靠向量点积/余弦计算，文档向量可离线预计算、可 ANN 加速，复杂度低但两塔无交互、精度有限；交叉编码器把两者拼接后过同一个 Transformer，token 级交叉注意力，精度高但每对都要现算、无法预计算。所以**召回用双塔保速度（宽召回 Top-50），重排用交叉编码器保精度（精选 Top-5）**，各取所长。

**Q5：写出 RRF 公式并解释为什么用它而不是加权求和。**

答：$\text{RRF}(d) = \sum_i \frac{1}{k + \text{rank}_i(d)}$，$k$ 常取 60，对文档在各路检索中的名次倒数求和。不用分数加权求和的原因：BM25 分数与余弦相似度量纲、分布完全不同，直接加权需先做脆弱的分数归一化；RRF 只用名次，天然免归一化、实现简单、对异常分数鲁棒。

**Q6：切块（Chunking）策略有哪些？切得不好会有什么后果？**

答：固定长度滑窗 + overlap、语义切块、父子切块（小块检索、大块返回）、结构感知切块（不切断标题/表格/代码）。切得不好的后果：答案被切在边界 → 检索命中但内容残缺；块太长 → 一个块混杂多个主题，向量语义被稀释，匹配变差且浪费上下文预算；块太短 → 缺少必要上下文，模型无法理解。切块参数必须挂评测集扫参。

**Q7：什么是 HyDE？为什么"用假设答案检索"反而有效？**

答：HyDE 先让 LLM 生成一个假设性答案，再用假设答案的向量去检索。原理：query（问句）与文档（陈述句）存在分布差异，而"答案与答案"都是陈述性的知识点表述，语义空间更接近。即使假设答案有事实错误，其主题词与句式已足够把正确文档拉近。代价是多一次生成调用，增加首 token 延迟。

**Q8：RAG 仍然幻觉怎么办？给出至少四条手段。**

答：(1) 检索层：提高 Context Precision/Recall（混合检索、重排），让证据质量更高；(2) 提示层：加忠实度约束（"仅依据材料回答，没有就说不知道"）；(3) 生成层：要求输出引用编号，对每句答案做引用校验（NLI 模型判答案是否被材料支持）；(4) 评测层：用 Faithfulness 指标持续监控 + bad case 回流评测集。RAG 只能缓解不能消除幻觉，需要多层兜底。

**Q9：如何评测一个 RAG 系统的好坏？**

答：用 RAGAS 等框架做分层评测。检索层：Context Recall（该召回的证据召回了几成，可用人工标注支撑答案的文档块）、Context Precision（排前的上下文相关占比）。生成层：Faithfulness（答案对材料的忠实度）、Answer Relevance（答案切题度）。关键纪律是**分层归因**：端到端答错时先区分"没检索到 / 检索到没排前 / 排前了没答好"，分别对应检索召回、重排、prompt 与模型能力的优化动作。

**Q10：进阶：Agentic RAG 和 GraphRAG 分别解决 Naive RAG 的什么短板？**

答：Naive RAG 是固定管线"检一次、生成一次"，短板是复杂问题（多跳、需要拆解、检一次不够）只能瞎检一次。Agentic RAG 把检索变成 Agent 的工具，由模型自主决定检索词、检索轮数、何时停止（反思式），支持多跳推理。GraphRAG 针对向量检索"只见局部相似、不见全局结构"的短板：抽取实体关系建知识图谱，回答"某领域的主要脉络是什么"这类需要跨文档聚合的问题时，先图检索/社区摘要再生成。两者代价都是延迟与成本显著上升，需按问题复杂度路由。

## 参考资料

- Lewis et al., *Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks*, 2020
- Karpukhin et al., *Dense Passage Retrieval for Open-Domain Question Answering*, 2020
- Gao et al., *Retrieval-Augmented Generation for Large Language Models: A Survey*, 2023
- Gao et al., *Precise Zero-Shot Dense Retrieval without Relevance Labels*（HyDE）, 2022
- Cormack et al., *Reciprocal Rank Fusion Outperforms Condorcet and Individual Rank Learning Methods*, 2009
- Edge et al., *From Local to Global: A Graph RAG Approach to Query-Focused Summarization*, 2024
- Es et al., *RAGAS: Automated Evaluation of Retrieval Augmented Generation*, 2023
- 本仓库关联文档：[LLM/5.提示词工程.md](5.提示词工程.md)、[Search/7.1.IVFPQ.md](../Search/7.1.IVFPQ.md)、[Search/7.2.HNSW.md](../Search/7.2.HNSW.md)
