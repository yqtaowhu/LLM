# MTP 多 Token 预测（Multi-Token Prediction）

## 目录
- [1.概述](#1概述)
- [2.核心原理](#2核心原理)
- [3.重要性质](#3重要性质)
- [4.与其他概念的关系](#4与其他概念的关系)
- [5.具体例子](#5具体例子)
- [6.常见面试问题](#6常见面试问题)
- [参考资料](#参考资料)

## 1.概述

**一句话定义**：MTP（Multi-Token Prediction，多 Token 预测）是让大模型在**一次前向计算中同时预测未来多个位置 token** 的训练目标与架构设计——它一方面作为辅助训练目标**提升模型能力**，另一方面在推理时充当**自投机解码器**加速生成。

**为什么重要**：
- 传统大模型遵循"一次只预测下一个 token"的铁律：训练时每个位置只产生 1 个 bit 级的监督信号，**训练信号稀疏**；推理时逐 token 串行解码，**延迟是硬伤**；
- MTP 一石二鸟：训练时逼迫模型"提前规划"（在当前隐藏状态里多存一点未来信息），显著提升代码、数学等推理型任务的表现；推理时预测出的 token 直接作为草稿参与投机解码，DeepSeek-V3 实测**解码吞吐约 1.8 倍**，第二 token 接受率高达 85%–90%；
- 它是**预训练原生设计**而非后补丁：MTP 模块在预训练阶段就与主干联合训练（DeepSeek-V3 中损失权重 $\lambda = 0.3$），这是它区别于 Medusa 等"事后加头"方案的关键。

时间线速览：Meta 2024 年论文《Better & Faster Large Language Models via Multi-token Prediction》提出共享主干 + 多头并行预测的经典范式；DeepSeek-V3（2024.12）将其改造为**串行级联的 MTP 模块**并融入大规模预训练，使 MTP 成为工业级大模型的标配设计之一。

## 2.核心原理

### 2.1 问题的起点：next-token 训练范式的两大短板

大模型训练目标是标准的下一 token 预测：

$$\mathcal{L}_{\text{main}} = -\sum_{t} \log P(x_{t+1} \mid x_{\le t}; \theta_{\text{main}})$$

其中 $x_{\le t}$ 是位置 $t$ 及之前的 token 序列，$\theta_{\text{main}}$ 是主模型参数。这个目标有两大结构性短板：

1. **训练信号稀疏**：每个位置只能"检验"模型对 1 个未来 token 的判断。同样的数据、同样的算力，模型获得的信息反馈是有限的；
2. **短视（myopic）**：模型被奖励只"看一步"，没有动力在隐藏状态里为更远的未来做规划——但人类写代码、做数学题时，恰恰是先想好几步再落笔的。

MTP 的直觉：**把"只看一步"的训练目标改成"同时看 $n$ 步"，迫使模型未雨绸缪。** 这就像学生做题：只对标准答案得 1 分，和要提前写出"接下来 3 步的解题计划"并对每步负责，后者的练习质量显然更高。

### 2.2 Meta 方案：共享主干 + n 个并行预测头

Meta 论文的经典架构：**一个共享的 Transformer 主干，顶部挂 $n$ 个预测头，每个头负责预测未来第 $i$ 个 token，输出层（unembedding 矩阵）共享**。

**结构**（以 $n=4$ 为例）：

```
                 ┌→ [Transformer Block 1] → LM Head（共享）→ 预测 x_{t+2}
主Transformer → h_t ─┼→ [Transformer Block 2] → LM Head（共享）→ 预测 x_{t+3}
（共享主干）         ├→ [Transformer Block 3] → LM Head（共享）→ 预测 x_{t+4}
                 └→ [Transformer Block 4] → LM Head（共享）→ 预测 x_{t+5}
                 （h_t 同时输入下一 token 头，即标准 next-token 目标）
```

**损失函数**：对每个偏移 $i$（$1 \le i \le n$），由第 $i$ 个头基于隐藏状态 $h_t$ 预测 $x_{t+i+1}$，损失为各头交叉熵的平均：

$$\mathcal{L}_{\text{MTP}} = \frac{1}{n} \sum_{i=1}^{n} \mathcal{L}_{\text{CE}}\big(\text{head}_i(h_t),\; x_{t+i+1}\big)$$

其中 $\mathcal{L}_{\text{CE}}$ 是词表上的交叉熵。总损失将 next-token 损失与 MTP 损失相加，两者同时反传更新主干。

**关键实验结论**（Meta 论文）：
- $n$ 个头**并行、各管各的偏移**，互不级联；
- 13B 模型配 4 个头时收益最大：HumanEval 提升 12 个百分点、MBPP 提升 17 个百分点；对摘要类确定性低的任务提升有限；
- 用这种模型做**自投机解码**（预测头出的 4 个候选 token 先后自检），HumanEval 上可获得约 **3 倍解码加速**。

### 2.3 DeepSeek-V3 方案：串行级联的 MTP 模块

Meta 的多头并行有一个隐患：**每个头只拿主干在 $t$ 时刻的隐藏状态 $h_t$，"各猜各的"，头与头之间没有因果传递**——预测 $x_{t+2}$ 的头并不知道"自己其实赌错了 $x_{t+1}$"。DeepSeek-V3 对此做了两个关键改造：

1. **串行级联（Cascading）**：每个 MTP 模块的输入 = 上一个模块的输出隐藏状态 + **下一个真实 token 的词向量**，保持因果链严谨；
2. **原生预训练**：MTP 模块不是训完再补，而是与主干一起从头预训练。

**模块结构**（深度 $K=1$，即额外预测 1 个 token，以位置 $i$ 为例）：

$$h_i'^{0} = \text{RMSNorm}\left(h_i^{\text{main}}\right)$$

$$h_i'^{1} = M_1\Big(\underbrace{W_1^T\big[\text{RMSNorm}(h_i'^{0});\; \text{RMSNorm}(\text{Emb}(t_{i+1}))\big]}_{\text{拼接主干隐状态与下一 token 词向量}}\Big)$$

其中：
- $h_i^{\text{main}}$：主模型在位置 $i$ 的输出隐藏状态；
- $\text{Emb}(t_{i+1})$：**真实**的下一 token（训练时）经过**共享 Embedding** 得到的词向量；
- $[\cdot\,;\cdot]$：向量拼接；$W_1$：把拼接结果投影回模型维度的矩阵；
- $M_1$：MTP 模块内部一个完整的 Transformer Block（自带独立的 RMSNorm，是"多想一步"的计算空间，比 Medusa 的 MLP 头深得多）；
- 输出 $h_i'^{1}$ 复用主模型的 **LM Head**（共享输出矩阵）预测 $t_{i+2}$。

**多个深度时**：第 $k$ 个模块以上一模块的输出 $h_i'^{k-1}$ 和真实 token $t_{i+k}$ 的词向量为输入，预测 $t_{i+k+1}$，形成一条"链"。DeepSeek-V3 实际取 $K=1$。

**总损失**（DeepSeek-V3，$\lambda_1 = 0.3$）：

$$\mathcal{L}_{\text{total}} = \mathcal{L}_{\text{main}} + \lambda_1 \sum_{k=1}^{K} \mathcal{D}_k$$

其中 $\mathcal{D}_k$ 是第 $k$ 个模块在所有位置上的交叉熵，取平均。$\lambda_1 = 0.3$ 说明 MTP 是**辅助目标**：主任务优先，MTP 提供额外监督信号。

$$\boxed{\text{Meta 多头：并行独立、各猜各的} \quad \text{vs} \quad \text{DeepSeek 模块：串行级联、保持因果}}$$

### 2.4 推理时怎么用：自投机解码

MTP 模块在推理时有两种用法：

1. **直接丢弃**：只保留主模型，效果与不训练 MTP 的模型几乎一致（验证了 MTP 主要通过"训练信号更密"提升主模型本身）；
2. **充当自投机解码的 draft（草稿模型）**——这才是加速的关键：
   - 主模型前向一步，输出 $x_{t+1}$，同时 MTP 模块顺手给出草稿 $x_{t+2}$；
   - 把 $x_{t+1}$ 喂回主模型再前向一步，**顺带验证** $x_{t+2}$ 是否与主模型本会输出的 token 一致（拒绝采样/回退验证，保证输出分布与标准自回归完全一致）；
   - 接受则一次吐出 2 个 token，拒绝则回退——**任何情况下正确性无损，只是平均每步产出 token 数不同**。

工程数据（DeepSeek-V3）：第二 token 接受率 85%–90%，解码速度约 1.8 倍；vLLM/SGLang 等推理框架已原生支持（`speculative_config: {"method": "mtp"}`）。

## 3.重要性质

**1. 训练信号密度提升**
公式上，每位置监督从 1 项变为 $1 + K$ 项交叉熵；直觉上，同一份数据，模型被迫对"未来 $K$ 步"都给出预测并接受检验，数据利用效率显著提高。Meta 实验显示同等数据量下 MTP 模型性能更好，等价于"变相增加训练数据"。

**2. 迫使模型提前规划（防短视）**
要预测 $x_{t+2}$，模型必须在 $h_t$ 中编码"如果我选了 $x_{t+1}$，再下一步该是什么"的信息——隐藏状态被迫携带**多步规划信息**。这对需要多步推理的任务（代码续写、数学推导）收益最大，对不确定性大的任务（摘要）收益小：因为远处 token 本身随机性高，监督信号噪声大。

**3. 因果链保持（DeepSeek 方案独有）**
每个模块的输入包含真实下一 token 的词向量，预测的是"给定正确历史下的 $k$ 步之后"——数学上仍是严格的条件概率 $\log P(x_{i+k+1} \mid x_{\le i+k})$ 的近似训练，不会引入错误历史的偏差（训练时永远用 ground truth，与教师强制一致）。

**4. 参数开销极小（极致复用）**
- 共享 Embedding 矩阵（词表 $\times$ $d$ 的大头参数，往往上 GB）；
- 共享 LM Head（又一个词表 $\times$ $d$）；
- 每个 MTP 模块只增加 1 层 Transformer + 投影矩阵。$K=1$ 时额外参数量约为主模型的 $\frac{1}{\text{层数}}$ 量级，几乎可以忽略。

**5. 可插拔性：训练有用、推理可丢**
MTP 模块是"旁路"：它通过梯度影响主干质量，但主模型的推理路径完全不依赖它。这带来工程灵活性——部署时可按显存/延迟预算选择"丢弃 MTP"或"启用 MTP 投机解码"，且**输出分布与标准自回归严格一致，无损**。

**6. 头数/深度存在边际收益递减**
Meta 论文观察到 $n=4$ 对 13B 模型效果最佳，更大 $n$ 收益趋平；远处 token 的熵大，监督信号噪声占比上升。DeepSeek-V3 干脆只取 $K=1$——用最小代价同时拿到"训练增益 + 推理加速"两项收益。

## 4.与其他概念的关系

**与 Medusa 的关系**：同为"多 token 输出头"，但路线不同——

| 维度 | Meta MTP | DeepSeek-V3 MTP | Medusa |
|---|---|---|---|
| 头的形态 | 每头 1 层 Transformer，并行 | 每 $K$ 模块 1 层 Transformer，串行级联 | 轻量 MLP 头，并行 |
| 训练时机 | 预训练原生联合训练 | 预训练原生联合训练（$\lambda{=}0.3$） | 事后 SFT 补丁 |
| 因果传递 | 无（各头独立） | 有（真实 token 词向量逐级传入） | 无（共享最后隐状态） |
| 推理角色 | 自投机 draft | 自投机 draft | 树形注意力 + 多候选 |

**与投机解码（speculative decoding）的关系**：经典投机解码需要一个**独立训练的小 draft 模型**（如 0.5B 的兄弟模型）来起草稿，存在 draft 与 target 分布不匹配、维护两套模型的工程负担；MTP 让大模型**自带草稿模块**——draft 与 target 深度共享表示，接受率天然更高（85%+ vs 小模型通常 60%–75%），且训练一次、两处受益。

**与解码策略（greedy/top-p/beam）的关系**：正交的两个维度。解码策略回答"**拿到分布后选哪个 token**"，作用在主模型输出端；MTP 回答"**训练目标和架构怎么让模型多想几步 + 一次算出多步草稿**"。MTP 模型完全可以再叠 top-p 等采样策略；而 beam search 的"多路"是搜索空间扩展，不改变每步前向只产生 1 个 token 的根本串行性。

**与多字节预测（multi-byte prediction）的关系**：MTP 在 BPE token 粒度上预测；2026 年最新的多字节预测工作把预测粒度下沉到 byte 级潜在 token 段，一次性预测整段字节，可视为 MTP 思想在字节级语言模型上的延续，解决了"头数随预测长度线性增长"的参数膨胀问题。

## 5.具体例子

### 例子 1：手算 MTP 训练损失（$n=2$ 的迷你模型）

**问题**：一个迷你模型，词表只有 4 个词：`我、爱、北京、天安门`。训练句为"我爱北京天安门"，主干在位置 1（看过"我"之后）给出隐藏状态 $h_1$，要求同时用 next-token 头和 1 个 MTP 头（Meta 风格）训练。各头在 $h_1$ 下经 softmax 得到的概率如下，计算 MTP 平均损失与纯 next-token 损失的差异。

**步骤**：
1. 真实标签：位置 1 的 next-token 是"爱"，未来第 2 个 token（偏移 2）是"北京"；
2. next-token 头输出：$P(\text{爱} \mid \text{我}) = 0.4$，其余 3 词合计 0.6；
3. MTP 头输出：$P(\text{北京} \mid \text{我，偏移}2) = 0.2$，其余合计 0.8；
4. 交叉熵 = $-\log P(\text{正确词})$。

**计算**：

$$\mathcal{L}_{\text{next}} = -\log(0.4) = 0.916$$

$$\mathcal{L}_{\text{MTP}} = \frac{1}{1}\left(-\log(0.2)\right) = 1.609$$

$$\mathcal{L}_{\text{total}} = \mathcal{L}_{\text{next}} + \mathcal{L}_{\text{MTP}} = 0.916 + 1.609 = 2.525$$

**结果解读**：MTP 项让总损失从 0.916 涨到 2.525——梯度不仅流向"预测'爱'"的路径，还同时流向"在 $h_1$ 里预存'北京'信息"的路径。反传后，主干在 $h_1$ 中被迫同时编码两个未来 token 的信息；训练越久，$P(\text{北京})$ 越接近 1，MTP 项损失趋近 0。**这 1.609 的额外监督，就是"一份参数、两份学费"的来源。**

### 例子 2：MTP 投机解码的加速收益估算

**问题**：一个 70B 主模型 + $K{=}1$ 的 MTP 模块。单次主模型前向（decode 阶段，batch 小、访存受限）耗时 40ms，MTP 模块单层 Transformer 前向仅 5ms，接受率 $\alpha = 0.85$。对比标准自回归与 MTP 自投机的每 token 平均耗时。

**步骤**：
1. 标准自回归：每产出 1 个 token 需要 1 次主模型前向，40ms/token；
2. MTP 自投机：每轮 = 主模型前向（得 $x_{t+1}$ 和草稿 $x_{t+2}$）+ MTP 模块前向 + 下一次主模型前向时顺带验证；
3. 若草稿被接受（概率 $\alpha$）：一轮两次主模型前向 + 1 次 MTP 前向，产出 2 个 token；
4. 若被拒绝（概率 $1-\alpha$）：产出 1 个 token，仍花 1 次主模型前向 + 1 次无谓的 MTP 前向。

**计算**：

- 接受路径耗时：$40 + 5 + 40 = 85\text{ms}$，产 2 token，即 $42.5\text{ms/token}$；
- 拒绝路径耗时：$40 + 5 = 45\text{ms}$，产 1 token，即 $45\text{ms/token}$；
- 期望每 token 耗时：

$$\alpha \times 42.5 + (1-\alpha) \times 45 = 0.85 \times 42.5 + 0.15 \times 45 = 36.1 + 6.75 = 42.9\text{ms}$$

- 加速比：

$$\frac{40}{42.9} \approx 0.93 \text{（本简化估算）}$$

**结果解读**：这个 toy 计算显示单深度 MTP 的净收益取决于 MTP 模块开销与接受率的平衡——真实系统（DeepSeek-V3，接受率 85%–90%，配合 batch 内多请求并行验证）实测约 **1.8 倍**。工程要点：MTP 模块必须足够轻（这里是 5ms vs 40ms），且验证零额外成本（验证发生在主模型本就要做的下一次前向里）；若 MTP 模块变重或接受率下降，收益会被侵蚀，这解释了为什么 DeepSeek-V3 只设 $K{=}1$ 而非堆叠多深度。

## 6.常见面试问题

**Q1：什么是 MTP？它解决 next-token 训练范式的什么问题？**

答：MTP（Multi-Token Prediction）让模型在每一步同时预测未来多个位置的 token。它解决两个问题：(1) **训练信号稀疏**——每个位置只监督 1 个 token，数据利用效率低，MTP 把每位置监督从 1 项变成 $1+K$ 项交叉熵；(2) **模型短视**——只奖励看一步，MTP 迫使隐藏状态编码多步规划信息。关键公式：$\mathcal{L} = \mathcal{L}_{\text{main}} + \lambda \sum_k \mathcal{D}_k$。直觉：让学生不仅对答案，还要对"后三步的计划"负责。

**Q2：Meta 的 MTP 和 DeepSeek-V3 的 MTP 架构上有什么本质区别？**

答：Meta 是**并行多头**：$n$ 个头各自独立地从主干隐藏状态 $h_t$ 预测 $x_{t+i+1}$，头与头之间无信息传递，"各猜各的"。DeepSeek-V3 是**串行级联**：第 $k$ 个模块的输入是上一模块输出（经 RMSNorm）与真实 token $t_{i+k}$ 词向量（经共享 Embedding）的拼接投影，保持因果链——每个头"知道"自己前一步的真实情况。后者推理时可直接当自投机草稿器，且因保留因果性而训练更严谨。

**Q3：写出 DeepSeek-V3 的 MTP 输入构造和总损失，$\lambda_1$ 为什么取 0.3？**

答：输入构造：$h_i'^{1} = M_1\big(W_1^T[\text{RMSNorm}(h_i'^{0}); \text{RMSNorm}(\text{Emb}(t_{i+1}))]\big)$，$h_i'^{0}=\text{RMSNorm}(h_i^{\text{main}})$，输出经共享 LM Head 预测 $t_{i+2}$。总损失 $\mathcal{L} = \mathcal{L}_{\text{main}} + 0.3 \times \mathcal{L}_{\text{MTP}}$。$\lambda_1 = 0.3 < 1$ 是刻意的：MTP 是**辅助目标**，若权重过大，模型会过度容量分配给"远处猜测"（那里噪声大），反而挤占 next-token 主任务；0.3 让主任务始终占主导，MTP 只提供正则化的额外监督。直觉：副业要给主业让路。

**Q4：MTP 为什么能同时提升训练质量和推理速度？这两件事有关联吗？**

答：有深层关联。**训练侧**：多目标监督逼主干在 $h_t$ 中编码未来多步信息，主干本身变强（Meta：13B 模型 HumanEval +12pp）；**推理侧**：编码了未来信息的隐藏状态，经一层轻量 Transformer 就能以 85%+ 准确率给出下一 token 草稿——如果主干没被这样训练过，同样的轻量头接受率会低得多。所以 MTP 不是"训练技巧"和"加速技巧"的简单并列，而是**同一份"提前规划"能力的两个变现出口**：变现为主模型质量（丢弃模块）和变现为解码加速（启用模块）。

**Q5：MTP 模块在推理时可以丢掉吗？为什么训练有用推理却可丢弃？**

答：可以。MTP 模块是旁路结构：主模型前向 $x_{\le t} \to h_t \to \text{LM Head} \to x_{t+1}$ 完全不经过 MTP 模块，两者只在训练时通过梯度耦合。它对主模型的作用发生在**训练期间**（更密集的监督信号塑造了更好的参数），训练结束后模块本身不再必要。工程价值：部署时按预算自由选择"丢弃省显存"或"启用做投机解码"，且两种模式输出分布一致、质量无损。

**Q6：MTP 充当自投机解码时，验证环节怎么做？会不会改变输出分布？**

答：验证采用**拒绝采样/回退验证**：主模型在某次前向中除输出 $x_{t+1}$ 外还得到完整分布 $p(\cdot \mid x_{\le t+1})$，用它检查草稿 $x_{t+2}$——若草稿 token 在主模型分布下足够"合理"（贪心场景下等于 argmax 一致；采样场景下按拒绝采样准则），则接受，否则回退用主模型自己的采样结果。**输出分布与纯自回归严格一致，数学上无损**。这与经典投机解码的验证机制完全相同，区别只在草稿来自 MTP 模块而非独立小模型，也因此省掉了独立 draft 模型的前向开销和分布不匹配问题。

**Q7：MTP 对哪些任务收益最大？对哪些任务收益有限？为什么？**

答：收益最大的是**代码生成和数学推理**（Meta 论文 HumanEval +12pp、MBPP +17pp）：这类任务 token 级不确定度低、结构性强的局部（变量名、语法模板、代数步骤）可以被准确预测多步，MTP 的远距离监督信噪比高，且"先规划再落笔"恰好契合任务本质。收益有限的是**摘要、开放创作**：远处 token 本身熵很高（同样合理的续写有很多种），逼模型预测高熵 token 等于在噪声上训练，监督信号弱且可能引入过拟合式的"错误自信"。判据：**未来 token 的条件熵越低，MTP 的边际收益越大**。

**Q8：对比 MTP、Medusa、EAGLE 这三类投机解码草稿方案。**

答：(1) **独立小模型**（经典 speculative decoding）：draft 与 target 分布不匹配，接受率较低，且要维护两套模型；(2) **Medusa**：在最后隐藏状态上加多个并行 MLP 头，事后 SFT 训练，配合树形注意力扩展多候选——浅层 MLP 思考能力有限，且"各猜各的"无因果传递；(3) **MTP（DeepSeek 风格）**：预训练原生联合训练 + 每模块独立完整 Transformer 层（思考更深）+ 串行级联（因果严谨）+ 共享 Embedding/LM Head（参数省）——三重优势使其第二 token 接受率高达 85%–90%，EAGLE 走的也是"复用目标模型特征 + 轻量自回归头"的类似思路。选择上：预训练时就规划加速 → MTP；只能对已有模型做后处理 → Medusa/EAGLE。

**Q9：为什么 DeepSeek-V3 只用 $K{=}1$（深度 1）而不是堆更多 MTP 模块？头数/深度怎么权衡？**

答：三个原因：(1) **边际收益递减**——预测越远，token 条件熵越高，监督信号噪声占比越大，Meta 实验显示 $n=4$ 后收益趋平；(2) **推理开销**——深度 $K$ 的自投机一轮需要 $K+1$ 次前向，深处草稿的接受率会随链式误差累积而下降，期望收益可能不抵开销；(3) **工程简洁性**——$K{=}1$ 已同时覆盖两大收益（训练增益 + 约 1.8 倍解码加速），额外模块带来的参数、显存和调度复杂度不划算。权衡公式：期望每轮产出 token 数 $\approx \frac{1-\alpha^{K+1}}{1-\alpha}$，需与每轮前向开销 $K+1$ 倍对比；当远处接受率 $\alpha$ 下降快时，加深度得不偿失。

**Q10（进阶）：MTP 训练时使用真实 token 的词向量作为输入（teacher forcing），推理时却只能用自己预测的 token，这个 train-test 不一致会带来什么问题？怎么缓解？**

答：训练时第 $k$ 模块吃的是 ground truth $t_{i+k}$ 的词向量，推理时草稿链上只能用**自己预测**的 token——一旦前面的草稿错了，后面模块的输入分布偏离训练分布（类似 exposure bias），这正是投机解码链式接受率逐深度下降的根源（例：$\alpha=0.85$ 时深度 3 的接受率约 $0.85^3 \approx 0.61$）。缓解手段：(1) 控制深度（DeepSeek 取 $K{=}1$，单步预测不受链式误差影响）；(2) 训练时对输入做少量扰动/ Scheduled Sampling，模拟预测历史；(3) 推理时依赖主模型验证兜底——即使草稿链错误也只损失速度、不损失正确性；(4) 树形解码（Medusa 风格）不押注单条链，而是保留多候选分支提高整体命中概率。

## 参考资料

1. Gloeckle et al. *Better & Faster Large Language Models via Multi-token Prediction*. Meta AI, 2024.
2. DeepSeek-AI. *DeepSeek-V3 Technical Report*. arXiv:2412.19437, 2024.（MTP 模块结构、$\lambda_1{=}0.3$、1.8 倍解码加速数据）
3. Cai et al. *Medusa: Simple LLM Inference Acceleration Framework with Multiple Decoding Heads*. ICML 2024.
4. Leviathan et al. *Fast Inference from Transformers via Speculative Decoding*. ICML 2023.（投机解码验证机制）
5. vLLM-Ascend 文档：Multi-Token Prediction (MTP) 特性指南. https://docs.vllm.ai/projects/ascend/
6. 相关笔记：本仓库 [LLM/4.大模型推理.md](./4.大模型推理.md)、[LLM/8.大模型解码策略.md](./8.大模型解码策略.md)
