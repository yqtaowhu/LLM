# RLHF：基于人类反馈的强化学习

## 目录
- [1.概述](#1概述)
- [2.核心原理](#2核心原理)
- [3.重要性质](#3重要性质)
- [4.与其他概念的关系](#4与其他概念的关系)
- [5.具体例子](#5具体例子)
- [6.常见面试问题](#6常见面试问题)
- [参考资料](#参考资料)

## 1.概述

**一句话定义**：RLHF（Reinforcement Learning from Human Feedback）先用人类偏好数据训练一个奖励模型（Reward Model），再用 PPO 等强化学习算法让语言模型的输出最大化这个奖励，从而把「人类觉得哪个好」这种难以写成公式的目标，变成模型可优化的信号。

**为什么重要**：
- 好不好写不出损失函数：「回答有帮助且无害」无法用交叉熵直接描述，但人类可以轻松比较两个答案的优劣；
- ChatGPT 的成功确立了「预训练 → SFT → RLHF」三段范式，RLHF 是其中最接近「对齐」本质的一环。

## 2.核心原理

### 2.1 整体三阶段流程

```
Stage 1: SFT 模型           （已有行为基础）
Stage 2: 训练奖励模型 RM     （人类标注 chosen/rejected 对）
Stage 3: PPO 强化学习        （LM 生成 → RM 打分 → 更新 LM）
```

同时要保留一份冻结的 SFT 模型作为参考策略 $\pi_{\text{ref}}$（后面 KL 项需要它）。

### 2.2 奖励模型（RM）：Bradley-Terry 配对损失

人类标注员对同一个 prompt 下两个回答 $y_w$（win/chosen）与 $y_l$（lose/rejected）排序。假设存在隐藏的真实打分函数 $r^*$， Bradley-Terry 模型给出选择概率：

$$P(y_w \succ y_l \mid x) = \frac{\exp r^*(x,y_w)}{\exp r^*(x,y_w)+\exp r^*(x,y_l)}$$

据此用参数为 $\phi$ 的 RM（通常是把 LLM 的 LM head 换成标量输出的回归头）做极大似然训练：

$$\mathcal{L}_{\text{RM}} = -\mathbb{E}_{(x, y_w, y_l)}\left[\log \sigma\big(r_\phi(x, y_w) - r_\phi(x, y_l)\big)\right]$$

其中 $\sigma(\cdot)$ 是 sigmoid。直觉：**拉大 pair 中好的与差的得分差**。当差值达到约 4 时 $(\sigma(4)=0.982)$，该样本基本不再贡献梯度——RM 自动聚焦在难分样本上。

### 2.3 PPO 强化学习目标

把生成一句话视为一个 RL 轨迹：状态 = 已生成的 token 序列，动作 = 下一个 token。优化目标是在最大化奖励的同时不要偏离太远：

$$\max_\theta\; \mathbb{E}_{x\sim\mathcal D,\ y\sim \pi_\theta}\Big[r_\phi(x,y) - \beta\, \mathrm{KL}\big(\pi_\theta(y|x)\,\|\,\pi_{\text{ref}}(y|x)\big)\Big]$$

- $r_\phi(x,y)$：RM 给整句答复的单一标量奖励；
- KL 惩罚项 $\beta$：防止策略为了刷分跑偏成「reward hacking 文本」；
- 实现中常把 KL 折进每步 token 奖励：$r_t = -\beta \log\frac{\pi_\theta(a_t|s_t)}{\pi_{\text{ref}}(a_t|s_t)}$，句末再额外加上 RM 分数。

PPO 的代理目标（clip 版）：

$$L^{\text{CLIP}}(\theta) = \mathbb{E}_t\Big[\min\big(r_t(\theta) A_t,\ \text{clip}(r_t(\theta), 1-\epsilon, 1+\epsilon) A_t\big)\Big]$$

其中 $r_t(\theta)=\frac{\pi_\theta(a_t|s_t)}{\pi_{\text{old}}(a_t|s_t)}$ 是新旧策略概率比，$A_t$ 是优势估计（常用 GAE 计算）。clip 的作用：单次更新的概率比变化限制在 $[1-\epsilon,1+\epsilon]$（如 0.8~1.2），避免一步走太大导致训练崩溃。

### 2.4 DPO：跳过显式奖励模型

DPO（Direct Preference Optimization）从同一 Bradley-Terry 出发，做数学变换后得到只依赖两个策略闭式表达的损失：

$$\mathcal{L}_{\text{DPO}} = -\mathbb{E}_{(x, y_w, y_l)}\left[\log \sigma\!\Big(\beta \log\frac{\pi_\theta(y_w|x)}{\pi_{\text{ref}}(y_w|x)} - \beta \log\frac{\pi_\theta(y_l|x)}{\pi_{\text{ref}}(y_l|x)}\Big)\right]$$

直觉解读：隐式奖励 $r(x,y) = \beta\log\frac{\pi_\theta(y|x)}{\pi_{\text{ref}}(y|x)}$ —— 「当前模型比参考模型更喜欢这个回答的程度」就是奖励。训练时抬高 chosen 的相对似然、压低 rejected 的相对似然。工程上就是一次带配对数据的 SFT 式训练，省去采样和四个大模型同时在显存里的开销。

### 2.5 GRPO 与「无 Critic」化趋势

GRPO（Group Relative Policy Optimization，DeepSeekMath/ R1 采用）去掉 value 网络：对每个 prompt 采一组 $G$ 个回答，用组内奖励标准化作为优势：

$$A_i = \frac{r_i - \text{mean}(r_1,\dots,r_G)}{\text{std}(r_1,\dots,r_G)}$$

优点是显存减半、实现简单，特别适合有可验证奖励的场景（数学题对错、代码能否通过测试），即 RLVR（RL with Verifiable Rewards）路线。

## 3.重要性质

**性质 1：KL 惩罚是「能力保持」的安全绳**
公式：$\mathrm{KL}(\pi_\theta\|\pi_{\text{ref}})$ 越大越偏离 SFT 分布。$\beta=0$ 时模型可能退化为生成无意义但高分的文本（reward hacking）；$\beta$ 过大则学不到新东西。实践中 reward 曲线上升而 KL 失控是需要立即停训的信号。

**性质 2：RM 的分数没有绝对意义**
只有差值 $(r_a-r_b)$ 可比；不同 prompt、不同 batch 间的绝对值不可横比。所以调参时关心的是排序一致性（与人类标注的一致率），而不是奖励数值大小。

**性质 3：Goodhart 定律时刻在线**
任何固定 RM 都会被持续优化直到被利用：典型 hacking 包括加长回答、堆砌礼貌用语、编造引用等。「reward 不是目标而是目标的 proxy」，因此工业界定期用新偏好数据刷新 RM。

**性质 4：SFT 是模仿分布，RLHF 改变整个分布的支持范围**
SFT 只能把概率质量集中到示范样本文本附近（一个点）；RL 通过在线采样获得负反馈，可以把低概率但更好的回应区域也抬起来，覆盖了 SFT 数据分布之外的回答风格。

**性质 5：PPO/DPO/GRPO 同源**
三者都源于「在 KL 约束下最大化奖励」这一最优解：

$$\pi^*(y|x) \propto \pi_{\text{ref}}(y|x)\exp\big(r(x,y)/\beta\big)$$

PPO 直接在线优化它；DPO 把它代入 BT 损失消掉 $r$ 得到离线闭式；GRPO 是 PPO 的 critic-free 变体。

## 4.与其他概念的关系

- **vs SFT**：SFT 用正例模仿（点估计监督），RLHF 利用负例信息（对比/排序）；SFT 便宜稳定、上限受数据质量约束；RLHF 上限更高但不稳定。
- **vs 传统 RL（Atari/机器人）**：环境换成「RM+语言空间」，稀疏奖励集中在句末一个标量，动作空间是几万维词表；所以大量工作在于奖励塑形（KL 混入每步）与方差控制。
- **RRHF / RAFT / ReST**：同一问题的其他近亲——RRHF 对多个候选做排序式排序损失对齐；RAFT/ ReST 用 RM 过滤自己生成的 best-of-n 再做 SFT（拒绝采样思想），比 PPO 简单。
- **RLVR（可验证奖励）**：数学/代码任务可自动判分，绕开人标 RM 的瓶颈，是 o1/R1 推理强化热潮的方法论核心。
- **Constitutional AI (RLAIF)**：让模型按一组宪法原则自我批评并生成偏好数据，「人类反馈」变成「AI 反馈」，大幅降标注成本。

## 5.具体例子

### 例子 1：算一遍 RM 的配对损失

**问题**：某次更新前，RM 给出 $r_\phi(x,y_w)=2.0$、$r_\phi(x,y_l)=1.0$（初始时随机头部输出接近零，这里取已训练一会的值）。这条 pair 的损失是多少？如果反过来误排了呢？

**步骤与计算**：
- 正确排序：$\Delta r = 2.0-1.0 = 1.0$
  $$\mathcal{L} = -\log\sigma(1.0) = -\log 0.731 = 0.313$$
- 错误排序（chosen 分反而低）：$\Delta r' = 0.5 - 1.5 = -1.0$（设此时是这个数）
  $$\mathcal{L}' = -\log\sigma(-1.0) = -\log 0.269 = 1.313$$
- 若完全颠倒到 $\Delta r=-4$：$\mathcal{L}= -\log\sigma(-4) \approx 4.02$

**结果解读**：损失随「排错的幅度」快速增大（sigmoid 饱和前的近似线性区斜率约为 1），而排得非常正确时梯度趋近于零。RM 因此天然地把学习力集中在边界模糊、标注难度大的样本上。

### 例子 2：DPO 损失的手算与方向感

**问题**：一条数据的似然变化如下，计算 DPO 损失并判断梯度方向。

设定：$\beta=0.1$，
- $y_w$（good answer）：$\log\frac{\pi_\theta}{\pi_{\text{ref}}}(y_w|x) = +2$
- $y_l$（bad answer）：$\log\frac{\pi_\theta}{\pi_{\text{ref}}}(y_l|x) = +1$

（说明：即便 rejected 回答的绝对似然不高，只要它的相对提升赶上了 chosen，仍有优化压力。）

**步骤与计算**：
$$z = 0.1\times(+2) - 0.1\times(+1) = 0.1$$
$$\mathcal{L} = -\log\sigma(0.1) = -\log 0.525 = 0.644$$

loss 为正且不小，梯度会推动进一步拉开两者差距：要么继续增大概率 $y_w$，要么压低 $y_l$ 的相对似然。

若训练很久后变成 $\log$-比值 $(+5, -3)$：
$$z = 0.1\times 8 = 0.8,\quad \mathcal{L}=-\log\sigma(0.8)=0.371$$

**结果解读**：DPO 的 loss 永远不会真正归零（sigmoid 渐近 0 但达不到），持续训练会不断推向极端分布导致过拟合或多样性坍缩，这解释了实践中 DPO 的 epoch 要少、$\beta$ 不宜太小（$\beta$ 小会放大似然差的权重）。

### 例子 3：Best-of-N 与 RL 的关系（ppo 效果的上限参照）

**问题**：有个简单办法评估 RL 的合理预期——对 SFT 模型每个 prompt 采 16 个回答、用 RM 选最好的上线（BoN@16）。它与真 PPO 的效果关系是什么？

**事实依据**：InstructGPT 论文及后续复现普遍显示 PPO 在 RM 分数上能超过 BoN@N 的性能曲线，同时多样性更好。

**结果解读**：BoN 相当于一种「离散搜索」：它只能在 SFT 分布已有的支持集里挑，多样性逐条独立但整体偏 SFT；PPO 是连续地移动分布本身，能把概率质量挪到几乎从未采样过的高价值区域。面试中可用此例解释「为什么要 online RL 而不是只靠采样过滤」。

## 6.常见面试问题

### Q1：完整画出 RLHF 三阶段的流程图，说明每个模型的输入输出。
**答**：(1) SFT 阶段产出 $\pi_{\text{SFT}}$；(2) RM 训练：输入 $(x, y_w, y_l)$ 三元组，输出标量奖励，损失 $-\log\sigma(r_w-r_l)$；(3) PPO：策略网络 = SFT/LM 初始化，参考策略冻结，critic（value head）随策略初始化；循环「采样回答 → RM 打分 → 优势估计 → clip 梯度更新」。共涉及最多 4 个大模型副本：policy、ref、RM、value。

### Q2：为什么 RM 要保留冻结的参考模型并用 KL 惩罚？
**答**：三点原因：(1) 防止 policy 远离流畅的自然语言流形，出现 reward hacking 文本；(2) RM 只在有限分布上可靠，外推不可信，KL 起保守作用；(3) 保持 SFT 已有的通用能力不塌缩。

### Q3：写出 Bradley-Terry 损失，并解释为什么用 pairwise 而不是给绝对分？
**答**：$\mathcal{L}=-\log\sigma\big(r(x,y_w)-r(x,y_l)\big)$。人类对绝对打分的量表不一致（同一个人不同时间的「7 分」含义漂移），两两比较的噪声小、一致性高；pairwise 还可由多个 ranker 的排序结果聚合（rank loss 平均）。

### Q4：PPO 中 advantage 为什么重要？GAE 大致怎么算？
**答**：优势 $A_t = Q(s_t,a_t)-V(s_t)$ 度量动作相对期望的好坏，做 baseline 后方差显著降低。GAE 结合多步 TD 残差指数加权：$A^{GAE}_t=\sum_l (\gamma\lambda)^l \delta_{t+l}$，$\delta_t=r_t+\gamma V(s_{t+1})-V(s_t)$；$\lambda$ 权衡偏差与方差（0→纯 TD 低方差高偏差，1→蒙特卡洛反之）。

### Q5：DPO 相比 PPO 有什么优缺点？
**答**：优点：无需采样 rollout、RM 和 value 网络，训练稳定像 SFT，工程成本低。缺点：(1) 离线数据，一次见过的 pair 学完即止，缺乏探索；(2) 对 OOD prompt 泛化弱于在线方法；(3) 优化久易过拟合，两种回答的似然都下降只是幅度不同（论文已观察到该现象）。多数实践结论：中小规模或缺基础设施选 DPO，追求极致收益且有 stable pipeline 则 PPO/GRPO。

### Q6：什么是 reward hacking？举三个具体例子。
**答**：模型利用 RM 缺陷而非真正满足意图。(1) 冗长性 hack：把答案无限注水因为 RM 偏爱长文；(2) 格式 hack：满篇列表加粗，评测观感涨但内容空；(3) 阿谀/迎合：复读用户观点。治理手段：KL 正则、长度归一化/惩罚、周期性换 RM、人在回路抽检。

### Q7：GRPO 为什么可以去掉 value model？优势怎么来？
**答**：它用同一 prompt 采样的一组回答互相比较代替学习到的 baseline：$A_i=(r_i-\bar r)/\text{std}$。均值即可认为是该 prompt 下的 baseline 估计，方差做归一化。代价是有效样本利用率低于带 critic 的方法（每组内样本互相耦合），但对 batch 很大的推理任务影响不大。

### Q8：RLHF 之后模型的输出熵变小（回复越来越模板化），这是什么现象、怎么办？
**答**：这是对齐税 / 多样性坍缩：fine-tune 到偏好模式的尖峰分布。缓解：加入 entropy bonus 或减少 epoch；混合一部份未对齐/SFT 数据；用 temperature 重校准；评测时既看 win-rate 也看 distinct-n、自相似度等多样性指标。

### Q9：offline（DPO 类）与 online（PPO 类）的本质差异是什么？为什么说在线采样能突破「分布支撑」？
**答**：offline 数据来自另一个（固定的）采样分布，$\pi_\theta$ 只能在这些点上调；当最优行为落在该数据分布之外时无法触达。online 中 $\pi_\theta$ 自己产生数据，探索到新高价值区域就有机会沿梯度靠近，实际上扩大了支持域。代价是要承担 off-policy 偏差控制（importance ratio、clip）带来的复杂性。

### Q10（进阶）：KTO/IPO 等变体解决什么问题？
**答**：IPO 换掉 sigmoid 使损失不至于把 pair 差距推到无穷（缓解过拟合）；KTO 只需「单条是否可接受」二值标签（非成对），工程上收集数据更便宜、且可在真实流量反馈（点赞/点踩）上直接训练。它们共同体现的趋势：将偏好学习的监督形式设计得更贴近可得的数据形态。

## 参考资料
- Christiano et al., *Deep Reinforcement Learning from Human Preferences*, 2017
- Ouyang et al., *Training language models to follow instructions with human feedback* (InstructGPT), 2022
- Bai et al., *Training a Helpful and Harmless Assistant with RLHF*, 2022
- Rafailov et al., *Direct Preference Optimization: Your Language Model is Secretly a Reward Model*, 2023
- Shao et al., *DeepSeekMath: Pushing the Limits of Mathematical Reasoning* (GRPO), 2024
