# 大模型分词（LLM Tokenization）

## 目录
- [1.概述](#1概述)
- [2.核心原理](#2核心原理)
- [3.重要性质](#3重要性质)
- [4.与其他概念的关系](#4与其他概念的关系)
- [5.具体例子](#5具体例子)
- [6.常见面试问题](#6常见面试问题)
- [参考资料](#参考资料)

## 1.概述

**一句话定义**：分词器（Tokenizer）是大模型的"第一层入口"——它把任意文本切成一串离散 token 并映射为词表 ID，模型看到的世界自始至终只是这串 ID；生成时再走相反的过程，把 ID 还原成文字。

**为什么重要**：
- 模型**不认识字符，只认识 token**。所有知识都存在"词表 ID → embedding 向量"的查找表里，词表里没有的东西，模型从结构上就无法高效表达；
- 分词质量直接决定**成本与速度**：按 token 计费、上下文窗口按 token 计数，同一段中文用不同分词器可能差出 2-3 倍 token 数，等于价格和有效上下文都差 2-3 倍；
- 分词是一个**鸡肋但关键**的工程决策：它不参与梯度训练、看起来只是"预处理"，但词表一旦定下来就很难换——换词表等于换模型。GPT 系列从 GPT-2 到 GPT-4 换了 3 代分词器，每次都是重新训练级别的决策。

一个容易被忽略的事实：问模型"strawberry 里有几个 r"它可能答错，不是模型笨，而是分词器把 strawberry 切成了几大块，**模型根本没见过逐字母的拼写**——很多"模型的愚蠢"其实是分词器的锅。

## 2.核心原理

### 2.1 问题的形式化：三种粒度的权衡

分词器要回答的问题：**给定语料和目标词表大小 $V$，把文本切成什么单位？**

三个候选粒度，各有死穴：

| 粒度 | 例子 | 优点 | 死穴 |
|---|---|---|---|
| 词（word） | 我 / 爱 / 自然 / 语言 | 语义完整、序列短 | 词表爆炸 + 海量 OOV（未登录词） |
| 字符（char） | 我 / 爱 / 自 ... | 词表极小、零 OOV | 序列极长、单字符语义太弱 |
| 字节（byte） | UTF-8 的 0~255 | 绝对无 OOV | 序列更长、字节无语义 |

$$\boxed{\text{词：序列短但 OOV 多} \quad \Longleftrightarrow \quad \text{字符/字节：无 OOV 但序列太长}}$$

子词（subword）是两者之间的折中：**高频词保持完整，低频词拆成更小的有意义的碎片**。例如 `unhappiness` 拆成 `un` + `happi` + `ness`，三块都是高频语素，既省 token 又有语义。主流大模型（GPT、Llama、Qwen、BERT 等）全部采用子词分词，代表算法是 BPE 及其变体。

### 2.2 BPE：字节对编码（Byte-Pair Encoding）

BPE 最早是一种数据压缩算法（1994 年 Gage 提出），2016 年 Sennrich 等人引入 NLP。核心思想一句话：**从最小的单位出发，反反复复合并"出现次数最多的相邻对"，直到词表达到目标大小**。

**训练过程**：

1. 把训练语料切成最小单位（字符或字节），构成初始词表；
2. 统计语料中所有**相邻符号对**的出现次数；
3. 把次数最多的那一对合并成新符号，加入词表，并用它替换语料中的所有出现；
4. 重复步骤 2-3，直到词表达到预设大小 $V$（如 50K、128K）。

用伪代码表示：

```text
词表 = 所有基础符号
while len(词表) < V:
    pair = argmax 出现次数(相邻符号对)
    词表 += 合并(pair)        # 新符号 = pair 左符号 + 右符号
    语料中所有 pair 替换为合并结果
```

**编码（推理时）过程**：对新文本，先切成基础符号，然后**按训练时学到的合并规则的顺序**（谁先学到谁先合并）依次应用每条合并规则，直到没有规则可再应用。注意不是"贪心找最长"，而是严格按合并序执行——这保证了训练和推理的切法一致。

**关键性质**：BPE 是确定性算法，词表 + 合并规则序列 完全决定切分结果；且任何文本总能被切到最终只剩基础符号，因此**理论上不存在 OOV**。

### 2.3 两个变体：WordPiece 与 Unigram LM

**WordPiece（BERT 采用）**：与 BPE 唯一的区别在"合并谁"的选择标准。BPE 选**出现次数最多**的对，WordPiece 选**使整体语言模型似然提升最大**的对。直觉上：两个符号各自出现 500 次但从不相邻，BPE 不在乎；WordPiece 会比较 $\log p(\text{合并后})$ 与 $\log p(分开)$ 的差，倾向合并"搭配紧"而非单纯"频次高"的对。BERT 词表 30522，`##` 前缀表示词内续片（如 `playing` → `play` + `##ing`）。

**Unigram LM（T5 采用，经 SentencePiece 使用）**：方向与 BPE 相反，是**自顶向下做减法**：

1. 先初始化一个**过大**的候选子词表（比如从语料统计出 500K 个候选）；
2. 假设每个子词独立，用 EM 算法估计每个子词的概率 $p(t)$，文本是"所有可能切分方式"的混合：
$$p(x) = \sum_{s \in S(x)} p(s), \quad p(s) = \prod_{t \in s} p(t)$$
其中 $S(x)$ 是 $x$ 的所有合法切分，$s$ 是一种切分，$t$ 是切分中的子词；
3. 迭代删除"删掉后语料似然损失最小"的子词，直到词表缩到目标大小。

**三者关系**：BPE 是自底向上贪心合并（确定），Unigram 是自顶向下概率剪枝（保留多解、可采样），WordPiece 是换了选对标准的 BPE。工程实现上，Google 的 **SentencePiece** 库支持 BPE 和 Unigram 两种算法（Llama 2 用 SentencePiece BPE，T5 用 Unigram）。

### 2.4 字节级 BPE（Byte-level BPE）：GPT 系的标配

标准 BPE 以 Unicode 字符为基础单位，但 Unicode 字符有 15 万+，初始词表就会爆炸。GPT-2 的解法：**先按 UTF-8 把文本编码成字节流（任何文本都是 256 种字节的组合），再在字节上跑 BPE**。

- 初始词表固定为 256 个字节，**从数学上彻底消灭 OOV**：任何字符——生僻汉字、emoji、小语种、乱码——都是字节，都能表示；
- 缺点：低频字符没机会被合并成整块，只能停留在字节级。比如一个生僻汉字占 3 个字节，若没有对应合并规则，它就消耗 3 个 token；emoji 常见 4 字节，最坏消耗 4 个 token。

实践中的折中：**大词表 + byte fallback**。Llama 2（词表 32K）用 SentencePiece，对进不了词表的字符回落到字节表示；Llama 3（词表 128K）改用 tiktoken 风格的纯字节级 BPE。词表越大，常见字符越容易整体入表，字节 fallback 越少触发。

### 2.5 预分词正则与特殊 token

工程上的 BPE 并不是对全文一口气合并，而是**先按正则表达式切成"预词"（pre-tokenize），每个预词内部独立做 BPE**。GPT-2 的正则把空格归属到词首、数字连续切、按字母/数字/标点分段。这一步的作用：

- 防止跨过空格/标点乱合并（避免把 `dog!` 和 `dog` 合成不同 token）；
- 数字方面，Llama 3/GPT-4 的正则把数字按 **1~3 位一组**切开（如 `12345` → `123` + `45`），缓解模型算术能力差的问题——长度不定的数字若总是一个 token，模型很难学会按位运算。

**特殊 token（special tokens）**：不属于普通文本、有固定 ID 的控制符号，如 `<|begin_of_text|>`、`<|end_of_text|>`（EOS）、`<|im_start|>`（对话角色标记）、`<|pad|>` 等。Llama 3 词表 128256 中有 256 个保留特殊 token。它们的两个作用：标记序列边界与结构（对话模板靠它拼接多轮），以及让模型在生成时能"自我终止"。

## 3.重要性质

### 3.1 可逆性与无损性

分词是**一一映射的编码**：encode 后再 decode 必须完全还原原文（字节级 BPE 天然可逆）。这点和 NLP 里的"归一化分词"（去大小写、去标点）本质不同——大模型的分词器不能丢任何信息，否则生成出来就缺字。注意唯一的不对称：decode 出的字节流按 UTF-8 解释时可能停在多字节字符中间（例如截断处切在汉字 3 字节的中间），生成流式输出时需要处理这种"半个字"。

### 3.2 词表大小：没有免费的午餐

词表大小 $V$ 是分词器唯一的核心超参，两侧都挨打：

- **$V$ 太小**：文本被切得很碎，token 序列长 → 训练与推理都慢（Transformer 计算量近似随序列长度平方增长）、成本高、单 token 语义弱；
- **$V$ 太大**：embedding 层参数量 = $V \times d$，随 $V$ 线性增长（Llama 3：$128256 \times 4096 \approx 5.25$ 亿参数仅 embedding 输入侧）；且低频 token 在语料中出现太少，学不充分，输出头 softmax 计算也更贵。

经验规律：主流模型从 GPT-2 的 50K 演进到 Llama 3 的 128K、Qwen2 的约 152K——**语料越大、多语言需求越强，最优词表越大**。多语言场景是词表膨胀的主要推手：中文若共享英文词表，大量汉字只能走字节 fallback，每字 2~3 个 token。

### 3.3 压缩率（fertility）：衡量分词器的硬指标

**压缩率**定义为平均每个 token 携带的字符数（倒数叫 fertility：平均每个词/字被切成几个 token）：

$$\text{压缩率} = \frac{\text{字符数}}{\text{token 数}}, \qquad \text{fertility} = \frac{\text{token 数}}{\text{字符数}}$$

- 英文：Llama 2 约 3.17 字符/token，Llama 3 提升到约 3.94（词表 32K → 128K 的直接收益）；
- 中文：老词表下常只有 0.7~1.0 字/token，Qwen 这类中文优化词表可达 1.5 字/token 以上。

压缩率差一倍，意味着**同样的上下文窗口能装的内容差一倍、API 账单差一倍**——这就是 Qwen、DeepSeek 强调中文词表优化的原因。

### 3.4 训练与推理的一致性

分词器必须**训练、推理、微调全链路同一个**：同一个词表、同一套合并规则、同一个预分词正则。常见翻车点：

- 微调时用了不同的对话模板/特殊 token，模型行为错乱；
- 前后端各自实现了一遍分词，正则细节不一致（如空格归属），导致模型看到"训练时从没见过的切法"，性能悄悄下降。

### 3.5 切分对语义的干扰（边界效应）

BPE 的切分由**统计频率**决定，不含语义意图，会带来系统性怪象：

- **字母计数失败**：`strawberry` 通常是一整个 token，模型没见过字母序列，数不出 3 个 r；
- **数字运算打折**：数字被不定长合并（`12345` 一个 token vs `12346` 两个），模型难以按位对齐；
- **同词不同码**：`" hello"`（带前导空格）和 `hello`（句首）是不同 token，模型要分别为两者学习语义；
- **对抗扰动**：在词中插入不可见字符或改用 Unicode 变体，可让文本被切成完全不同的 token，绕过安全对齐。

## 4.与其他概念的关系

### 4.1 与 Embedding 层的关系

词表 ID 是 embedding 查找表的行号：token $i$ → 向量 $E_i \in \mathbb{R}^d$。词表大小直接决定 embedding 参数量与 vocab 相关的显存占用（输出 logits 矩阵 $V \times d$ 同样大小，且是训练时 softmax 的瓶颈）。

### 4.2 与上下文窗口、计费的关系

上下文长度、API 价格、生成速度全部以 **token 为单位**而非字符。中文场景下"窗口够不够、账单贵不贵"取决于分词器的中文压缩率。粗略换算：英文 1 token ≈ 0.75 个单词；中文 1 token ≈ 1~1.5 个汉字（取决于分词器）。

### 4.3 与传统 NLP 分词的关系

中文分词（jieba、LTP）是**语言学任务**：目标切出有语义的词（"乒乓球/拍卖/完了"），面向下游特征工程；大模型分词是**统计编码任务**：目标是在预算 $V$ 内最大化压缩，切出来的单位只求"高频、可复用"，不必是语言学上的词（可能是一个词的一半、也可能是一个短语）。

### 4.4 主流实现流派对比

| 维度 | BPE | WordPiece | Unigram LM |
|---|---|---|---|
| 策略 | 自底向上合并 | 自底向上合并（换标准） | 自顶向下剪枝 |
| 选择依据 | 相邻对频次最高 | 似然增益最大 | 删除后似然损失最小 |
| 切分唯一性 | 唯一（按合并序） | 唯一（贪心最长） | 可多解（Viterbi 取最优） |
| 代表模型 | GPT、Llama 3、Qwen | BERT | T5、ALBERT |

工程库：**tiktoken**（OpenAI，字节级 BPE，Rust 实现，快，GPT-4/Llama 3 用）、**SentencePiece**（Google，语言无关、空格视为普通字符，支持 BPE/Unigram，Llama 2/Qwen/T5 用）、**HuggingFace tokenizers**（Rust，以上算法全支持）。

### 4.5 Tokenizer-free 方向：绕过分词器

分词器的痛点（多语言不均衡、数字/代码切分差、每换语言要重定词表）催生了"去分词器"研究：

- **MegaByte**（Meta, 2023）：patch 化字节序列，全局模型处理 patch、局部小模型处理 patch 内字节；
- **Byte Latent Transformer（BLT）**（Meta, 2024）：完全不看词表，按**熵**动态把字节聚成 patch——难的地方切细、简单的地方切粗，计算按需分配，效果追平 Llama 3 同时对噪声更鲁棒；
- 大方向：让"切多粗"由模型动态决定，而不是训练前用固定词表一刀切。目前主流生产模型仍是词表 BPE，但这是明确的演进方向。

## 5.具体例子

### 例 1：手推 BPE 训练与编码

**问题**：给定迷你语料 `low`×5、`lower`×2、`newest`×6、`widest`×3，目标词表从基础字符开始合并 4 轮，最后编码单词 `lowest`。

**步骤**：

1. 切成字符并统计相邻对频次：

| 相邻对 | 来源 | 频次 |
|---|---|---|
| e s | newest×6 + widest×3 | 9 |
| l o | low×5 + lower×2 | 7 |
| e w? 不存在 | — | — |
| e r | lower×2 | 2 |
| e t | — | 0 |
| s t | newest×6 + widest×3 | 9 |

2. 频次最高的是 `e s`（9，先出现者先合并）→ 合并为 `es`，词表加入 `es`；
3. 重新统计：`es t` 出现 9 次（newest、widest 全部）→ 合并为 `est`；
4. 重新统计：`l o` 出现 7 次 → 合并为 `lo`；
5. 重新统计：`lo w` 出现 7 次 → 合并为 `low`。

**计算**（最终词表）：`l,o,w,e,r,n,s,t,i,d` + `es` + `est` + `lo` + `low`。

**编码 `lowest`**：先切 `l o w e s t`；按合并序应用：`es`→`l o w es t`，`est`→`l o w est`，`lo`→`lo w est`，`low`→`low est`。

**结果解读**：`lowest` 编码为 **2 个 token**：`low` + `est`。高频词 `low` 完整保留，后缀 `est` 被共享出来复用于 newest/widest——这正是子词分词"高频保持完整、低频共享碎片"的设计意图。若某天来了个没见过的词 `lowish`，也能切成 `low` + `ish`（或更碎），**永远不会 OOV**。

### 例 2：字节 fallback 处理 emoji 与生僻字

**问题**：文本包含 emoji `👍` 和生僻字 `㵘`（音 màn），分词器词表里都没有这两个字符的整块 token，会怎样？

**步骤与计算**：

1. UTF-8 编码：`👍` → 4 个字节 `[F0, 9F, 91, 8D]`；`㵘` → 3 个字节 `[E3, B5, 98]`；
2. 字节级 BPE 在 256 个基础字节上有完整覆盖，逐字节编码即可；
3. 若词表中有部分常见字节对合并（如 `9F 91`），可以省 1 个 token。

**结果解读**：`👍` 最坏消耗 4 个 token，`㵘` 消耗 3 个 token。**没有 OOV、解码可逆，但代价是高频多字节字符切得碎**——这就是为什么面向中文的模型要特意把常用汉字放进词表：常用汉字一个 token，生僻字 3 个 token，差距体现在成本和窗口利用率上。

### 例 3：压缩率如何决定成本

**问题**：一段 10000 字的中文文档，分词器 A（英文为主词表）中文压缩率 0.7 字/token，分词器 B（中文优化词表）1.5 字/token。API 定价相同（每 1M token 收费），比较两者成本，以及放进 128K 上下文窗口的可行性。

**步骤与计算**：

1. 分词器 A：token 数 = $10000 \div 0.7 \approx 14286$ 个；
2. 分词器 B：token 数 = $10000 \div 1.5 \approx 6667$ 个；
3. 成本比：A 是 B 的 $14286 \div 6667 \approx 2.14$ 倍，**同样的字数多花一倍钱**；
4. 窗口占用：128K 窗口下，A 只能放约 $131072 \times 0.7 \approx 9$ 万字，B 能放约 $131072 \times 1.5 \approx 19$ 万字。

**结果解读**：压缩率不只是"省 token"，它同时决定了**有效上下文长度**和**生成速度**（每秒生成的字符数 = token 速率 × 压缩率）。选模型时只看"128K 窗口"不看词表，中文场景可能实际只等价于别人的几十 K。

## 6.常见面试问题

**Q1：为什么大模型不直接用"词"或"单字"作为基本单位，而要用子词？**

答：词粒度下词表随语料爆炸、OOV 海量，embedding 矩阵存不下也学不好；单字粒度序列太长（Transformer 计算量随长度近似平方增长），且单字语义太弱、模型要把语义"攒"在更多层里。子词是折中：高频词完整保留（序列短、语义强），低频词拆成可复用碎片（词表可控、无 OOV）。关键公式：embedding 参数量 $V \times d$ 与序列长度两边的 trade-off。

**Q2：简述 BPE 的训练流程和编码流程，两者为什么必须严格一致？**

答：训练：从基础符号开始，每轮把**频次最高的相邻符号对**合并入词表，直到达到词表大小 $V$；编码：新文本先切基础符号，再**按训练时学到的合并规则顺序**依次应用。必须一致的原因：切分结果是"词表 + 合并序"的确定性函数，推理时的切法若与预训练不同，模型会遇到大量训练时没见过的 token 组合，分布偏移导致性能下降。

**Q3：WordPiece 和 BPE 的区别是什么？**

答：合并候选的选择标准不同。BPE 选频次最高的相邻对；WordPiece 选使语料语言模型似然增益最大的对，即比较合并前后概率 $\log p(x)$ 的变化。直觉上 BPE 是"数次数"，WordPiece 是"看搭配是否紧密"——两个符号各自高频但总是不相邻，WordPiece 不会合并它们。BERT 词表 30522，用 `##` 标记词内续片。

**Q4：Unigram LM 和 BPE 在方法论上的本质区别？**

答：方向相反。BPE 自底向上：从最小单位**做加法**贪心合并，切分唯一确定；Unigram 自顶向下：先建超大候选词表，假设每个子词独立、文本概率是所有切分的混合 $p(x)=\sum_{s \in S(x)} \prod_{t \in s} p(t)$，用 EM 估计子词概率后**做减法**迭代删除似然损失最小的子词。Unigram 保留概率化的多解性，可用 Viterbi 算法取最优切分，也可采样实现数据增强。

**Q5：什么是 Byte-level BPE？它如何彻底解决 OOV？代价是什么？**

答：先把文本按 UTF-8 编码成字节流（任意文本都是 256 种字节的序列），再在字节上跑 BPE。由于任何可编码文本都在 256 字节的覆盖范围内，**数学上不存在 OOV**。代价：低频字符（生僻字、emoji、小语种）停留在字节级，一个字符消耗 2~4 个 token，压缩率差；缓解方式是加大词表（Llama 3 用 128K），让常见字符能合并成整块。

**Q6：词表大小如何选择？过大和过小各有什么问题？**

答：核心是三边权衡：序列长度（与 $V$ 负相关，影响计算量与压缩率）、embedding/输出头参数量 $V \times d$（与 $V$ 正相关，影响显存与训练量）、低频 token 的学习充分性（$V$ 大则很多 token 语料中出现太少学不好）。经验上随语料规模和多语言需求增长：GPT-2 50K → Llama 3 128K → Qwen2 约 152K。Fertility（平均每字切成几个 token）是验证词表是否够大的实用指标。

**Q7：什么是压缩率（fertility）？为什么中文场景特别在意它？**

答：压缩率 = 字符数 / token 数，衡量每个 token 携带的信息量。影响三件事：API 成本（按 token 计费）、有效上下文长度（窗口按 token 计数）、生成速度（每秒字符数 = token 速率 × 压缩率）。中文在英文主导的词表里大量字符走字节 fallback（每字 2~3 token，压缩率可能不到 1），而中文优化词表（Qwen 约 152K）可达 1.5 字/token——同样的中文任务，token 数、账单、窗口占用都可能差一倍以上。

**Q8：特殊 token（special tokens）有什么用？使用时有哪些坑？**

答：它们是词表中保留固定 ID 的控制符号，不参与普通文本切分，用于：标记序列边界（BOS/EOS）、拼接对话结构（`<|im_start|>` 等角色标记）、提供终止信号（模型生成 EOS 即停止）、padding。坑点：对话模板必须与训练时完全一致（多一个少一个换行都会掉点）；用户输入中若混入特殊 token 字符串需转义，否则可能被解析成控制符号造成注入；微调时新增特殊 token 会改变 embedding 矩阵尺寸，需小心初始化。

**Q9：为什么大模型数不好 "strawberry" 里有多少个 r？这和分词有什么关系？改进思路有哪些？**

答：BPE 按统计频率切分，`strawberry` 通常整体或大块编码成一个 token，模型在训练中从没见过"逐字母展开"的形式，缺少字符级的中间表示，无法可靠计数。改进思路：让分词保留字符信息（数字按 1~3 位切分的正则同理是为算术服务）；对字符级任务构造训练数据（教模型"拼写"）；更根本的是字节级/动态分词方向（如 BLT），保留完整字节信息让模型可以按需"下钻"到字符。

**Q10：Tokenizer-free（无分词器）路线是什么？BLT 的核心思想是什么？它可能取代 BPE 吗？**

答：MegaByte 和 Byte Latent Transformer（BLT，Meta 2024）直接建模原始字节流，绕开固定词表。BLT 核心是用一个小模型按**下一字节熵**动态切 patch：预测不确定性高的位置切得细（信息密集），确定性强的地方切得粗，全局 Transformer 只在 patch 间计算、局部模型处理 patch 内字节，从而把计算量"按需分配"。论文显示其可追平同规模 Llama 3 且对输入噪声更鲁棒。短期内 BPE 因生态成熟（工具链、推理优化、可复现）仍是主流，但多语言与代码场景的痛点使 tokenizer-free 是明确的中期方向。

## 参考资料

- Sennrich et al. *Neural Machine Translation of Rare Words with Subword Units* (ACL 2016) —— BPE 引入 NLP
- Schuster & Nakajima. *Japanese and Korean Voice Search* (ICASSP 2012) —— WordPiece
- Kudo. *Subword Regularization* (ACL 2018) —— Unigram LM 与 SentencePiece
- Radford et al. *GPT-2 Technical Report* (OpenAI 2019) —— 字节级 BPE
- Meta. *The Llama 3 Herd of Models* (2024) —— 128K 词表与 tiktoken 实践
- Pagnoni et al. *Byte Latent Transformer: Patches Scale Better Than Tokens* (Meta 2024)
- Yu et al. *MegaByte: Predicting Million-byte Sequences with Multiscale Transformers* (2023)
- 开源库文档：HuggingFace tokenizers、Google SentencePiece、OpenAI tiktoken
