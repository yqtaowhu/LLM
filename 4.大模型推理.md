# 大模型推理（LLM Inference）

## 目录
- [1.概述](#1概述)
- [2.核心原理](#2核心原理)
- [3.重要性质](#3重要性质)
- [4.与其他概念的关系](#4与其他概念的关系)
- [5.具体例子](#5具体例子)
- [6.常见面试问题](#6常见面试问题)
- [参考资料](#参考资料)

## 1.概述

**一句话定义**：大模型推理是把训练好的 Transformer 权重变成线上服务的过程：自回归地逐 token 生成回答，核心挑战是在延迟（首 token 延迟 TTFT、token 间延迟 TPOT）和吞吐量之间取得平衡，同时把昂贵的显存用到极致。

**为什么重要**：
- 一个在线服务的推理成本常年高于其一次性训练成本；规模化后 GPU 是最大开支项；
- 预训练/SFT 的能力要靠推理阶段的系统优化与解码策略才能兑现成用户体验。

## 2.核心原理

### 2.1 推理的两个阶段：Prefill 与 Decode

一次请求的生命周期分两段：

```
Prompt: "介绍一下北京"   (7 个 token)
        │
        ├─ Prefill（预填充）：整段 prompt 并行前向，生成所有位置的 KV
        │    特点：计算密集型（大矩阵乘法），GPU 利用率高
        ▼
   第一个 token 输出 → TTFT（Time To First Token）
        │
        ├─ Decode（解码）：逐 token 自回归，每次只算一个新 token
        │    特点：访存密集型——每步都要读全部权重和 KV Cache，
        │            算术强度极低，瓶颈在带宽而非算力
        ▼
   <eos> 结束 → 总时间 = TTFT + (N_out − 1) × TPOT
```

这个区分是所有推理优化的出发点：prefill 慢于算力不足、decode 慢于带宽不足。

### 2.2 KV Cache：空间换时间的基石

自回归生成时，历史 token 的 K/V 若每步都重算则是 $O(T^2)$ 前向；观察表明注意力只依赖「旧 token 的 K/V」，它们一旦算出就不变——于是缓存下来：

```
步骤 t 的计算：
  新 token 只有 q_t, k_t, v_t 需要现算   （1 个位置的 QKV）
  与缓存的 K[1..t−1], V[1..t−1] 拼接     → attention 只看过去
  追加 k_t, v_t 进缓存                    → 显存持续增长
```

单条序列的 KV Cache 字节数公式：

$$\text{KV bytes} = 2 \times n_{\text{layers}} \times n_{\text{kv-heads}} \times d_{\text{head}} \times L \times b$$

其中 2 = K 和 V 两份，$L$ 为当前序列长度，$b$ 为每元素字节数（fp16=2）。以 GQA（分组查询注意力）后每层 kv-head 数通常小于 q-head 数，是省显存的关键设计（如 Llama-3 用 GQA 把 kv heads 从 32 减到 8）。

### 2.3 解码采样策略

从词表分布 $p(x_t|x_{<t})$ 中选下一个 token 的方式：

| 策略 | 做法 | 特点 |
|---|---|---|
| Greedy | 取 argmax | 确定性，易重复 |
| Temperature | 先除 $T$ 再 softmax：$p_i \propto \exp(z_i/T)$ | $T{<}1$ 更尖更确定，$T{>}1$ 更平更多样 |
| Top-k | 只保留概率最高的 k 个重新归一化 | k 固定不适应分布陡峭程度 |
| Top-p (nucleus) | 从高到低累积到概率 p 截断 | 动态候选数，主流默认 |
| Repetition penalty / frequency penalty | 对已出现 token 打折 | 抑制复读 |

温度作用的直觉：$\log p$ 分布被除以 $T$ 后，相邻候选的分差被压缩或放大 $T$ 倍；$T\to 0$ 退化为贪心。工业对话常用 $T\in[0.6,0.8]$，代码补全用更低值保稳定。

### 2.4 Continuous Batching 与 PagedAttention（vLLM）

- **静态 batching 的浪费**：一批里最长的请求未结束时，其余早已完成的槽位空转；
- **Continuous batching（迭代级调度）**：每个 decode step 之后动态插入/移除请求，slot 实时复用，吞吐提升可达数倍到十倍以上；
- **PagedAttention**：借鉴操作系统虚拟内存分页，把每条序列的 KV Cache 切成固定大小的 block（如 16 tokens/块），按需分配 + 写时复制，加上 prefix caching 跨请求共享公共前缀——解决静态预留导致的碎片化（内部/外部碎片），使 batch 可以塞得更满。

### 2.5 投机解码（Speculative Decoding）

利用「小模型起草、大模型验证」绕开 decode 的访存瓶颈：

```
草稿模型 M_draft（小）连采 k 个 token：y1..yk        （很便宜）
目标模型 M_target 对 y1..yk 一次并行前向打分          （等价于 prefill）
从左到右逐个验证：以 min(1, p_target/p_draft) 的概率接受
首个拒绝处回退、并按修正分布重采一个 token
```

关键性质：验证采用 **reject sampling 修正**后，输出分布在数学上严格等于直接用目标模型采样（无损）。加速比取决于小模型对大模型的「命中率」，实践中可达 2~3 倍； Medusa / EAGLE 类方法则改为多个解码头或特征层预测，省去独立草稿模型。

### 2.6 量化与显存估算

- **权重量化**：W8A16（int8）、W4A16（GPTQ/AWQ，常见 4bit）、W4A8 等。AWQ 观察到少量显著通道（salient channels）对精度重要，按激活统计保护它们；GPTQ 基于 Hessian 的逐层误差补偿；
- **KV cache 量化**：FP8 / INT8 KV 已普遍应用于长上下文服务；
- **全量模型显存近似**：参数量 × 每参数字节 + 激活 + 缓存。快速估算：7B fp16 ≈ 14GB；INT4 ≈ 3.5~4GB。

## 3.重要性质

**性质 1：Decode 阶段是 memory-bound**
算术强度 $\approx O(1)$ FLOP / byte（batch 内所有请求共享同一份权重读取）：批越大摊薄越充分。这就是 continuous batching 能大幅提吞吐的根本原因——weight 的读取成本被批内均摊。

**性质 2：RoPE 可顺带做相对位置编码与 Cache 兼容**
注意力的内积分解保证新 query 只需和缓存中所有 key 做点积即可，无需改动模型结构——KV Cache 是任何 decoder-only 模型免费获得的性质。

**性质 3：输出长度主导延迟方差**
总延迟 $\approx \alpha + \beta N_{out}$，尾部用户（要求长答案的请求）决定 P99。调度器据此可以做 preemptible（可抢占）/ 分块 prefill（chunked prefill），避免一条超长 prefill 卡住整个 step。

**性质 4：投机解码的加速上限由命中率决定**
平均接受长度 $\tau$ 在 $(1,k]$ 区间，加速比上界约为 $\frac{k+1}{k\cdot c+1}$ 形式（$c$ 为 draft/target 时间比）；若 draft 太差（命中低）反而变慢。因此「同族蒸馏的小 draft」效果最好。

**性质 5：量化误差不是均匀的**
outlier（离群激活）集中在少数通道/维度（LLM.int8 的发现），朴素 per-tensor int8 会全军覆没在这些维度；混合精度（敏感层保 fp16）或更细粒度 scale（per-group/blockwise）是必须项。

## 4.与其他概念的关系

- **vs 训练阶段**：训练批大且 token 全可见（teacher forcing），追求 FLOPs 利用率；推理 decode 批内每个序列长度不一、逐 token 进行，追求显存与调度效率。
- **MQA/GQA 与 MHA**：MHA 每 head 一份 KV；MQA 所有 head 共享一份（省太多但损精度）；GQA 是折中（组间共享），现代模型标配。
- **vLLM / TensorRT-LLM / SGLang**：都是围绕上述原理的系统实现，差异在调度策略（SGLang 有 RadixAttention 前缀树复用）、kernel 优化、分布式支持。
- **MoE 架构的推理特殊性**：路由稀疏激活降低了 FLOPs 但权重总量巨大，显存压力不减反增，加速手段转向专家并行与细粒度量化。

## 5.具体例子

### 例子 1：手算 KV Cache 显存

**问题**：Llama-3-70B 风格配置：80 层，隐藏维 8192，64 个 query heads，head_dim=128，GQA 下 8 个 kv heads，fp16 存储。求单个请求生成长度 4096 时 KV Cache 大小。若是 batch=32 并发会怎样？

**步骤与计算**：
每 token 每层的 KV 元素数：$2\times(\text{kv\_heads}\times d_{head})=2\times(8\times128)=2048$
总元素数：$2048\times 80\times 4096 = 671{,}088{,}640 \approx 6.71\times10^8$
字节：$\times 2 = 1.34\ \text{GB}$（约 1.25 GiB）

batch=32：$1.34\times 32 = 43\ \text{GB}$ —— 仅 KV 就吃掉一张 A100-80G 的过半显存（还没算约 140GB 的 fp16 权重本身，需要多卡）。

**结果解读**：(1) KV 随并发×长度线性膨胀，这就是 PagedAttention 必须出现的原因；(2) 若无 GQA（kv_heads=64），同样的计算结果是 8 倍即 ~10.7GB/请求——说明架构层面的省 KV 设计比运维调参威力更大。

### 例子 2：Prefill 与 Decode 的耗时对比（为什么两阶段不同质）

**问题**：假定硬件 A100 的内存带宽 2TB/s，某个 7B fp16 模型权重 14GB。(a) batch=1、纯 weight-bound 的 decode 每个 step 最少要多久？(b) 输出 500 token 的最小延迟？

**步骤与计算**：
- (a) 每 decode step 至少读一遍全部权重：$t_{step}\ge \frac{14\,\text{GB}}{2000\,\text{GB/s}} = 0.007\ \text{s}= 7\,\text{ms}$，理论上限速率约 143 token/s；
- (b) $500 \times 7\,\text{ms} = 3.5\ \text{s}$（实际因 kernel 启动、cache miss 常慢 2~5 倍）。

对比 batch=32 时同样读一次权重服务 32 个 token，单卡「有效速率」变为 32×143 ≈ 4580 token/s。

**结果解读**：这解释了两个现象：一、为什么单请求流式输出速度有物理下限、提高并发几乎不掉点（直到带宽饱和）；二、为什么推理压测要看 throughput 与 latency 的 trade-off 曲线而非单点指标。

### 例子 3：Top-p 采样截断示例

**问题**：分布为 {猫：0.45，狗:0.30，鸟:0.15，鱼:0.07，马:0.03}。取 top-p=0.85 时的候选集是什么？top-k=2 呢？

**步骤**：按降序累计：猫 0.45 → +狗 0.75 → +鸟 0.90（超过 0.85，截止）。故候选={猫，狗，鸟}（鸟虽让累计越过 p 也被包含，因为达到阈值的那一步必须收进来）。Top-k=2 则只剩{猫，狗}，重归一化为 (0.6, 0.4)。

**结果解读**：当模型非常确定（比如代码场景第一个候选就有 0.99 概率）时，top-p 自动退化为只有 1 个候选——「概率陡就少抽签、概率平时多抽签」，这是它优于固定 top-k 的本质。

## 6.常见面试问题

### Q1：推理为什么要用 KV Cache？没有它会怎样？
**答**：自回归生成每步新增一个 token，若不缓存则要对整个前缀重算 K/V，复杂度 $O(L^2)$ 且大量重复计算；缓存后每步只需算新 token 的 1 个位置，代价是显存随序列长度线性增长。它是 decode 阶段一切优化的前提。

### Q2：KV Cache 的显存怎么估算？有哪些减小它的手段？
**答**：bytes = 2×层数×kv_head 数×head_dim×seq_len×dtype_bytes×batch。手段：(1) MQA/GQA 结构性减少 kv heads；(2) PagedAttention 去碎片化提高并发；(3) KV 量化（FP8/INT8/KV6）；(4) 滑窗注意力/StreamingLLM 丢弃远端缓存；(5) MLA（DeepSeek 的低秩联合压缩 KV）。

### Q3：Prefill 和 Decode 分别是 compute-bound 还是 memory-bound？各有什么对应优化？
**答**：Prefill：prompt 并行进网络，是大矩阵乘 → compute-bound，优化靠 FlashAttention、chunked prefill、prefix reuse。Decode：一次处理 1 个 token 却要读全部权重 → memory-bound，优化靠 large-batch/continuous batching、投机解码（转化为并行验证的 prefill 型计算）、权重低比特量化。

### Q4：简述投机解码的流程及其「无损」的原因。
**答**：draft 连采 k 步 → target 一次并行前向 → 逐个位置以 $\min(1,\frac{p_t}{p_d})$ 接受，拒绝则按残差分布 $\max(0,p_t-p_d)$ 重采样。无损证明：该接收-重采样机制构造了与 $p_t$ 完全一致的边缘分布，因此期望输出等同直接采样目标模型。

### Q5： temperature 和 top-p 该怎么组合使用？为什么要避免都拉高？
**答**：temperature 控制 logits 平滑度，top-p 控制候选范围截断。两者叠加风险：先升温抹平差距再截断，可能让真正的低质候选进入集合，产生胡言乱语。实践建议：选定其一为主（常 T=0.6~0.8 + top_p=0.9~0.95 默认），高风险任务（代码、数学）低温甚至 greedy。

### Q6：Continuous batching 和普通 dynamic batching 的区别？
**答**：dynamic batching 攒一批请求一起跑完整生命周期（长短请求互相等待）；continuous batching 在每步 decode 后即时加入新请求、踢出完成请求，batch 组成随时变化，消除队头阻塞，vLLM/ORCA 论文实测吞吐提升 2~20 倍。

### Q7：GPTQ 和 AWQ 各自的核心思想？
**答**：GPTQ：逐列量化并用近二阶信息（Hessian 逆）把量化误差摊派到尚未量化的其他权重上，最小化全局重建误差。AWQ：依据激活分布识别少数 salient weight 通道，通过对这些通道配 per-channel scaling 把它们移到不易被舍入破坏的位置，实现无反向传播的训练无关量化。工程上 AWQ 更简单稳健，GPTQ 精度上限略好。

### Q8：一个用户反馈「首字很慢但后面流畅」，你会从哪些角度排查？
**答**：TTFT 高的可能原因：(1) 长 prompt 的 prefill 计算耗时 → chunked prefill / 缩短上下文；(2) 未命中 prefix cache，重复 system prompt 反复重算 → 开启前缀复用；(3) 排队等待（并发高峰）→ 扩容或调度优先级；(4) 网络/TLS/首次加载。TPOT 正常说明 decode 侧健康，重点查 prefill 与排队。

### Q9：什么是 prefix caching？它为什么在 Agent/RAG 场景收益特别大？
**答**：把相同前缀（system prompt、few-shot 示例、检索到的长文档）对应的 KV 存下来供后续请求复用，命中时跳过其 prefill。RAG/Agent 的公共上下文占比极高且多次调用前缀不变，TTFT 可从秒级降到几十毫秒，成本大幅下降。需注意哈希匹配与缓存失效管理。

### Q10（进阶）：如何为一个上线服务设定 SLO（如 TTFT<500ms、TPOT<50ms）下的最大并发？给出你的容量规划思路。
**答**：思路：(1) 以代表性流量分布建模输入/输出长度；(2) 用「带宽守恒」估 token/s 上限 ≈ 有效带宽/权重字节，对照 decode 负载；(3) 结合 PagedAttention 的 KV 容量约束得出并发上限；(4) 压测拟合 latency-throughput 曲线找拐点留 30% 余量；(5) 长尾场景（超长输出）单独隔离部署。考察点是否理解「decode 由带宽决定、并发上限由 KV 显存决定」这两条主线。

## 参考资料
- Pope et al., *Efficiently Scaling Transformer Inference*, 2022
- Kwon et al., *Efficient Memory Management for Large Language Model Serving with PagedAttention* (vLLM), 2023
- Leviathan et al., *Fast Inference from Transformers via Speculative Decoding*, 2022
- Frantar et al., *GPTQ: Accurate Post-Training Quantization*, 2022; Lin et al., *AWQ*, 2023
- Yu et al., *ORCA: A Distributed Serving System for Transformer-Based Generative Models*, 2022
